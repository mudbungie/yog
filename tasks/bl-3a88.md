+++
title = "tool_host::remote a_stop_ends_the_wait_on_the_tool flakes under full-suite load: a sleep/poll race, seen three times across three gates in one day"
created = 1786767027
updated = 1786843335
claimant = "Flake3a88"
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
Pre-existing race, not tied to any one change: the test passes isolated and on re-run, and reddened once each under load in the bl-4841, bl-f297 and bl-1747 gates (all 2026-08-14). Sibling of the same class seen once: actions::verbs::tests::assign_claims. Both look like sleep/poll timing races. Diagnose the wait shape in tool_host::remote's stop path (the test asserts a stop ends the wait — under load the wait may be granted after the deadline the test allows) and fix the test's synchronization to an event rather than a duration, or the product's wait if the race is real. Verify against the tree; this body drifts.