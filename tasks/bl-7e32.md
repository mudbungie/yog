+++
title = "context-menu seats per §11 doctrine: conversation and ball rows, and the unpin sole-carrier fix"
created = 1785287630
updated = 1785373424
claimant = "entrance-7e32"
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Implements the §11 context-menu doctrine's v1 seats (bl-ef89), excluding the workspace tab's Delete entry (that rides bl-0ccc with the verb it needs). Scope: (1) conversation-row context menu carrying Stop (+children) and Flush — pointer-targeted at the row under the cursor, and opening it neither focuses nor acknowledges the row (right-click is not the §6 gesture); the composer-side affordances stay the visible selection-targeted carriers. (2) ball-row context menu carrying Assign / Move (destination submenu) / Release / Close, accelerating the §8.2 ball actions beside the composer. (3) The standing violation fix: unpin today lives only in the pinned tab's context menu (src/shell/navigator.rs) — the overflow menu must keep listing a pinned foreign/replay entry with its ★ lit as the visible pin/unpin toggle (pinning changes where an entry ALSO appears, never where it lives), demoting the tab menu's unpin to the accelerator the doctrine allows. Every menu verb must already exist on a visible affordance or a key: delete every context menu and the UI loses clicks, never capabilities.