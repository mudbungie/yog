+++
title = "S0 polish: bootstrap composer clipped off-screen at default window size"
created = 1784605132
updated = 1784605132
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Live S0 walk (installed yog, 900x774 default window, GNOME): the empty-world bootstrap composer in src/shell/workspace.rs::bootstrap lays out horizontally (label 'start a conversation:' + text_edit_singleline + Start button in one ui.horizontal row). The left roster panel takes ~600px, altitude-1 gets ~300px, the label alone consumes it — THE TEXT BOX AND START BUTTON ARE OFF-SCREEN at default size. Screenshot evidence in session scratchpad (yog-s0e.png: 'start a conversat' clipped, no box visible). STORIES S0 step 1: 'the window opens to the composer, focused' — the box must be visible and usable at the default window size, no resize required. Fix (shell paint-only): stack the bootstrap composer vertically — invitation label, full-width TextEdit (ui.add_sized or TextEdit::desired_width(f32::INFINITY)), Start button below — and give the TextEdit initial focus (request_focus on first frame; Enter submits via lost_focus+enter or keep the button as the gesture per current fire_bare). Also consider bounding the roster SidePanel default/max width (e.g. default_width ~280, resizable) so altitude-1 has room at 900px — check shell/mod.rs SidePanel setup. Keep it glue-only; no logic moves. Gate: make check green; the change is coverage-excluded shell paint but must not break acceptance tests (shell/acceptance.rs).