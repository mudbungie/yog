+++
title = "composer becomes the inbox: items land above your typing, a rising fold line, send snaps it down, commit-boundary rules persist through the chat"
created = 1785733346
updated = 1785733460
claimant = "inbox-designer"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
CLOSED-SCOPE DESIGN ball, delivered: docs/DESIGN.md amended with the inbox-composer contract (§11 bottom accessories), the persistent commit-boundary transcript rule (§11 Transcript, 'Every crossing leaves its line'), §5.1 #29, §5.3 fold-override amendment, and the in-flight strip seat re-ruled preserved.

Operator design ruling 2026-08-03, verbatim (recorded in DESIGN): 'I think the user input should be overhauled to be the inbox. You type, you're typing into the inbox. Other items that are coming in: you see them land in the inbox above your text input. There is a horizontal rule that goes up to see more items pile in, or the user types enough to push it up (anything but the user input gets a fold arrow, ofc), and then when the prompt goes out, it snaps back down, making it clear that the input was pushed "over the line" into the prompt. Also, since that's a commit boundary, that line, such as it is, should persist, these lines (faint, gray, with a commit id on the right side) should persist through the entire chat.'

Central ruling, OPERATOR-RULED verbatim: 'deposit at send. typing in a box shouldn't hit disk, and it also shouldn't auto-trigger the agent on turn. That's the one lie of this system, it's not _actually_ there until you hit enter.' The typed draft is the existing per-target yog RAM draft, rendered as the queue's last item; it becomes a lernie deposit only at Enter, through the unchanged verbs. Draft-as-live-deposit lost three ways: lernie's delivery drain is unconditional (a live driver would missend half-typed text), the new-conversation target has no inbox (two homes by construction), and I2 forbids yog hand-writing workspace internals.

Fold line derived from structure (items + wrapped draft, floor = bare input, cap = half pane); empty inbox is the general path. The cut is the commit: crossed = what lernie's drain renamed and committed, pending = what sits in inbox/ — yog holds no membership claim. Persistent boundary rules: one per maximal delivered run, id = ensuing step's meta.json commit (bl-98da's spine read); absence of a commit = no line. Snap is render-layer ephemera, structurally triggered. In-flight strip seat preserved above the fold line. No new Action variants (bl-8aab).

Implementation balls filed, claim-gated on this ball: bl-a119 (inbox-composer surface: queue + fold line + snap), bl-95a9 (transcript boundary rules: crossing lines + commit id). No lernie-side ask exists — the losing ruling was the one that needed one.