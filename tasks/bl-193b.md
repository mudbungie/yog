+++
title = "real-substrate story drive: Xvfb harness + drive S0/S1 against the live wire"
created = 1784696638
updated = 1784696638
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-abd9"
on = "claim"
+++
## Intent
STORIES' done-bar has two halves; the second ('the flow works against the real one') now has a working harness, proven 2026-07-21: scripts live in session scratchpad as yogdrive.sh — graduate them to scripts/drive/ and document the protocol in STORIES.md.
## The proven protocol
- Isolated display: Xvfb :99 (never the user's seat — synthetic input on the live seat leaks keystrokes into whatever the user has focused; this happened, see session log 2026-07-21).
- Launch: env -u WAYLAND_DISPLAY DISPLAY=:99 XDG_DATA_HOME=<scratch> yog; find window by pid via xdotool search --name '^yog$'; window ~1150x760@1x.
- Input: xdotool windowfocus + XTEST type/key/click (works natively on Xvfb; blocked by mutter on the user's Wayland seat).
- Capture: ffmpeg -f x11grab -window_id 0x<hex> -i :99.
- Scratch world seeding BEFORE launch (operator config, mirrors a real machine): $scratch/yog/world/lernie/models.yaml with a codex/gpt-5.4 entry; $scratch/yog/world/lernie/template/providers.yaml (codex/gpt-5.4 both roles — consumed by lernie new's config-root template override, lernie d7cad09); symlink $scratch/brazen/credentials -> ~/.local/share/brazen/credentials.
- Assertions: screenshots + ops.jsonl + workspace dirs (steps/*/response.json, agents/*/messages/*).
## Result 2026-07-21 (pre-rework binaries)
S0 drive green to the wire: composer->Enter -> prime/new/prompt -> workspace born codex via template override -> gpt-5.4 replied 'Wire check OK.' on disk. UI failed the payoff (reply never took the view) — that is bl-abd9's scope; re-drive after it lands.
## Deliverable
scripts/drive/yogdrive.sh (+ a story-drive runner for S0/S1: bare start, seeded restart, prompt-into-existing, message-to-agent), STORIES.md 'real-substrate drive' section, and a drive log of the post-rework run.