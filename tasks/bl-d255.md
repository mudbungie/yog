+++
title = "src/boundary/action.rs rests at 298 of the 300 cap with no seam: split it before the next gesture lands on the wall"
created = 1788754120
updated = 1788754120
priority = 3
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["usability-r3"]
+++
From lane Y8: one enum with doc-heavy variants at 298 lines. The next variant hits the wall at the moment someone is finishing something else (bl-b278's exact shape). Split along a real seam — e.g. the enum's docs into a sibling that the module map names, or the variants grouped by the DESIGN §8.5 gesture families into submodules re-exported from action.rs — and add the DESIGN §12 rows.