+++
title = "Automatic upstream code delivery is broken: bl close seals outside git-commit so .githooks/post-commit never fires — wire a close.post push plugin"
created = 1784350040
updated = 1784350040
priority = 1
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Evidence: after 5 bl closes landed on local main (through faccc40), origin/main still sat at ca2f3bb; refs/heads/balls/tasks meanwhile advanced (bl-tracker close.post works). The .githooks/post-commit hook (added in bl-d5f3) assumed delivery commits fire git hooks; bl-delivery's seal does not (the root checkout's pre-commit gate is run by bl itself, not via git commit).

Fix, balls-native (plugins are the sanctioned extension seam):
1. Add scripts/bl-push-main to the repo: a tiny sh plugin that ignores stdin and runs git push --quiet origin main from the invocation-path repo; it must ALWAYS exit 0 (fail-open — a failed push warns on stderr; a non-zero exit in close.post would trigger plugin rollback and un-seal a commit the remote may already have). Mirror .githooks/post-commit's guards (origin exists).
2. Wire it in THIS checkout's landing: symlink into the landing's config/plugins/bin/ (bl conf writes the schedule, never binaries — create the symlink directly: ln -s /home/u/dev/yog/scripts/bl-push-main '/home/u/.local/state/balls/clones/%2Fhome%2Fmark%2Fdev%2Fyog/config/config/plugins/bin/bl-push-main'), then bl conf append close.post bl-push-main (after bl-tracker; harmless because the plugin cannot fail). Also add a [source] hint? No — checkout-local wiring, no acquisition story needed; skip.
3. Keep .githooks/post-commit (still correct for ordinary manual commits on main, if any ever happen legitimately); correct the README Delivery section to describe the real mechanism: bl close -> close.post -> bl-push-main pushes main; bl-tracker pushes balls/tasks.
4. Verification IS the close: this task's own bl close must fire the new plugin. After close, git ls-remote origin main must equal local main. Report both hashes.

Note the wiring in step 2 is checkout-local config (landing + symlink), not repo files — do it from /home/u/dev/yog directly (it is not a repo edit); only scripts/bl-push-main and README go through the worktree.