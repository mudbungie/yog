+++
title = "inter-agent messages fail on display names: lernie message addresses ids, but names are yog prose — make the minted name a first-class addressable fact"
created = 1785645878
updated = 1785645878
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator hit it live 2026-08-02: told valid-overcrowd to message shudder-storeroom; the message tool failed twice with 'no agent "shudder-storeroom" in this workspace — check the id against the workspace's agents/* refs' (ops agents/<agent-id>/messages/005-tool.json, is_error). Facts: lernie message resolves agents/* refs (ids like <agent-id>); yog mints display names (§3.3 ladder) and stamps them only as prose ('You are shudder-storeroom.') at fire; no machine-readable name→id mapping exists anywhere. So agents cannot address peers by the only names the operator and every UI surface use.

DESIGN QUESTION (rule before coding): where does the name live as a fact?
(a) lernie first-class: prompt/dispatch grow a --name, stored under the agent ref; message resolves id OR unique name. Single source becomes lernie; yog's §3.3 ladder reads it back instead of re-deriving. Cost: lernie feature + release + yog pin bump (the bl-56bd ecosystem-rung dance).
(b) yog-side registry file in the workspace that lernie's message tool also reads — two crates reading one file, no release ordering, but the format becomes an implicit interface.
(c) mitigation only: tool description teaches the model ids + how to list agents/* (works today, ugly UX, operator still speaks names).
Attack each: (a) is the honest single-source fix; (b) smuggles an interface; (c) is a patch. Deliverable: ruling recorded here + in a lernie ball (lernie repo ~/dev/lernie, bl-9391 is the prior cross-repo precedent), then implementation plan as subtasks. Do not implement the lernie side without noting the release/pin sequencing.