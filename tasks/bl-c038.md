+++
title = "composer input lives in the conversation pane, not a full-width bottom strip"
created = 1785731170
updated = 1785731187
claimant = "pane-mover"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator request 2026-08-03, verbatim: 'I would like the conversation/chat line to be in the conversation box, rather than across the entire bottom.'

Today the composer/input line (likely src/shell/input_bar.rs — verify against the tree, this filing is from a quick grep) renders as a full-window bottom strip (egui TopBottomPanel idiom spans the whole window). The operator wants it scoped to the conversation pane: the input belongs to the conversation it feeds — it should sit at the bottom of the conversation box only, leaving other panes (nav, inspector, etc.) full-height.

Scope: move the composer's layout home from the window-level bottom panel into the conversation pane's own layout. Behavior (focus, keys, send, target line, preview) unchanged — this is layout placement, not composer semantics. Check DESIGN for any section describing the shell layout and amend it to record the placement. Verify every path and premise against the actual tree before editing; do not trust this body's file guesses.