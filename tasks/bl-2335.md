+++
title = "transcript: model turns are labeled with the model id — label them with the agent's name instead"
created = 1785645768
updated = 1785645934
claimant = "naming-fixer"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator report 2026-08-02, verbatim: 'the model response should just be its agent name, not the model'. Transcript rows currently render '· gpt-5.4: Yes' — the sender label for a model turn should be the conversation's agent name (the §3.3 display name, e.g. shudder-storeroom), not the model id. The model id stays discoverable (hover, or the steps/config views own it — it's a config fact, not a speaker). Keep the one-function display-name rule (§3.3, bl-2f30): derive the name the same way the composer target line does; never a second spelling.