+++
title = "retire the 'You are <name>.' goal stamp: the first user message is the user's — identity rides lernie's assembled context"
created = 1785649293
updated = 1785731679
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-08f2"
on = "claim"
+++
## Operator ruling (2026-08-01)

'The first message from the user is prepended with "You are <name>". This shouldn't be the way that it's communicated. It should just be in the context files.' — the goal payload the operator (or a ball body) wrote must reach the model unmutated; identity belongs in the harness-assembled context, which is lernie's job (lernie bl-d55f: with the name a stored fact per lernie bl-c8ed, lernie's §2.8 assembled context states it).

## What this ball is

Once yog's lernie pin carries bl-d55f (a release AFTER 0.0.4/bl-5134 — verify the actual version before starting), stop composing the stamp at fire:

1. `src/start/identity.rs`: `identity_preamble` leaves the fire path — the goal is the payload alone. The name still mints in yog and passes via `--name` (bl-08f2 point 1, unchanged); lernie states it to the model.
2. The composer's greyed `You are <name>.` preview (src/shell/bootstrap.rs, ram.rs) — re-decide its wording now that the line is no longer literally what fire prepends; the preview predicting the *name* stays (I7), the claim that this exact line leads the goal goes.
3. `parse_identity_stamp`/`strip_identity_stamp` stay as the LEGACY parse only (bl-08f2 point 3 already demotes them to a legacy rung for pre-0.0.4 roots; new roots simply never match). Delete with the legacy rung when 30-day retention ages the old roots out.
4. Occupied-set derivation must already read the lernie name fact by then (bl-08f2 point 4) — verify it no longer depends on new goals carrying stamps.

## Verify before editing

bl-08f2 will have landed first (needs edge) and may have moved these files — verify every path and premise against the tree, and read DESIGN §3.3 as amended by bl-08f2. Amend §3.3 again for this change: the stamp's compose leaves, the legacy parse's reason-to-exist is recorded.

## Note

bl-08f2's original point 2 ('the goal preamble keeps its first line') is OVERRULED by this operator ruling — its body has been amended to point here.