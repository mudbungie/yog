+++
title = "starting a new conversation should offer model selection"
created = 1785645900
updated = 1785646133
claimant = "birth-fixer"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator request 2026-08-02, verbatim: 'when starting a new conversation, I should be able to select the model.' The composer's new-conversation flow starts on the workspace default; the operator wants the pair chosen at start. Design questions to settle against lernie 0.0.3's actual surface before coding: (1) can a conversation be born on a specific config (per-conversation governing commit) WITHOUT advancing the workspace default? If yes, that's the clean semantic: start-time pick scopes to this conversation only. (2) If lernie only reads the workspace default at birth, then start-time pick = the same write the §9.4 picker does (advance default) immediately followed by the start — acceptable, but the UI must say it moves the workspace default too (one sentence, not a dialog). Either way REUSE the §9.4 picker component inline in the composer/start flow (collapsed by default — the default pair shown with a change… affordance, same idiom as the chat header's model line) — never a second picker implementation. Mind overlaps: bl-90b5 (activity indicator near composer) and the picker files (bl-fb6b landed 29f2335) are hot.