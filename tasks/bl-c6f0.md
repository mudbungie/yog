+++
title = "a routed tool whose input carries a url and no command line is opaque and held on every call: classify it open-world, the class whose meaning is network reach"
created = 1789002764
updated = 1789002785
claimant = "Cantaloups-Y16"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["usability-r3"]
+++
yog-android is adding an `http` tool (network fetch) to the phone foot; thrall's pinned mcp fetch (bl-b6ab) is the same shape. Under bl-72bd's routed lane a tool with no command line is opaque → held every call, so an operator would answer every fetch by hand or add a rules: row per box. Ruling: the routed lane reads a `url` (or `urls`) input on a tool with no command line as the open-world class — that is the class's definition (VISION §4.11: reach outside the workspace, allowed by default, revocable by the floor) — exactly as it reads a command line for shell-shaped tools; anything else stays opaque. A rules: row still overrides. Tests both directions; VISION §4.11 and DESIGN §8.6 amended; no PROTOCOL bump.