+++
title = "tool hosts I — advertisement and presence: a tool host's set lands in its registration, liveness is connection RAM, the workspace surface renders both (REMOTE §5)"
created = 1786757988
updated = 1786757999
claimant = "Halliard"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
Step 7 of the client/server split, first half (REMOTE §5, refined bl-bc7c; operator accepted dynamic tools 2026-08-13). A tool host presents its tool set when it connects; the engine writes it into the client's registration (world, file — beside the bl-8bbc registry shape) when it differs from what is stored. Presence — connected right now — is connection-scoped RAM in the wire server, never a file.

Deliverable:
- The advertisement's boundary spelling. The wire adds NO verbs (REMOTE §3), so presenting a tool set is a boundary gesture every face carries, all serializations; the client identity it lands under comes from the intake (the wire connection's certificate CN, exactly as scoping reads it) — an intake with no client identity refuses in-band. Advertisement element shape, recorded in REMOTE §5/§10 citing this ball: {name (single path component), description (one string), input_schema (JSON Schema, verbatim)} — three facts, nothing more; a name collision inside one client's set declines loudly; cross-client collisions are legal (disambiguation is the load act's job, second ball).
- Presence tracking: the wire server knows which registered clients hold a live connection now; a read the boundary already owes (the clients listing, second ball) can ask it; nothing durable.
- The workspace surface renders its registered clients — present or absent — and each one's advertised tools, live (§5's promised rendering; the seat sees the flap, the model's prefix does not).
- Strike REMOTE §10's advertisement-schema open question; record what was decided.

Out of scope (second ball, needs this one): the client-management tool, loading, routing, the lernie ToolInjection consumer, the pin bump, the client-side executor. Verify shapes against the tree; this body drifts. Boundary-surface work: serialize.