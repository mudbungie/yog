+++
title = "nav derivation: visible_rows over the descent forest — per-subtree ConvRow at any depth, direct/total counts, expansion-set flatten"
created = 1786511694
updated = 1786511694
parent = "bl-fa82"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-720a"
on = "claim"
+++
Subtask of the expander epic; the spec subtask rules this. Pure nav/ work, no
shell paint. Verify cited paths against HEAD first.

**Deliverable: src/nav/convs/expand.rs** (new file, per the spec's §12 rows) —
a flatten in the jsonview mold (src/jsonview/mod.rs:39-90: pre-order walk that
stops descending at a collapsed node; toggle fn) over
`git_tree::descent_order`, producing the list's visible rows given the
expanded set: every visible member becomes a row of the EXISTING ConvRow
shape, aggregated over *its own* subtree.

**The refactor that carries it:** row() (src/nav/convs/row.rs:129-180)
currently aggregates the subtree of a depth-0 root. Generalize it to build a
ConvRow for ANY member's subtree slice — the root-only build() becomes the
all-collapsed case (one call per depth-0 subtree), which is the invariant the
existing tests already pin. ConvRow gains what the paint needs: depth (indent),
`direct` (strict children_of count — §5.1 #8, NOT the stop_children prefix
test), and total descendants (= subtree members − 1; keep `members` as-is if
that is cheaper — one fact, one home, do not store both a members and a total
that derive from each other). Expansion state itself NEVER rides ConvRow — it
is ShellState ephemera; the flatten takes it as a parameter
(&HashSet<String>), exactly like jsonview::flatten's collapsed set.

**Sort:** depth-0 subtrees keep bl-cad5 recency order; within a subtree,
descent order (id-sorted siblings) as descent_order already yields.
Grouped-by-ball (src/nav/convs/group.rs) partitions the same visible rows.

**Boundary:** per the spec's decision — the conversations answer stays
root-rows; encode `direct` there only if the spec said yes
(src/boundary/reply/rows.rs + its optional-facts test).

**Tests (in-crate, the close gate holds 100%):** all-collapsed output ==
today's build() rows; expanding a root reveals exactly its direct children,
descent-ordered, each aggregating its own subtree (a collapsed child's badge
covers its hidden descendants); recursion two deep; direct vs total distinct
under a grandchild; strict-rule edges (id outside the two-token grammar ⇒
root; deleted intermediate ⇒ root — mirror src/git_tree/descent.rs:107-259
fixtures); toggle round-trip. Extend src/nav/convs/tests/rows.rs; keep the
existing one_row_per_root_with_subtree_member_counts green as the
all-collapsed pin.

**Caps:** convs.rs is at 248, row.rs 208 — new logic goes in expand.rs, and if
a touched file projects ≥250, split along the seam the spec named.