+++
title = "first start flow accuses itself of drift: 2 yog-drift rows (unenumerated/unannounced) on a pristine world"
created = 1785646876
updated = 1785646876
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Repro: fresh seeded world, one goal sent (the S0 rung, nothing else). ops.jsonl gains ['yog-drift','unenumerated'] and ['yog-drift','unannounced'] rows (exit -4, stderr = the workspace path) within seconds of the start, and the activity chip goes red ('N ops · 2 drift') — permanently, since drift rows persist in the trail across restarts. The workspace the sweep accuses is the one the start flow itself just created; the watcher presumably had no enumeration of it yet. A pristine, healthy first run must not paint the trail red — that is cry-wolf and buries real drift (bl-49f4's K count becomes noise). Fix: the sweep must treat a root it is seeing for the first time (created since the last enumeration, or by our own in-flight start) as enumerated-at-birth, not drifted. Acceptance: S0 drive world ends with 0 drift rows; a genuinely unannounced change still yields one.