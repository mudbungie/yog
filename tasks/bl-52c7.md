+++
title = "REGRESSION of bl-20f4: S7-T5 picks the descent-tree member by stale pixel coordinates — the tree moved down under two new attention lines"
created = 1786162695
updated = 1786515796
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
tags = ["drive"]
+++
Found by the 2026-08-07 re-baseline drive (bl-c63e), docs/drive-logs/2026-08-07-ladder-rebaseline.md. Reproduced identically on two runs of `scripts/drive/stories.sh run-s7` against release `aafa438`.

**This is a recurrence of the closed ball bl-20f4, "drive harness selects the conversation by stale pixel coordinates"** — same defect class, different beat. bl-20f4 fixed the conversation roster by moving it onto §11 keyboard bindings; this coordinate was left behind because §11 binds the roster, not a pick among a conversation s members.

Beat: `S7-T5 tree: selecting a member retargets (child watermark)`, `scripts/drive/beats_s7.sh:153`.

    pick_child() { "$drive" click "$wid" 420 107; }

    S7-T5 tree: selecting a member retargets (child watermark) FAIL — no child seen record

## Evidence

Screenshot `s7-02-tree.png` shows the descent tree has moved DOWN. At y=105 — where the click lands — the pane now paints a red attention line, not a member row:

    budget-exhausted — this agent tree hit its spend ceiling (refs/lernie/budget-exhausted)
    declined-transfer — a child s work product failed to apply and was declined (refs/lernie/conflicted)

The two member rows now sit at y=145 (`pound  budget-exhausted  declined-transfer  mail 1`) and y=166 (`pound  declined-transfer`), pushed down by those two lines plus the `model . gpt-5.4 . frozen at 90c8e00f  change...` row. The click lands on text; no child watermark is ever written to ui.json, so the beat correctly reports `no child seen record` — the gesture never happened.

## This is the failure the harness s own rule predicts

scripts/drive/stories.sh STEERING RULE, verbatim:

> A coordinate is not a spelling — it is a measurement of a layout, and a row inserted above a target silently retargets every click below it (the 2026-07-24 drift, docs/drive-logs/2026-07-24-s0-s1-wire-blocked.md).

That is exactly what happened: rows were inserted above the target.

## Why the coordinate was tolerated, and why that reasoning has expired

The beat tags itself `CLICK (a VIEW, and a pick — §11 rule 2)`: choosing WHICH member of the descent tree to select has no §11 binding, so no keyboard spelling was preferred. But the harness also says a coordinate survives only where §8.5 gives no boundary representation. Since VISION §4.8 ("the drive harness stops steering pixels: story beats address the headless surface"), the right repair is to check whether the descent member is addressable across the DESIGN §8.5 control boundary — a member has an agent id (`<root>-<CHILD_SEG>`), which is an address, and "a §11 rule-2 pick is a pick only at the pointer; at the line the thing picked has an address" (stories.sh, verbatim).

If no boundary spelling exists yet, that absence is the real finding and this ball should become the ask for one, rather than a re-measured coordinate that will drift again the next time the header grows a line.

---

**This ball's premise expired, and bl-8905 fixes the beat as a side effect.** Verbatim from the body: 'this coordinate was left behind because §11 binds the roster, not a pick among a conversation's members.' That stopped being true at bl-fa82 — ↑/↓ now walk the visible LIST rows, and a member is one of them once its parent is unfolded (→). So bl-20f4's own cure, moving the beat off pixels and onto §11 bindings, is available here for the first time.

bl-8905 retired the altitude-1 descent tree the coordinate was aimed at, so the beat had to move regardless. scripts/drive/beats_s7.sh's s7_tree is now s7_descent and drives keys only: → unfolds the already-selected root, the next ↓ steps into the first child. That is shell::focus::list_step → AppModel::focus_agent — the identical call the deleted tree's click made, so the assertion (the CHILD's watermark lands in ui.json) is unchanged and the stale coordinate is gone rather than re-measured.

**Not closed by me, and not verified on the wire.** I cannot run the drive beats (this environment cannot screenshot yog — Wayland/GNOME blocks capture), so what I am claiming is that the surface the beat aimed at no longer exists and the replacement is coordinate-free. Someone with a display should run scripts/drive/stories.sh run-s7 and close this on the evidence, not on this note.
