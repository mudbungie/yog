+++
title = "attempt science projection: one derived headless query joining frozen inputs, refs, usage, and outcome"
created = 1785823813
updated = 1785823813
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
tags = ["implementation"]

[[blockers]]
id = "bl-8746"
on = "claim"
+++
Source: bl-2b8c ruling (VISION §4.10 item 7). Verify premises against the tree; VISION/DESIGN win over this body.

One typed query through the §4.8 control boundary (headless-spellable, same data both frontends) projecting, per attempt: frozen inputs (goal, pinned documents, governing config commit — model/skills ride it), base/source/target OIDs and the delivered commit when one exists (balls delivery identities), terminal response, usage and wall time (step records), project diff (target..source, a pure git read), judge verdicts (messages), and the accepted/rejected/reworked outcome — all derived from existing authorities at read time.

- Accepted = the target's history records this attempt's delivery; rejected = a cohort sibling was accepted (or the attempt was discarded) while this target advance never happened; reworked = the source advanced after a refusal or verdict. All queries, no fields.
- Store nothing; cache nothing. This is §4.5's join discipline applied to attempts.
- Composes with V3 (bl-c2bd) rendering; neither owns the other's surface. Decide placement per DESIGN §11/§8.5 and amend DESIGN where it lands.