+++
title = "bump lernie pin to the release carrying the fork-dialog prune (bl-5a36)"
created = 1785732068
updated = 1785732068
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Cross-store ordering as prose (edges cannot span stores): lernie bl-5a36 (closed, lernie main feeb127) fixed the runaway-fork composition bug found in yog bl-d023 — a child's dispatch commit now prunes the fork point's inherited messages/**, summary/**, skills/** so a child never sees the parent's tail instruction as its own unanswered marching orders (the energize infinite-depth incident, 2026-08-03).

DO NOT CLAIM until a lernie release carrying bl-5a36 is published on crates.io (expected 0.0.5; verify the actual published version and confirm it contains src/prompt/dispatch/step_commit/inherited.rs by reading the published crate, not by version arithmetic — the 0.0.4 cycle proved release-content assumptions go stale). Then: bump the exact pin in Cargo.toml (currently =0.0.4), refresh Cargo.lock, verify deny.toml covers the lock, confirm brazen parity (exactly one brazen in the lock), full gate. Registry-only sourcing per AGENTS.md; brazen untouched. Mirror of bl-5134's shape.