+++
title = "bump lernie pin =0.0.3 -> =0.0.4 (agent naming, lernie bl-c8ed)"
created = 1785645965
updated = 1785648127
claimant = "Larch"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Step 3 of the bl-50f3 release ordering: lernie feature (lernie store bl-c8ed) -> lernie 0.0.4 on crates.io (lernie store bl-4c15) -> THIS pin bump -> yog reads the name back (next ball). Cross-store dependency recorded as prose because edges cannot span stores: DO NOT claim until lernie 0.0.4 is live on crates.io (`cargo search lernie` or the lernie store shows bl-4c15 closed). Bump `lernie = "=0.0.3"` to `=0.0.4` in Cargo.toml, refresh Cargo.lock, verify `deny.toml` license allow-list still covers the lock, full gate green. brazen stays =0.0.5 — naming touches no provider layer. Registry-only sourcing per AGENTS.md rule 6: never a git/path dep for this.

BLOCKED (2026-08-01, checked by Larch): lernie 0.0.4 is NOT yet on crates.io. Verified two ways:
- `curl -A "yog-bl5134-check" https://crates.io/api/v1/crates/lernie` lists versions ['0.0.3','0.0.2','0.0.1','0.0.0'] only.
- `cargo add lernie@=0.0.4 --dry-run` fails: "the crate `lernie@=0.0.4` could not be found in registry index."

Per this ball's own instructions, not proceeding — no Cargo.toml/Cargo.lock change made. Re-claim once lernie store bl-4c15 closes and 0.0.4 is confirmed live.