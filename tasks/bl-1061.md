+++
title = "three S6-T1 beats have not run since 2026-08-11 and their first real-substrate execution is unproven"
created = 1786683387
updated = 1786685433
claimant = "Pintle"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["drive"]
+++
bl-2d45 (7a91ed69, 2026-08-11) named its moved stop-and-ack stage `s6_attention` — a name `beats_s6.sh` already used for world C's S6-T1 stage. Bash sources every `beats_*.sh` into one flat namespace and the later definition wins, so from that delivery until bl-0e44 landed (844ffd45, 2026-08-13) `run_s7`'s `s6_attention "$wid" "$out" "$agent"` call (beats_s7.sh:123) executed the STOP stage, and these three beats did not exist:

    S6-T1 budget: ack watermark after the sort's head
    S6-T1 conflicted: ack watermark on the same landing
    S6-T1 mail: no watermark exists to silence it

A beat that never runs writes no verdict row, so no ladder output could show it.

bl-0e44 renamed the newcomer to `s6_stop_ack` and added `one_name_one_definition` (harness.sh) to refuse the collision structurally. **The three beats are therefore restored but UNEXERCISED**: the last time they ran was against a yog two days and many deliveries old. Their fixtures (`lay_forensics`, the budget/conflicted marks, the §6 sort head) and their assertions (`seen_kind` over ui.json) have had no substrate contact since.

## The work

One real-substrate `run-s7` drive on current main. Read the three rows in `verdicts.jsonl`. If any is red, that red is about the two days of drift, not about bl-0e44 or bl-2d45 — diagnose it as a fresh finding.

Filed by Ingot while closing bl-a5b4, whose entire symptom was this collision.