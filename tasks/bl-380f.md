+++
title = "VISION promises model and skill selection from ball tags, but fleet birth discards tags"
created = 1787206352
updated = 1787206352
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["missing", "design", "model", "agentic"]
+++
`docs/VISION.md` promises:

> “Skills are seeded at spawn, keyed on ball tags.”

and says yog selects skills and model from the ball's tags at fire.

The same document's gap ledger still records:

> “G6 | Model by complexity heuristic, not tag map | yog has a per-role picker; no policy layer, no dataset.”

Fleet birth reads the complete board row, then `BallSpec` carries only id, title, body and join. Tags do not reach the start plan. Closed `bl-3381` explicitly excluded the policy table and called for separate work; no later live task owns it.

## Required design result

Either land the promised tag-derived model/skill policy or amend the product promise. Keep ball tags authoritative: derive at fire, do not mirror them into yog state. Specify conflict/default behavior and make removing a default delete policy rather than edit core code. Drive two tagged balls to observably different frozen birth configurations.