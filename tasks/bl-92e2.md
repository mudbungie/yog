+++
title = "close gate runs coverage but not fmt-check — fmt drift lands on main and breaks make check for everyone after"
created = 1785647364
updated = 1785647487
claimant = "Elmet"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Found during bl-a69a: the pre-commit hook that gates bl close runs line-cap, rules-audit and tarpaulin, but NOT cargo fmt --check — while make check (the documented complete local gate, AGENTS.md 'mirrors CI exactly') runs fmt-check first. So an agent can close a ball whose formatting drifts, main accumulates drift, and the next make check fails on someone else's hunk (observed: four files' whitespace drift inherited from main during bl-a69a, fixed incidentally there). One gate, one definition: the hook must run the same fmt-check make check runs (cheap, fails fast), or AGENTS.md must stop claiming the mirrors match. Check .git/hooks/pre-commit or wherever the hook script lives (scripts/?), and CI's own steps, before editing.