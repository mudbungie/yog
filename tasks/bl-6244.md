+++
title = "REGRESSION of bl-8c2d: run-unseeded still expects CONFIG where the current default correctly returns AUTH"
created = 1787206334
updated = 1787275977
claimant = "Zircons-Drive"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["regression", "drive", "provider"]
+++
## Reproduction

Run `run-unseeded` with the current default-style provider template and an unfinished isolated wall. The first phase expects a CONFIG refusal, but the shipped template now contains an offerable OAuth row, so the product correctly returns AUTH. The run ends 9/10 and the old configuration-remedy branch is no longer exercised.

Closed `bl-8c2d` changed the default install behavior after closed `bl-9e10` authored this beat. There is no follow-up adapting the fixture.

## Required invariant

Acceptance fixtures must construct the exact missing prerequisite they claim to test. Make the CONFIG phase use a genuinely absent provider/config row, or retire and replace the obsolete phase while preserving coverage of the configuration remedy. The current correct product state must not leave the standing ladder red.