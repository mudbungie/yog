+++
title = "remove ball Move end to end: both carriers, the codec arm, reassign, and the submenu machinery it alone justifies"
created = 1787379479
updated = 1787379487
claimant = "Thornbury"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
Operator ruling 2026-08-21: remove ball Move ("move to:") — it breaks no rule,
it just won't get use. Scope: the WHOLE capability, both carriers (pointer
affordance AND the §8.5 `/move` line), since the judgment is about the verb's
usefulness, not the pixels. Everything below verified 2026-08-21.

## What dies (all of it Move-only; nothing else consumes these)

UI carriers:
- Composer ball row: `src/shell/ball_bar.rs:82-90` ("move to:" label +
  destination buttons), `MOVE_HINT` (:34-37), `move_ball` (:136-146), the
  `targets` param of `actions` (:49-54).
- Context submenu: `src/nav/menu.rs:198-208` (the "move to" Entry),
  `MOVE_CARRIER` (:29-31), `Verb::MoveTo` (:56-58), `Seat::BallRow::move_to`
  (:104-110); dispatch arm `src/shell/menus.rs:132-134`.
- `Action::Submenu` (`src/nav/menu.rs:66-73`) and its renderer
  (`src/shell/menus.rs:91-97`): Move is its ONLY producer — delete it; the
  roster is declared closed and §11 justifies Submenu solely by Move
  (DESIGN:8748).
- Destination rule `nav::tabs::move_targets` (`src/nav/tabs.rs:32-49`, no
  other consumer) and its four call sites (`input_bar.rs:225`,
  `navigator.rs:171` + per-row filter :206-212, `board/rows.rs:127-130`,
  `start_rows.rs:131`; ready row's empty seat `start_rows.rs:78-91`).
- `actions::move_enabled` (`src/actions/mod.rs:242-248`, two callers, both
  Move).

Line/wire/executor:
- §8.5 line: `src/boundary/line/balls.rs:22-36` `moved()`, parse arm
  `parse.rs:96`, speller `spell.rs:46`, help row `help/table.rs:112-120`.
- `Action::Move` (`src/boundary/action.rs:92-98`), codec encode/decode
  (`codec.rs:77-82`, `codec/balls.rs:25-26`,:127-132, the "move" op-name in
  `codec.rs:179` — the bl family's six becomes five), addressing arms
  (`address.rs:90`,:108, `address/agent.rs:70`), dispatch
  (`dispatch.rs:100-102`), executor `verbs::reassign`
  (`src/actions/verbs/balls.rs:100-119` + re-export `verbs.rs:63`).
  `unclaim`/`assign` stay — Release and Assign use them independently.

Hovers that spell `/move [id] <to>` must be rewritten in the same change or
`every_control_hover_names_its_keyboard_spelling` fails: `ball_bar.rs:37`,
`navigator.rs:196-201`, `board/rows.rs:120-123` (deleting the help row
retires /move from the accepted vocabulary automatically).

## Tests to delete/update (map, verified)

- Menu: `src/nav/menu/tests/mod.rs:179-207`, :209-221, :157-176 (move_to
  fields), :226-243, the verbs() submenu-flatten helper :22-35; doctrine
  sweep `tests/doctrine.rs:28-38`, :47-53 (submenu recursion dies with
  Submenu).
- `src/app/balls/tests/internal.rs:170-194` — split: keep the is_named
  assertions (:190-193), drop the move_targets half.
- `src/actions/tests.rs:176-182` (move_enabled).
- Line/codec: `src/boundary/line/tests.rs:146-155`,
  `tests/refusals.rs:121-122`, `tests/parity/balls.rs:27-32`,
  `src/boundary/codec/tests/mod.rs:159-164`, `src/boundary/tests.rs:104-127`
  (drop the Move variant).
- `tests/integration/boundary_verbs/balls.rs` — bl_actions arity 6→5, drop
  the Move variant and its argv expectations.
- `src/actions/verbs/tests/mod.rs:165-190` (reassign), :212-221/:245 (origin
  sweep op count drops by reassign's two).
- `tests/integration/stories_s4_t2.rs` — the story is assign-MOVE-release:
  re-scope to assign-release (keep the surviving verbs' coverage), and amend
  the STORIES.md:1163 contract + :1104 roster line to match.

## DESIGN.md amendments (same change, citing this ball)

- §11 keyboard rule 2 (~7926-7930): Move is the load-bearing example of the
  pointer-pick carve-out — replace the example (the overflow menu remains a
  pick). Keys table row ~7963 ("Move stays a click"). Composer accessories
  ~7813 (Close/Release/Move → Close/Release). Discoverability rule 4 ~8645
  (the "move to:" const is the canonical example — pick another surviving
  pair or trim). Rule 3 ~8634 example. Hover acceptance ~8666 ("a Move
  destination" example). Roster row ~8727 and the Submenu justification
  ~8748. Enablement list ~8731-8733.
- §8.2 argv table row ~3369; claimant rider ~3388-3390; §4.8 roster ~3779;
  §5.3 variant list ~6130; §3.2 late-mutability ~297; §3.6 "Move is two of
  those" ~1160.
- §3.1 migration ruling ~265-270: currently routes workspace renaming through
  Move; the same sentence already offers the hand-`mv` through the §8.4
  hatches — make that the stated path.
- §12 rows: ~8819 (targets.rs/move picker wording), ~8924 (nav row), ~9006
  (chrome's "a move-target submenu"), and the homes of deleted code (~9030,
  ~9040). Also `src/shell/keys.rs:12` and `src/shell/chrome.rs:7,:37` doc
  mentions.
- docs/REMOTE.md mentions (~1614/1866/1902/1910/2120) are historical
  narrative — leave history as history unless design_citations complains.

## Direction

Pure subtraction, no replacement mechanism, no deprecation shim. The one
judgment already made: Submenu goes (Move was its only producer). Coverage
floor discipline: deleting code deletes its tests in the same commit.