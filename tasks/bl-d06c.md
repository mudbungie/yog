+++
title = "multi_tool is classified opaque and held on every call, so a question that fans out reads (clients get ×2) parks with no answer: classify the envelope by its invocations, strictest wins"
created = 1788934188
updated = 1788934405
claimant = "Cantaloups-Y14"
priority = 1
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["usability-r3"]
+++
Operator screenshot 2026-09-08 (campaign rounds/3/shots/phone-complaints/held-multi-tool-0.0.4.png): conversation BanisterIsthmus asked 'what devices do you have access to?'; the agent called `clients list` (ok) then `multi_tool` {execution: parallel, invocations: [clients get NickelBuzz, clients get cedarmoon-foot, …]} and the control HELD it: 'classified opaque (multi_tool is not a tool this control implements and its input carries no command line …)'. The user saw a question with tool calls and no reply. Ruling: multi_tool is an envelope, never a subject — the classifier judges each invocation exactly as if it were called alone (by name against the closed intrinsic enum, or by command line for shell-shaped ones) and the envelope takes the STRICTEST verdict; an envelope containing an unknown name is opaque as before. Here every invocation is `clients get` (a read) so the envelope passes. Amend VISION §4.11 / DESIGN §8.6; test: an envelope of reads passes, an envelope with one destructive shell holds, an envelope with an unknown tool is opaque. Also decide whether `multi_tool` stays in the worker grant now that litany's python built-in exists (litany bl-99bb planned its retirement) — if retiring, this classification still applies to any envelope shape.

---

Decision on multi_tool's place in the worker grant: nothing to file — it is already retired, upstream, and the retirement is in the pin. litany bl-99bb shipped it (litany docs/DESIGN_CODE_EXECUTION.md section 5: "A program is a multi-tool envelope whose list is written at run time"); the name is gone from template/providers.yaml's worker grant, ships no schema and no skill, and a fresh invocation is declined in band by the grant gate as the ungranted name it now is. yog pins litany =0.0.13, so no yog-side or litany-side grant change is warranted and none is filed.

Premise check on today's tree, driven on a scratch engine: the reported park is unreachable at the pin. The engine door answers a multi_tool block with the grant gate's own sentence ("is not callable by a worker ... declaring is not permitting") BEFORE the tool control is consulted, so the opaque hold cannot be reached for it. The eleven names the grant does carry all adjudicate; none is opaque.

The ruling ("an envelope is never a subject") stands as doctrine and is recorded in VISION 4.11 item 1 and DESIGN 8.6 — but it wants no fold in yog, for two reasons that are one. The engine already does it and better: a program's inner invocations re-enter the grant gate AND the control at the moment each runs (litany's "no bypass"), which binds the judgment to what executes rather than to a list written ahead of it. And a fold hung on a name the closed intrinsic map does NOT hold would lower a class on the invocation's own say-so — the inference bl-72bd deleted, and the one path left by which an unread invocation could talk itself into a passing class. A sound fold must hang on a name in the map; there is none, and python (the successor envelope) carries its own row.

What actually parked the conversation was neither half: a name in the shipped worker grant with no row in the intrinsic map falls to the routed lane and holds on every call. That class is now guarded rather than fixed — tests/engine_grant.rs mints a workspace with the real binary, reads the worker grant out of the config commit the engine authored, and holds every name in it (plus tool_host::engine_act::NAMES and tool_host::clients::NAME) against classify::Leg::of. It reddens on the grant as it stood, naming the row to write. Also corrected: the hand copy of that grant in test_support stood one pin behind the engine.
