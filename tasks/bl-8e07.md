+++
title = "develop user stories S2+ and run real-substrate workflow evaluations"
created = 1784955678
updated = 1784955887
claimant = "Exultantly-8e07"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator ask (2026-07-24): the STORIES.md ladder stops at S1 automation. Develop the next user stories (S2+) and RUN the workflow evaluations against the live wire. STORIES.md is the living deliverable; DESIGN.md stays the architecture authority.

## Part 1 — stories (docs/STORIES.md, delivered)

Deepened in place:
- S3 step 5 + S3-T7 — shipping: `bl close --as <bound workspace name>` (§8.2
  identity rider), the ball then re-deriving as delivered from the closed
  listing.
- S4 retitled "spheres, corrals, and the board" + steps 4-7 and S4-T4..T7 —
  the conversation board: per-conversation ball badges coloured by the §3.5
  join state, the two honest gaps (no stamp = no badge; unknown stamp = the id
  uncoloured; agent-picked balls bind at the workspace altitude only), the
  recent|by-ball toggle as a pure stable partition, the workspace tab strip.
- S5 retitled "the tools and their configuration" + S5-T1..T6 — toolchain
  verdicts with remediation, the gate refusing every mutating dispatch while
  reads continue, the three §9 editors under one load/stage/validate/hash-
  guard/rename discipline.

New rungs:
- S6 Triager — attention as derived predicate, acknowledging is focusing and
  converges, ack clears the signal not the fact, the activity accessory.
  S6-T1..T5.
- S7 Forensic — the five inspector tabs, step drill-in through jsonview, Raw
  toggles, budget fold, inbox/tool-in-progress. S7-T1..T5.
- S8 Neighbour — the nested world, the shared store as a chosen overlap,
  `yog env`/`yog exec`, the no-marks knob driving `bl conf`, I0. S8-T1..T4.
- S9 Settler — phase 2 (§16.7): install ONLY yog. The one rung that escalates
  DOWNWARD: its acceptance is that S0 entry bar disappears, W5 deleted,
  `lernie-tool-bl` stamping `--as $YOG_NAME` so §3.3 phase-1 preamble goes.
  S9-T1..T4.

Test->enabling-task map extended: Z10..Z14 (to file) carry the S3-S8 tests
over already-built surfaces; W8-W11 carry S9. Priority line now P0=S0+S1,
P1=S2+S3, P2=S4+S6, P3=S5+S7+S8, P4=S9.

## Part 2 — real-substrate evaluation (docs/drive-logs/2026-07-24-s0-s1-wire-blocked.md)

Ran the worktree release build under Xvfb :99 on a scratch world. 6/8 beats
PASS. Two FAILs, neither a yog regression:

1. S0 payoff + (indirectly) the wire half of S1: installed bz 0.0.4 vs the
   installed lernie linked brazen 0.0.3 — `lernie prompt` refuses before the
   model call. Reproduced outside yog. Filed bl-f69b (re-pin + re-run; and
   DESIGN W5 is blind to this skew class).
2. S1 message-to-agent: stories.sh clicks (74,56) to select a conversation,
   which is now the `recent | by ball` toggle (bl-de16 shifted rows down), so
   the composer stayed on "new conversation" and Enter fired a prompt.
   Confirmed by hand that (140,77) selects and the composer relabels to
   Message. Filed bl-20f4 (drive selection via the §11 keyboard bindings).

Positive finding: with the wire dead, yog behaved as designed — Stopped
classification, §6 rule 2 stirring the strip (3 need attention), per-row ⚑1
badges, ack-on-focus dropping the strip 3->2, and no false ⚠ on the -2
detached sentinel. Gap: no cause is rendered anywhere (Steps shows
`001 · 0 attempts · 0 tok`); the zero-byte response.json is evidence yog does
not yet read. Filed bl-7f2e.

Filed: bl-20f4, bl-7f2e, bl-f69b (all p2).