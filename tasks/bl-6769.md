+++
title = "payload-preview rung reads block-array content — lernie children's first messages are block-shaped and currently fall through the ladder"
created = 1785737232
updated = 1785737238
claimant = "rung-reader"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Found during bl-63a1 (db10e9d): the display ladder's payload rung (extract_request_preview, src/start or src/nav territory — verify) reads the first user message's content with .as_str(), so a block-array content (the shape lernie children's step-001 request.json actually carries) yields nothing and the ladder falls to the id floor. The rung should read the first text block of a block array as the preview line — same honesty (verbatim bytes, first line, clipped), one more accepted shape. Named agents unaffected; this only lifts nameless pre-mint agents (existing worlds) off the id floor when their payload genuinely has text. Test both shapes (string content, block-array content) plus the no-text-block case (floors as today). Verify paths against the tree — bl-63a1 just touched the floor seats.