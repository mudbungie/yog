+++
title = "pass --name at fire; display ladder rung one reads the lernie name fact; prose stamp demoted to model-facing prose plus legacy parse"
created = 1785645966
updated = 1785645966
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-5134"
on = "claim"
+++
Final step of the bl-50f3 ruling (see bl-50f3 body for the full ruling and DESIGN 3.3 for the amended contract). With lernie >=0.0.4 pinned:

1. At fire, the start flow passes the minted name to lernie (`--name <name>` on the prompt spawn, src/start). The composer preview/mint mechanics are unchanged (mint stays yog policy: src/names, RNG + per-workspace occupied set, previewed before spawn, I7 intact). On the lost-race re-mint, the re-derived name is what is passed — preview is a prediction, the stamp is the truth, unchanged.
2. The goal preamble keeps its first line `You are <name>.` — it is model-facing prose (agent self-identity), frozen dispatch content composed from the same string at the same instant as --name, no longer a fact home and never parsed as the primary rung.
3. Display ladder (src/nav/convs.rs, src/git_tree/enumerate.rs, src/git_tree/model.rs): rung one becomes the lernie-stored name fact read off the agent (per lernie bl-c8ed storage home — expected: `agents/<id>/name` in the worktree / `git show agents/<id>:name`); the existing `You are <x>.` goal parse (src/start/identity.rs) demotes to a legacy rung between it and the first-payload-line rung, covering pre-0.0.4 roots until 30-day retention ages them out — then delete the legacy rung.
4. Occupied-set derivation for the mint moves to the same fact (names of the workspace live roots read from lernie storage, falling back to the legacy goal parse while legacy roots live).
5. Descent children may now carry names lernie mints or accepts — the 3.3 honest-scope limit is lifted; if the enumerate path surfaces child names, the ladder shows them by the same rung with no yog special case.

DO NOT start before the pin bump ball closes (enforced by the needs edge).