+++
title = "W13 delete phase-1 scaffolding: toolgate, probes, gate-checks"
created = 1784784300
updated = 1785129694
claimant = "waxier-07b9"
parent = "bl-b5d1"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
DESIGN §16.7 W13. Needs W8+W10+W11. Delete src/world/toolgate/, pane wiring, dispatch gate-checks unwind, DESIGN §5/§16 phase-1 language retires.

STATUS (waxier-07b9): DONE, delivering.
- Deleted src/world/toolgate{.rs,/probe.rs,/tests.rs} (565 LOC) and tests/stories_s0_t4.rs (75 LOC). Net -1138 LOC over 52 files.
- Unwound the dispatch gate: actions::verbs gate_check + every verb's `gate: &ToolchainState` param; start::run require_tools + StartError::GateRefused; start::exec Deps.gate; AppModel's toolchain/probe fields and AppModel::new's probe arg; test_support::OkProbe and tests/support::{OkProbe,StaleLernie,ok_gate}.
- src/shell/toolchain.rs -> src/shell/login_pane.rs: the pane's phase-1 half (verdict rows, Re-probe, disclaimer) deleted; the §8.3 Login surface it also hosted survives unchanged.
- Also unbundled actions::verbs' `stamp: (&Path,&str)` (it existed only to fit the gate arg under the arg cap) so all nine verbs take (bl, state_root, ts, ...).
- DESIGN: §16.4 rewritten (coherence structural, not gated); §16.6 heading + W5 turned into a retirement record preserving the two blind-spot incidents and dropping the lernie --version upstream ask (nothing in yog reads it); §16.7 W13 marked landed with a 3-point as-built; §15 Z6 retired, Z8 dep dropped; §5.2/§8.2/§8.3/§11/§12 pane + shim facts corrected. STORIES: premise, test map, S0 item 6 + S0-T4, S5 items 1-2 + S5-T1/T2, S9 items, INV-1 retired.
- cargo test green (28 binaries), fmt/clippy -D warnings clean, rules-audit clean.