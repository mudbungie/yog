+++
title = "REGRESSION of bl-c0e2: drive preflight is green while scratch workspace walls have no config or credentials"
created = 1786510248
updated = 1786511428
claimant = "Gasket"
priority = 1
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
tags = ["regression", "drive"]
+++
The workspace-wall migration moved brazen config, credentials, and model cache to `<yog-data>/world/walls/<workspace>/brazen/*` (DESIGN §16.2). The real-substrate harness still uses the retired ambient layout:

- `scripts/drive/preflight.sh` checks `$HOME/.local/share/brazen/credentials` and reports green.
- `scripts/drive/yogdrive.sh` links credentials at `<scratch>/brazen/credentials`.
- `scripts/drive/cleanroom.sh` documents and seeds the same ambient path.
- the run fixtures still contain ambient `BRAZEN_CONFIG` premises.

No production fold reads those scratch paths after bl-c0e2. A wire run can therefore pass preflight and launch with an empty workspace wall, then fail authentication. The no-wire `run-s5s8` on `96d5f4e` also reproduced the exact 10-red table in bl-00ee because workspace birth never established the required wall; bl-00ee still diagnoses the retired ambient override.

Seed and verify the wall actually used by each fixture workspace. Update bl-00ee and the clean-room assertions to prove the credential/config source reaches `<world>/walls/<name>/brazen`, while the operator world remains untouched. The preflight must not claim wire readiness for a path no driven process reads.

---

Premise check against the tree before building (three of four hold, one is already fixed, and the body's stated *consequence* is understated):

CONFIRMED — `preflight.sh:64` checked `$HOME/.local/share/brazen/credentials` and printed OK. `yogdrive.sh launch` mkdir'd `<scratch>/brazen` and symlinked that same host dir into it. `cleanroom.sh` symlinked `${XDG_CONFIG_HOME}/brazen` into the room and its header documented both as deliberately shared. None of the three is read by anything yog spawns since bl-c0e2: `bz_host::run` folds `BrazenPaths::of(env)` off `YOG_WALL` and *injects* the wall's path as `BRAZEN_CONFIG`, so brazen never reaches its own `$XDG_CONFIG_HOME` fall-through, and `bz_host::store` roots the credential store and model cache at the wall rather than the process env.

ALREADY FIXED, not a premise to act on — 'the run fixtures still contain ambient BRAZEN_CONFIG premises' is stale. `beats_s5.sh` was reseated onto the wall (`wall_config()`, and a header saying an exported BRAZEN_CONFIG is now inert) and `beats_s8.sh` asserts the *absence* of BRAZEN_CONFIG/YOG_WALL in a hatch's env. No script exports one.

UNDERSTATED — the body says a wire run 'can pass preflight and launch with an empty workspace wall, then fail authentication'. It is worse and earlier than authentication. Verified by asking the binary itself: `YOG_WALL=$(mktemp -d) yog bz --list-providers --json` answers brazen's seven SHIPPED rows (anthropic, openai, mistral, openai-responses, google, ollama, claude-code) and no more, because a newborn wall is an empty directory. The world seed copies the operator's `template/providers.yaml`, which names `openai-chatgpt` — a row that exists only in the operator's *ambient* `~/.config/brazen/config.toml`, which no scratch wall inherits. `start::ensure::create_workspace` runs `template::gate` BEFORE `lernie new`, against `Deps::provider_rows` lensed onto the TARGET workspace's wall (`boundary/dispatch.rs:204`). So the gate refuses, no workspace is ever created, and every beat of every run verb goes red. 'Empty gates nothing' does not save it: the table is not empty, it is brazen's defaults.

WHAT PREFLIGHT NOW NAMES (step 0, QUALITY §3): a required row per provider the seeded birth template declares, judged against the table a fresh wall actually answers — asked of the `yog` under drive through a throwaway empty `YOG_WALL`, never a copy of brazen's default list kept in the script to drift. Plus a wire-advisory tier naming the two host fixture SOURCES (`~/.config/brazen/config.toml`, `~/.local/share/brazen/credentials/<provider>.json`) and the wall destination they are copied into.

WHAT IS SEEDED — `harness.sh` gains `wall_dir`/`seed_wall`: one spelling of the §16.2 layout, and the per-workspace brazen fixture copied into `<world>/walls/<leaf>/brazen/{config.toml,credentials/}` after each mint. Verified end to end: a wall seeded that way answers `openai-chatgpt` with `"credential":"stored"`. It runs after the mint because the wall is keyed by the §3.1 leaf yog mints at the fire — which is exactly why the birth gate is preflight's subject and not the seeder's: a gate that fires before a workspace exists can be reported, never seeded. World A (run-s5s8) keeps its own minimal wall config: the host's real config is ~90 lines and would push §9.1's Apply row off every coordinate in that run.
