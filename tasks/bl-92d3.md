+++
title = "the two boundary rosters are at the 300 wall: Action 295 and Reply 297, and an enum has no seam"
created = 1788148295
updated = 1788322041
claimant = "Bridger"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
bl-c088 looked at src/boundary/{action,query,reply}.rs and left them, correctly: an enum cannot be split across files, so those rosters have no seam of the kind the cap rule means. bl-f4e3 then added one variant to two of them and the census now reads action.rs 295, reply.rs 297 against a 300 wall. That inverts the rule AGENTS.md states — a file resting on the wall fires on whoever touches it next, at the moment they are finishing something else, when the cheapest way out is exactly the shave the rule forbids.

The escape hatch is already named, in DESIGN §12's own row for that file: 'a family whose members every layer beneath already reads as a pair folds to ONE variant over that family's own Verb'. Four families have taken it (monitor, fleet, the routing leg, the §3.8 fan). The one left untaken is the bl family — Close, Assign, Release, Create, Update — and every layer beneath already reads it as a family: codec/balls.rs, line/balls.rs, reply/balls.rs, answer/balls.rs. Folding those five to Action::Ball(balls::Verb) frees roughly 25 lines in the roster and removes nothing.

Not done under bl-f4e3 on purpose: it is a shared-surface refactor touching the codec, the line reader and writer, both address tables and the dispatch match, and shared-surface work serializes. File it, do it alone, and do it BEFORE the next boundary act rather than during one.

Reply's own hatch is thinner — its receipts do not group the same way — so measure before assuming the same move works there. Check the census first either way: make line-cap LINE_CAP=249.