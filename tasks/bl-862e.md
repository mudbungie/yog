+++
title = "src/app/balls/tests/glue/ceiling.rs is orphaned: no mod declaration reaches it, so its two spend-ceiling beats never compile or run"
created = 1790127249
updated = 1790127551
claimant = "Urinalyses-L"
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
Found while adding src/app/balls/tests/glue/workflow.rs (bl-b680). The file sits beside glue.rs, uses super::{fake_litany, model_focused, prepared, world} and asserts the spend ceiling refuses the Prompt door with a trail row — but glue.rs declares no 'mod ceiling;' and nothing else does, so the file is dead text under the 100% floor. Either re-declare it (and fix whatever no longer compiles — model_focused may not exist) or delete it if the beats moved elsewhere; check src/control/tests/ceiling.rs and the boundary ceiling module first.