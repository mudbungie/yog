+++
title = "BLOCKED on lernie bl-22a5 — the retarget gesture: move a live conversation onto the config the picker just wrote"
created = 1786162426
updated = 1786162426
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-a0d4"
on = "claim"
+++
Consumes lernie's `retarget` verb (lernie bl-22a5) once it ships and is published.

lernie bl-22a5 builds the exit from the config freeze: `lernie retarget <workspace> <agent> [--config <name>]` writes `refs/lernie/retarget/<agent-id>`, and the agent's own executor lands it by rebase-forward at the next step boundary, after which `governing_config` answers the new commit.

yog's half is the gesture, and its seat is decided by bl-a0d4: that ball makes a conversation state the config it is frozen at, and states it loudest exactly when the governing commit is no longer any config branch's tip. That sentence is where the way out belongs — the operator reads 'this conversation is frozen at <oid>' and the retarget is the verb beside it, not a fourth control somewhere else.

Blocked twice over: on lernie bl-22a5 landing, and on a lernie release + pin bump (yog pins =0.0.5 today).

Depends on: bl-a0d4 (the sentence this gesture attaches to).