+++
title = "transcript boundary rules: one faint rule per crossing, commit id = ensuing step's meta.json commit"
created = 1785733455
updated = 1785733502
claimant = "line-drawer"
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-929d"
on = "claim"
+++
Implements the bl-929d persistent commit-boundary contract — VERIFY docs/DESIGN.md §11 ('Every crossing leaves its line') and §5.1 #29 on main before editing; where this body and DESIGN disagree, DESIGN wins. The transcript renders one faint ash rule, full row width, short commit oid right-aligned, above each crossing: each maximal run of delivered .md entries (structure the bl-1f21 turn projection already computes — no new row fact, no key, no stored state). The id is the ensuing step's meta.json commit — the read-state tip the model call was assembled against, bl-98da's exact spine read, reused not re-derived; the pairing of steps to transcript position rides the same ordinal alignment (both sequences serialized under lernie's executor lock). Two drains with no step between = ONE crossing under one line (both batches entered the same prompt). Absence of a commit = no line: a crossing whose ensuing step has landed no meta.json draws nothing — the in-flight strip owns that interval, and the line materializes on the ordinary §7.2 re-derivation. Hover explains the seat (bl-68ac): what the id is (the commit the next turn's model call read), never how derived. Derived per snapshot; frame does no git or file IO.