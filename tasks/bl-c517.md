+++
title = "S5/S7/S8 drive coverage: world fixtures (primed project, config branch) + drive beats"
created = 1785124356
updated = 1785131439
claimant = "waxier-c517"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-84f3"
on = "claim"
+++
Second half of the bl-f69b S2+ inventory, EXTENDED with bl-84f3's residuals (docs/drive-logs/2026-07-26-s3-s4-s6-wire-green.md).

SCOPE
1. World fixtures the runner does not lay today: a primed project, a config branch. Extend the drive runner's fixture layer, then add and green drive beats for S5 Operator, S7 Forensic, S8 Neighbour.
2. bl-84f3's fixture-blocked rows: S3-T4 (needs a FAILING pre-commit hook fixture), S4-T4 uncoloured-id case, S4-T6, S4-T7 pins/overflow, S6-T1's other four predicates, S6-T5's 'M ⚠' half.
3. bl-84f3's drivable-but-not-driven rows: S3-T2, S4-T2, S6-T2.
4. Note from bl-84f3: the by-ball and activity beats assert a negative (vacuous-pass risk if a click misses) — screenshots are the other half of the proof; keep that discipline.
S9 Settler stays out — waits on phase 2.

Use the runner's await/until_landed primitives — never a sleep gating a verdict.

STATUS — DONE. No Rust changed: the delivery is scripts/drive/ + docs.

41 new beats written and greened against the build I was given (main 81dafdd). Two new runner verbs / worlds:
- `stories.sh run-s5s8` — world A, ZERO model calls: S5-T3/T4/T5 (all three §9 editors), S8-T1/T2/T3/T4, and the residual ball rows S3-T2, S4-T2 (assign + release), S3-T4, S6-T5. Fixtures: a ball bound by ▶ Start whose goal is never sent; a scratch BRAZEN_CONFIG (brazen's config is ambient by design, so an Apply beat would otherwise edit the operator's own machine); a trimmed models.yaml (the seed's comment banner is what forces config mode to scroll); a failing pre-commit hook PLUS a commit in the ball worktree (an empty work branch never reaches a commit, so the gate never runs).
- `stories.sh run-s7` — world C, 2 model calls: S7-T2/T4/T5, S6-T1 rules 3/4/5, S6-T2, S4-T4's uncoloured-id case, S4-T7. Fixtures written as lernie's own on-disk state: a hyphen-descent child branch, a strictly newer second root, refs/lernie/{budget-exhausted,conflicted}, a pending inbox deposit, a request.json that is not JSON, and a foreign workspace via `yog exec lernie new`.

THE PHASE MOVED MID-TASK. main went 81dafdd -> b42af4d (bl-b5d1's batteries + W13's gate deletion) while these ran. Consequences, all recorded in the log:
- S5-T1/T2 (the toolchain pane + the capability gate) were driven GREEN on 81dafdd and then DELETED with the phase they tested — STORIES' "removed, not skipped", now a thing that happened to a real drive. 3 beats, gone with the stub-lernie fixture.
- S8-T3 tightened: `yog env` now prints three exports (PATH joined the §16.2 set, its head the world's tools shim).
- VERIFIED ON THE MERGED BUILD: `run` 8/8, `run-s7` 12/12, `run-s5s8` 19/26, `run-s3s4s6` 6/15. All 16 red beats have ONE cause and it is a regression on main, not in this delivery: `bl claim` fails under the embedded balls ("bl-delivery: no ball on the wire: neither command.id nor a sealed bl-id trailer (§7)"), which kills the whole ball rung. Filed as bl-c9f2 with the repro, the localisation, and the acceptance test (these two runners going green again). It landed unseen because the batteries' own proof drove S0/S1 only — the two rungs that never touch bl.

ALSO FILED (not fixed): bl-9cb0 (the marks pane + composer ball row render for a workspace with no ball — focused_join returns the UnassignedWorkspace row, and `bl conf` then fails with a missing-binary message for a no-project fact), bl-307f (a malformed step file renders plain bytes where §11/S7-T2 promise an error row), bl-a942 (upstream: lernie scan exits 1 for a root recipient with pending mail; yog surfaces it verbatim).

NOT DRIVEN, stated in the log and STORIES: S5-T6 (the device flow ends at a real OAuth prompt), S7-T3's budget fold (derived RAM, no on-disk counterpart), S4-T6 beyond its head, §6 rule 5's self-clear, S9.

Deliverables: scripts/drive/{beats_s5,beats_s8,beats_s3res,beats_s7,beats_s6}.sh + two stories.sh verbs; docs/drive-logs/2026-07-27-s5-s7-s8-wire-green.md (beat tables, six harness findings, the regression section, screenshot descriptions); docs/STORIES.md (four-world harness, two new beat lists, rewritten "Drivable next", the S5/S7/S8 status paragraph, the red-ball-rung notice, four more harness rules).