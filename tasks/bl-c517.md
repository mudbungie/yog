+++
title = "S5/S7/S8 drive coverage: world fixtures (primed project, config branch) + drive beats"
created = 1785124356
updated = 1785130849
claimant = "waxier-c517"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-84f3"
on = "claim"
+++
Second half of the bl-f69b S2+ inventory, EXTENDED with bl-84f3's residuals (docs/drive-logs/2026-07-26-s3-s4-s6-wire-green.md).

SCOPE
1. World fixtures the runner does not lay today: a primed project, a config branch. Extend the drive runner's fixture layer, then add and green drive beats for S5 Operator, S7 Forensic, S8 Neighbour.
2. bl-84f3's fixture-blocked rows: S3-T4 (needs a FAILING pre-commit hook fixture), S4-T4 uncoloured-id case, S4-T6, S4-T7 pins/overflow, S6-T1's other four predicates, S6-T5's 'M ⚠' half.
3. bl-84f3's drivable-but-not-driven rows: S3-T2, S4-T2, S6-T2.
4. Note from bl-84f3: the by-ball and activity beats assert a negative (vacuous-pass risk if a click misses) — screenshots are the other half of the proof; keep that discipline.
S9 Settler stays out — waits on phase 2.

Use the runner's await/until_landed primitives — never a sleep gating a verdict.

STATUS — DONE. 41 new beats, all green on the live wire; the full suite (S0..S8) is 64 beats, all green. No Rust changed: the delivery is scripts/drive/ + docs.

Two new runner verbs, two new worlds:
- `stories.sh run-s5s8` — world A, 29 beats, ZERO model calls: S5-T1/T2/T3/T4/T5, S8-T1/T2/T3/T4, and the residual ball rows S3-T2, S4-T2 (assign + release), S3-T4, S6-T5. Fixtures: a ball bound by ▶ Start whose goal is never sent (so a workspace with a real config/default lineage and a bound project cost nothing), a scratch BRAZEN_CONFIG (brazen's config is ambient by design — an Apply beat would otherwise edit the operator's own machine), a trimmed models.yaml (the seed's comment banner is what forces config mode to scroll), a stub lernie missing one driven verb (the W5 gate's own shape), and a failing pre-commit hook PLUS a commit in the ball worktree.
- `stories.sh run-s7` — world C, 12 beats, 2 model calls: S7-T2/T4/T5, S6-T1 rules 3/4/5, S6-T2, S4-T4's uncoloured-id case, S4-T7. Fixtures written as lernie's own on-disk state: a hyphen-descent child branch, a strictly newer second root, refs/lernie/{budget-exhausted,conflicted}, a pending inbox deposit, a request.json that is not JSON, and a foreign workspace via `yog exec lernie new`.

Beats live in beats_s5.sh / beats_s8.sh / beats_s3res.sh (world A) and beats_s7.sh / beats_s6.sh (world C); stories.sh grew two verbs and two source lines. yogdrive.sh is unchanged (a `scroll` verb was added and then deleted — see the log's finding 2).

Baselines re-run green after every change: `run` (S0/S1, 8) and `run-s3s4s6` (15).

NOT DRIVEN, recorded honestly in the log and STORIES.md: S5-T6 (the device flow ends at a real OAuth prompt), S7-T3's budget fold (derived RAM, no on-disk counterpart; the limit line wants a workflow.yaml fixture), S4-T6 beyond its head (four roots in known states is still unlaid), §6 rule 5's self-clear, S9 (phase 2).

FILED, not fixed (this task writes no Rust): bl-9cb0 (the marks pane + composer ball row render for a workspace with no ball — focused_join returns the UnassignedWorkspace row and `bl conf` then fails with a missing-binary message), bl-307f (a malformed step file renders plain bytes where §11/S7-T2 promise an error row), bl-a942 (upstream: lernie scan exits 1 for a root recipient with pending mail; yog surfaces it verbatim).

Deliverables: docs/drive-logs/2026-07-27-s5-s7-s8-wire-green.md (beat tables, six harness findings, screenshot descriptions, ops trails) and docs/STORIES.md (the four-world harness description, the two new beat lists, a rewritten "Drivable next", the S5/S7/S8 standing-status paragraph, and four more harness rules).