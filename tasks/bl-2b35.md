+++
title = "macOS: cli_outbound detach test reads /proc/self/stat for the session id — fails on macos-14 CI"
created = 1784350424
updated = 1784350509
claimant = "filtered"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
CI on main (run 29621958730 and successors) fails the macos job in cli_outbound::tests::detach (detached_child_survives_and_leads_own_session and possibly the cwd/env fifo sibling): the spawned sh script proves setsid by reading field 6 of /proc/self/stat, which does not exist on macOS — the fifo write malforms/fails and the test times out or asserts wrong.

Fix shape (agent verifies against actual code): keep the survival proof fully portable (fifo write completing after the internal Child is dropped proves no Drop-kill — that part needs no /proc). For the session-leadership assertion, use a portable source: 'ps -o sess= -p $$' works on both Linux and macOS (verify locally on Linux; macOS ps supports -o sess), or split the assertion: survival portable + session-id assertion via /proc gated to cfg(target_os = linux) with the ps-based check on macOS — prefer ONE portable mechanism (ps) over a cfg split if it proves stable. Tests must hold SPAWN_LOCK across write+spawn per the repo's spawn discipline; coverage must stay 100% (tarpaulin runs serial per tarpaulin.toml).

Acceptance: macos-14 CI job green on this test (the only remaining allowed macOS red is fs_watcher coalesces_atomic_rename_to_destination = bl-71ee, being fixed separately); linux stays green.