+++
title = "shift-enter inserts a newline in the composer; enter alone sends"
created = 1785733415
updated = 1785733415
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator request 2026-08-03, verbatim: 'shift-enter should enter a newline without sending.'

The composer (src/shell/input_bar.rs / keys.rs territory — verify against the tree; bl-c038 moved the composer's panel seat into the conversation pane at 85e569d) currently treats Enter as send. Contract: Enter sends; Shift+Enter inserts a newline at the cursor without sending. Multi-line drafts must render (the input grows or scrolls per the existing egui idiom). Check how egui's TextEdit handles Enter vs multiline — the fix may be flipping to a multiline widget with an explicit send-on-plain-Enter key intercept; follow whatever the codebase's existing key-handling idiom is (src/shell/keys.rs). Note bl-a33d (ready, unclaimed) separately wants Ctrl+Enter as send-and-interrupt — do not implement it, but leave the key routing shaped so it can land without rework. Test per repo idiom; amend DESIGN where the composer's key contract is recorded. The input-as-inbox design (bl-929d, in flight) will rework the composer surface — keep this change minimal and behavioral so it survives that rework.