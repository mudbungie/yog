+++
title = "the login spawn's own doc names wall::pairs, but the flow read moved it to one wall::env lens"
created = 1788493855
updated = 1788493856
claimant = "Spellbind-Y"
priority = 3
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
bl-7c9f gave Runs::start a second reader of the workspace's wall — flow_of, which asks brazen's table which flow the row serves — so the spawn now derives ONE `wall::env` lens and lays it on the child with `wall::pairs_of`, rather than calling `wall::pairs` directly. Two docs still name the old call: `Runs::start`'s own doc comment (src/login/runs.rs) and REMOTE §8.3's Action::Login bullet ("laid on the spawn as \`wall::pairs\`"). The claim each makes is still true — the wall rides the child's environment — but the function named is no longer the one called, and DESIGN/REMOTE are authorities. One-line prose fix in each; no behaviour.