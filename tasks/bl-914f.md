+++
title = "CI-only coverage red on main since the lernie 0.0.10 lockfile change: tarpaulin misattributes transcript/spine.rs's multi-line hline call (87-88) as uncovered — bind the stroke, one-line the call"
created = 1786845366
updated = 1786845366
claimant = "Dills"
priority = 1
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
Both the release-PR CI and the main-push CI fail 99.99% < 100 naming src/transcript/spine.rs 87-88 — two argument lines inside the one ui.painter().hline(...) call in the notch closure — while the SAME tree passes the full local gate. The class is the documented tarpaulin llvm attribution hazard (the 'bound rather than chained' comments in steps_view); the dependency-graph change (lernie 0.0.10 in the lockfile, bl-4c1f) shifted codegen enough to move the phantom onto CI. Until this lands, main CI is red: the release PR cannot merge and every speculative candidate built on this tree stores a PERMANENT FAIL verdict (the bl-673a rule protects only infrastructure kills, not genuine gate fails). Fix shape: bind the stroke to a local and collapse the call to one line, the house workaround. No behavior change.