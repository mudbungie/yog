+++
title = "speculate.yml restates the balls pin and a dead billing premise: derive the version from Cargo.toml, bump the substrate to 0.5.11"
created = 1786843063
updated = 1786843063
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
Three defects in .github/workflows/speculate.yml, one root: facts restated instead of derived.

1. The header claims the repo is private and the runs bill Actions minutes. The repo has been public since the 2026-08-13 publication; public-repo standard hosted runners are free (the design doc's own Remote-builders section records this). The comment encodes a dead scarcity premise.
2. The balls version is restated in three places (two step names, a cache key, the install command). CLAUDE.md rule 6 is explicit that Cargo.toml is the pin authority and restatements have gone stale before — this one is stale the moment the pin moves. Fix: a step reads the pin out of Cargo.toml once and the cache key + install both reference it.
3. The pin itself is behind the substrate: balls 0.5.11 (speculator auto-adoption, balls bl-b761) is released and installed on the box; yog embeds =0.5.10. Bump Cargo.toml to =0.5.11 and refresh the lock.