+++
title = "REMOTE read path: migrate the whole §11 inspector family onto the wire, and spell config-frozen-at"
created = 1786767804
updated = 1786768673
claimant = "Worming"
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
The whole §11 inspector family still reads in process: transcript, steps, step detail, files, rail, inbox, and the seat's own conversation view. bl-44e9 unblocked it and did not take it.

Everything it needs exists. Each of the seven has a query; each is answered by the same answer::inspector functions the frame's view-models delegate to; and the pin the four pinnable tabs fold through is now a selection over an answer rather than a derivation — the notch carries the budget as a rollup, the transcript-as-of is a prefix of the chat that was answered, and Query::Files carries `at` for the one tab whose subject is a different tree. So this is bl-adcb's three moves again rather than a new shape.

What comes out with it: the four per-snapshot memos (transcript, steps, rail, files). An answer is the cached fold refreshed at the asker's cadence, which is what the Work tab's two memos became.

Two things to settle while doing it, neither of them a blocker.

1. Config-frozen-at has no query at all. The Config tab reads the governing-config derivation at the pinned commit in process. It is the second read whose subject is a tree the pin names, so it takes `at` the way the files read does — but it has to be spelled first, in all three serializations plus a help page.

2. The Files preview still branches on the pin seat-side, because building a whole listing to read one file's bytes is what the containment rule costs a memoized seat. One ask answers listing and preview together, so the branch dissolves rather than being moved.

Watch the harness cost: a migrated read paints an answer that landed a round trip later, and the acceptance drivers settle reads to a fixed point now (bl-44e9). A bespoke frame loop in a test file needs the settle between its passes or the surface it paints is blank.

REMOTE §9.7 carries the reasoning and the residual list.

---

Landed: the whole §11 inspector family reads over the wire (transcript, steps, step detail, files with the preview in the same ask, rail, inbox) and config-frozen-at is spelled as Query::Governing { workspace, agent, at } across all three serializations plus a help page, answered by answer::inspector::governing.

The four per-snapshot memos (tx/steps/rail/files) are deleted with the accessors. Two seats outside the inspector share its questions rather than keeping copies: the centre's auth/wound banners ask Query::Steps, the composer's recall asks Query::Transcript, and a standing question keyed by its envelope makes two callers one ask.

Query::Governing takes 'at' the way Query::Files does — a selection naming which commit — and absent it is the agent's own tip resolved engine-side, so no seat has to know a tip to ask. It is the family's one read that refuses rather than answering absent: the walk is the workspace's git and 'this conversation has no policy' is never a reading.

The Files preview's seat-side pin branch dissolved rather than moving. Its cost was a selection change: Ephemera::files_sel names the entry's path now, not its row index, because a number indexes a listing that landed a round trip ago — work_sel's own shape one tab over.

The pin stayed seat-side and DESIGN 8.5 is unamended. shell/inspector/rail.rs is two selections now: pick a notch out of the landed spine, cut the landed transcript in front of it.

Live tail: it moves at the asker's 500ms cadence rather than the derivation's, so streamed text arrives a row at a time. The follow-stream graduation was weighed and declined — it would cost a second lane on the asker (the asker's pass is serial over the standing set), which is the poster renamed, and REMOTE 10's criterion is a machine polling a machine, not prose a person reads. Recorded in REMOTE 9.7 rather than left as an unstated cost.

Harness: the fixed-point settle is one definition now (World::drain), spent by both drivers. Three counted passes stopped being enough the moment a read chained — the step drill-in's sequence name is picked out of the step list that landed. Two facts came out of it: a Link may never be settled twice without a frame between, and a fresh egui Context measures before it paints, so the whole-window driver pre-rolls two frames and reads the frame after the drain.

Not taken, ruled rather than deferred: the seat's own Query::Agent (AppModel::focused_conversation). It is the selection's view, not a tab read, and its consumers are frame-synchronous — the composer's target name, the focus walk's ancestor unfold, two click-time act gates — so a round trip of lateness is a visible regression, not a cost. Filed as bl-48ae with the three shapes weighed.

Witness: shell::acceptance::wire::the_inspector_paints_the_chat_the_wire_answers_and_nothing_without_one, both directions. New files: src/shell/inspector/reads.rs, src/boundary/answer/inspector/tests/governing.rs. DESIGN 12 rows updated for reads/rail/vms/ram-inspector/wire/acceptance.
