+++
title = "boundary-complete the inspector reads: the transcript, steps, files, rail and inbox surfaces have no headless spelling — the chats themselves are unreachable by any face but the window (REMOTE.md §9 step 1)"
created = 1786682898
updated = 1786684222
claimant = "Herring"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
docs/REMOTE.md §9 step 1, the first build step of the client/server split (adopted, bl-b9a2) — and standalone-valuable with no network at all: it finishes VISION V5 teleop parity.

Today src/shell/inspector/vms.rs builds Transcript, StepsView/StepDetail, FilesView/Preview, InboxEntry, Rail/Pin and jsonview by reading disk directly on the frame thread (memoized per snapshot via src/app/memo.rs). None of these have a Query variant, so no seat but the GUI can read a conversation: not JSON envelope, not slash line, not deposit.

Deliverable: Query/Reply pairs for these reads (Query::WorkDiff and Query::Search are the precedent — both read world bytes at the answer chokepoint, src/boundary/answer.rs), all three serializations per the compile gates, and the inspector painting from replies. Verify current file/type names before editing — this body was written at main 5df44f20 and drifts.

NOTE: this is boundary-surface work — serialize it; do not run it in parallel with other codec/boundary balls.