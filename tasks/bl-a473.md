+++
title = "decompose 3/10 — src/boundary/**/tests/: eleven §8.5 corpora at 260-296 lines"
created = 1787546567
updated = 1787547522
claimant = "Gunwale"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["decompose"]
+++
Third of ten waves sequenced by bl-b278. The test half of wave 2's subsystem, kept apart so the two never touch one file.

    src/boundary/sugar/tests.rs                              296
    src/boundary/reply/tests/roundtrip/surface/inspector.rs  294
    src/boundary/codec/tests/mod.rs                          287
    src/boundary/control/tests/mod.rs                        286
    src/boundary/fan/tests.rs                                274
    src/boundary/line/config/tests.rs                        272
    src/boundary/reply/tests/roundtrip/surface/listings.rs   268
    src/boundary/reply/tests/roundtrip/strict.rs             268
    src/boundary/line/tests/refusals.rs                      268
    src/boundary/consume/tests.rs                            262
    src/boundary/tests.rs                                    260

**`src/boundary/reply/tests/mod.rs` (297) and `tests/rows.rs` (287) are NOT
in this wave** — they are bl-9d34's, which is a duplication defect rather than
a split, and doing it there dissolves both lengths at once. Take bl-9d34 first
or leave those two files alone.

`src/boundary/sugar/tests.rs` was under another claim's surface (bl-4e31) when
this was filed; check `bl list` before touching it.

These are corpora, so no DESIGN §12 rows are due — a test module is covered by
its production module's row. The usual seam is per-family (one variant family
per file), which `reply/tests/` already runs on and can extend.

## The rule this wave runs on

Split along a **real seam** (AGENTS.md, "300-line hard cap": *"Over the cap?
Split along a real seam and add the row to DESIGN §12; never shave lines to
duck the limit"*). A split that leaves two files with no coherent separate
subject is worse than the long file — **if a file has no seam, leave it and
say so in the close note.** That is a finding, not a failure.

Every new production module needs its DESIGN §12 row: `tests/design_module_map.rs`
fails on an absent row, and it checks more than presence — rows stay in sort
order, brace families (`src/app/{mod,roots}.rs`) must expand, and a test module
is covered by its production module's row and never earns one of its own (the
`X.rs` ↔ `X/tests.rs` seam). A submodule of an integration binary under
`tests/` earns no row either (precedent: `tests/multiplex_bl/fixtures.rs`), and
such a submodule needs `#[path = "<binary>/<name>.rs"]` because the binary's
own file is the crate root.

100% coverage is the floor and a split must not lose a test: moved, not
dropped. Verify with `grep -rc '#\[test\]' src tests --include=*.rs` before and
after.

**Verify the census yourself** — `make line-cap LINE_CAP=249` — rather than
trusting the list below. The tree moves under several agents and this body is a
snapshot taken when bl-b278's first wave landed.