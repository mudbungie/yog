+++
title = "paint: bare ⚑ in the row's right-pinned metadata group, with its words on hover"
created = 1786511517
updated = 1786511517
parent = "bl-b9e3"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-5c64"
on = "claim"
+++
Subtask of bl-b9e3. Blocked on the spec subtask — read the amended
`docs/DESIGN.md` §6 and §11 first and implement what they say, not this body, if
the two ever disagree.

**The change,** `src/shell/conv_list.rs`. Delete this from the row's left prefix
group (line 149):

```rust
if row.attention > 0 {
    ui.colored_label(theme::BRAZEN, format!("⚑{}", row.attention));
}
```

and paint a bare `⚑` — no number — inside the existing right-pinned group
(`ui.with_layout(egui::Layout::right_to_left(...))` at line 175, alongside
`conv_ball::row_badge`, the `(N)` member count and `age_label`). Keep
`theme::BRAZEN`: DESIGN §11:3944 makes bronze the hue for *another agent's
doing*, and that is what the flag is.

**Give it words on hover.** The current `⚑N` has none — every other mark in the
row hovers its meaning (`state_badge`, `flight_badge`, `STATE_UNCERTAIN`,
`verdict_badge`), and the §11 glyph doctrine at DESIGN:5657 is explicit that
this seat is *"a dense repeating row"* that hovers rather than states. The
sentence should say what the flag means in §6's own terms — evidence arrived
here while you weren't looking — and that opening the conversation clears it.
Put the string in `src/theme/badges.rs` beside the other phrases, not inline:
that file is already the one mapping from mark to label-and-sentence
(`theme::mark_badge`, §6:1579).

**What does NOT change:**
- `ConvRow::attention: usize` (`src/nav/convs/row.rs:49`) and its derivation at
  `row.rs:155` — the paint drops to a `> 0` read, the field stays a count.
- `src/boundary/reply/rows.rs:34,54` — the headless answer keeps its
  `"attention"` number. Changing the boundary serialization is a separate
  decision with its own compatibility cost and is out of scope here.
- `src/shell/top_bar.rs:49/114/169/179` — the strip total and the workspace tab
  badges keep their counts (DESIGN §6:1585 rollups, §6:1617 queue depth).

**Done when:** `make test` is green (the view-model tests in
`src/nav/convs/tests/rows.rs` assert the count, which survives — if one asserts
the *rendered* string, fix it), coverage stays at 100%, no source file exceeds
300 lines, and `tests/integration/glyph_coverage.rs` still passes with `⚑`
present in the scanned set.