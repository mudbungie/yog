+++
title = "the last of the seat-shaped residue: src/nav/menu.rs and the actions root's form predicates have no caller and no carrier"
created = 1788416883
updated = 1788483801
claimant = "Spellbind-R"
priority = 3
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["boundary"]
+++
Left over from bl-7cc8, which deleted drafts, the previews, the fork composer, science compose/respdiff and the start gate, and flagged these as the same shape outside its list:

- `src/nav/menu.rs` — the §11 context-menu roster. No caller but its own tests since bl-7942.
- the actions root's remaining form predicates — `work_dir_refusal`, `goal_present`, `new_prompt_enabled`, `create_ball_enabled`, `new_ball_hints`. They judge a seat's form INPUTS before a fire; every executor already refuses exactly what they predict, and the refusal at fire is the one home (§8.5: views never cross; `actions::enabled`'s three predicates that DO cross on the agent answer stay).

RULING: same as bl-7cc8 — delete, chase the graph for anything that becomes callerless, amend DESIGN (§8.2's predicate paragraph, §11's tombstone table, the §12 rows) to say the fact is the seat's or the executor's, and run `tests/design_module_map.rs` both directions.

---

Landed as DELETE. Both premises verified against the tree first: `src/nav/menu.rs` was reached by nothing but its own tests, and none of the five form predicates had a production caller or a reply field carrying it.

**Chasing the graph took three more.** `nav::menu` was the last reader of `enabled`'s `assign_enabled` / `close_enabled` / `unclaim_enabled`, which left them production-callerless with only their own unit tests and one STORIES beat. They went too, on REMOTE §9.4's own rule — *a gate that is not derivable from a row goes on the row* — read the other way: `BoundBall::state` is a §3.5 `JoinState` that `Query::WorkspaceBalls` already answers on every row, so each gate is a one-line `matches!` a seat makes for itself, and `bl` refuses again at the fire. Same shape as the `StartGate` bl-7cc8 deleted for inputs that cross. `nudge_enabled` / `stop_enabled` / `stop_children_offered` stay: they ARE the §8.5 agent answer's `nudgeable`/`stoppable`/`stop_children`.

With the composer's rules gone, `src/actions/enabled.rs`'s own seam went with them — it was split from the root at §12's budget on the line *"[super] asks whether there is anything to fire, this asks whether this selection permits the verb"*, and the first half is exactly what left. Folded back into `src/actions/mod.rs` (112 lines), tests likewise.

**One dead premise found in the doc, not in the ball.** DESIGN §8.1 step 2 read: *"One predicate decides whether a fire may go at all, `actions::goal_present`: **a blank goal never sends**, and it is checked inside the shared `prompt` body so every spelling is gated once between them (bl-6191)."* No fire path reads it — not `boundary::dispatch::doors`, not `start::execute_prompt`, not `start::prompt`. The invariant had no enforcement anywhere in this crate. Amended rather than restored, on bl-7cc8's own precedent for the message gate's content half: a test of a composer's text is a seat's, and no server-side refusal was added there either.

**One coverage hole, and its fix names the right home.** Deleting `work_dir_refusal`'s tests left `cli_outbound::chunk`'s `WorkDirNotADir` arm — the path that exists but is not a directory — uncovered (99.99%). The test moved to `cli_outbound/tests/spawn.rs` beside the missing-directory one, which is where the question actually forks.

Docs amended: DESIGN §3.3, §3.6, §8.1 step 2, §11's tombstone table (folded into bl-7cc8's row, which this is the residue of, rather than a seventh row under a "Six facts" sentence) and the two §12 rows; REMOTE §9.4's citation and STORIES S4-T2. `tests/design_module_map.rs` run both directions: a stray `src/` file fails `every_source_file_has_a_row`, a re-added `menu` entry fails `every_row_names_a_file_that_exists`.

Net ~680 lines out.
