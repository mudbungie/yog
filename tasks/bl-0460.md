+++
title = "birth grants clients to the roles that work, never to machinery: after bl-52b7 no role in an existing or new workspace can reach a foot until providers.yaml names clients"
created = 1788742989
updated = 1788743084
claimant = "Cantaloups-Y1"
priority = 1
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["usability-r1"]
+++
bl-52b7 (0aa51081) made the clients tool part of the grant: Injection::tools declares nothing unless the role's providers.yaml grant names `clients`. litany's shipped template does not grant it (it is yog's tool), so every worker in every workspace lost the roster and with it every foot. Ruling: the grant is policy and policy lives in yog (VISION §2): yog's install-wide template override (world/litany/template/providers.yaml, DESIGN §16.2 / QUALITY §3 step 0) grants `clients` to the working roles (the default/worker role and any operator-facing role) and NEVER to compactor/reviewer/other checkpoint machinery. Existing workspaces: the wall's governing config is per-workspace, so state how an existing workspace picks it up (a documented one-line edit, or a birth-time fold that yog already owns) — not an unconditional injection. Test: a fresh workspace's worker injection carries clients; a compactor's is empty; the enrolled-foot path (thrall main, PROTOCOL 13) runs a routed bash end to end.