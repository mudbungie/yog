+++
title = "decompose 9/10 — the §7 fleet tier and the substrate tail: twenty-one files at 251-291 lines"
created = 1787546581
updated = 1787620473
claimant = "Capstan2"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["decompose"]
+++
Ninth of ten waves sequenced by bl-b278. Everything under `src/` the eight waves above do not name. Large in file count but small in each: most are corpora that split cleanly per beat family.

The fleet/control tier:

    src/control/tests/mod.rs        289
    src/fork/mod.rs                 289
    src/fan/tests.rs                284
    src/fleet/pilot.rs              277
    src/fleet/pilot/tests.rs        276
    src/science/tests/join.rs       275
    src/delete/agent/tests.rs       270
    src/control/rules.rs            260
    src/monitor/sentry/tests.rs     259
    src/actions/mod.rs              258

The substrate and plumbing tail:

    src/keymap/tests/mod.rs         291
    src/cli_outbound/tests/run.rs   282
    src/workdiff/tests/read.rs      281
    src/main.rs                     275
    src/world/marks.rs              266
    src/workdiff/mod.rs             262
    src/watch/mod.rs                254
    src/world/tools/tests.rs        253
    src/projects/join.rs            253
    src/fs_watcher/tests/mod.rs     251
    src/cli_outbound/mod.rs         251

Two need reading before cutting. `src/main.rs` is a tarpaulin exclude and a
thin entry on purpose (AGENTS.md rule 10: *"`main.rs` is a thin entry with no
error-plumbing layer"*) — if it is 275 lines of thin entry, the seam is real
and worth finding; if it is not thin any more, say so, because that is a
different defect. `src/cli_outbound/sys.rs` is the one lawful home of
`unsafe` (`rules/unsafe-outside-sys.yml`) and is not in this list — do not
move a line into or out of it.

This wave can be taken in two claims if it is too much for one; the two blocks
above touch no common file.

## The rule this wave runs on

Split along a **real seam** (AGENTS.md, "300-line hard cap": *"Over the cap?
Split along a real seam and add the row to DESIGN §12; never shave lines to
duck the limit"*). A split that leaves two files with no coherent separate
subject is worse than the long file — **if a file has no seam, leave it and
say so in the close note.** That is a finding, not a failure.

Every new production module needs its DESIGN §12 row: `tests/design_module_map.rs`
fails on an absent row, and it checks more than presence — rows stay in sort
order, brace families (`src/app/{mod,roots}.rs`) must expand, and a test module
is covered by its production module's row and never earns one of its own (the
`X.rs` ↔ `X/tests.rs` seam). A submodule of an integration binary under
`tests/` earns no row either (precedent: `tests/multiplex_bl/fixtures.rs`), and
such a submodule needs `#[path = "<binary>/<name>.rs"]` because the binary's
own file is the crate root.

100% coverage is the floor and a split must not lose a test: moved, not
dropped. Verify with `grep -rc '#\[test\]' src tests --include=*.rs` before and
after.

**Verify the census yourself** — `make line-cap LINE_CAP=249` — rather than
trusting the list below. The tree moves under several agents and this body is a
snapshot taken when bl-b278's first wave landed.