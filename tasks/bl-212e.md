+++
title = "ops error surface shows stale failures as if current"
created = 1784783961
updated = 1784959179
claimant = "Exultantly-212e"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
A failed op from 3 days prior (lernie prime, unrecognized subcommand — since fixed and re-run green) still read as THE error when the operator hit an unrelated failure on 2026-07-22, sending diagnosis down a false trail. The activity/ops surface should scope or age error prominence: a later successful run of the same verb should retire the failure from the ambient surface (it stays in the log; absence of a live failure is the record).

## STATUS (checkpoint 2026-07-25 ~04:45Z, Exultantly-212e)

Worktree: /home/u/.local/state/balls/plugins/bl-delivery/home/u/dev/yog/bl-212e (claimed; all work COMMITTED on work/bl-212e, working tree clean).

### Implementation — DONE
Retirement rule, derived at read time, nothing stored: walking the ops tail newest-first, a failed row is a LIVE failure unless a later row with the same (cwd, verb) did not fail. verb = the leading two tokens of the joined argv (binary + subcommand: 'bl close', 'lernie prime', 'yog-step mint') — the argv tail carries per-run operands (ball id, composed goal) that never repeat, so keying on the whole argv would retire nothing; cwd scopes it so a clean run in one project leaves another's failure live. Success = !OpRow::failed (the surface's own classifier negated — no second definition of success).

Files:
- NEW src/opslog/live.rs — live_failures(rows) -> Vec<bool>, verb_key(), plus Activity/activity() MOVED here from rows.rs (chip's error count now counts live failures only). 5 unit tests.
- src/opslog/rows.rs — Activity/activity() and their test removed (OpRow/SurfaceFailure unchanged).
- src/opslog/mod.rs — declares/ re-exports live (pub use live::{Activity, activity, live_failures}).
- src/shell/activity.rs — zips rows with live_failures; ichor only on a LIVE failure, a retired failure keeps its warn glyph but renders weak.
- src/app/focus.rs — activity() doc notes the live-only count.
- src/app/balls/tests/ops.rs — model-level test a_re_run_green_verb_retires_its_stale_failure_from_the_chip.
- docs/DESIGN.md — §6 new paragraph 'Ops error prominence retires the same way — derived, never stored' (the rule verbatim), §5.1 row 25, §11 activity-accessory bullet, §12 module-map row for src/opslog/{mod,line,rows,live,detached}.rs.

### Gates — PASS on the merged tree
fmt-check, clippy -D warnings, ast-grep rules-audit, cargo-deny all clean; cargo tarpaulin: 100.00% coverage, 4306/4306 (src/opslog/live.rs 18/18). main folded in twice already: bl-a649 (detached stderr; conflicts in docs/DESIGN.md §12 row and src/app/balls/tests/ops.rs resolved by keeping BOTH tests and the union of imports) and bl-8e07 (docs only).

### REMAINING — just the close
Run from /home/u/dev/yog: bl close bl-212e --as Exultantly-212e. Two transient aborts so far: (1) killed under machine load, (2) bl-delivery no-resurrection invariant because main had moved (docs/STORIES.md, docs/drive-logs/*) — the fix is 'git merge main' in the worktree first, then close again. So the loop is: cd worktree, git merge main (resolve if needed), then close; repeat if main moves under the ~25-minute tarpaulin gate. Nothing else is left to write.