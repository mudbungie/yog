+++
title = "the third boundary roster is at the 300 wall too: Query rests at 298 and the §9 config reads are the untaken fold"
created = 1788329638
updated = 1788329638
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
bl-92d3 measured `action.rs` (295) and `reply/model.rs` (297) and took the `bl` family fold on the first. It did not measure `src/boundary/query.rs`, which rests at **298** against the same 300 wall — the same inversion AGENTS.md names: a file ON the wall fires on whoever touches it next, at the moment they are finishing something else, when the cheapest way out is exactly the shave the rule forbids.

That is not hypothetical. bl-2410 needs ONE new populating read (`Query::Roles`) and cannot add it: a bare variant plus a minimal doc lands at ~305, and there is no honest way to fit it.

The hatch is §12's own, already exercised four times on `Action` and once by bl-92d3: *a family whose members every layer beneath already reads as a pair folds to ONE variant over that family's own type*.

**The §9 config reads are that family, and the seam is drawn in the layer beneath already.** `ReadConfig`, `Lineages`, `Models`, `Marks`, `Providers` (and `Roles` when it lands) are answered by ONE executor module — `boundary/config.rs`, whose own doc calls them "the §9 family's executors" — routed by one block of `answer.rs`, spelled by `codec/config.rs`, and read by `line/config.rs`. Fold them to `Query::Config(config::Read)`.

Two things to get right rather than fast:
- The address table cannot delegate uniformly the way `Action::Tune` does: `ReadConfig` carries a `ConfigFile` destination whose workspace is nested and optional (`ConfigFile::workspace_slot` already returns `Option<&mut String>`), while the others name a workspace outright. The carrier's own `workspace_slot` must therefore be `Option`, and that is the honest signature rather than a special case at the table.
- The fold is in the carrier, never in the surface: each member keeps its own slash verb, its own envelope `op` and its own help page, so no protocol version moves and the corpus regenerates byte-identical. That is the check that the refactor was behaviour-preserving.

Measure `reply/model.rs` again while here (bl-92d3 acquitted it at 270; confirm it has not crept), and do this ALONE, before the next boundary act rather than during one — shared-surface work serializes.