+++
title = "W14 batteries proof: clean-room story drive with only yog+git on PATH"
created = 1784784300
updated = 1785130183
claimant = "waxier-97c7"
parent = "bl-b5d1"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
DESIGN §16.7 W14. Needs W12-W13 [sealed]. Fresh XDG roots, PATH scrubbed of lernie/bl/bz, scripts/drive/stories.sh S0/S1 green to the live wire; record as standing done-bar in STORIES.

STATUS 2026-07-26 — DONE, and it found the hole it was built to find.

The room: scripts/drive/cleanroom.sh (new, tracked) builds PATH=<room>/bin:/usr/bin:/bin — one yog binary plus system git/sh/coreutils and the harness's Xvfb/xdotool/ffmpeg — with fresh XDG_DATA_HOME/XDG_STATE_HOME/XDG_CONFIG_HOME/LERNIE_HOME, and ASSERTS the scrub (command -v lernie|bl|bz must all fail, yog+git must resolve) before driving. Only brazen's config+credentials reach in from ambient, which is §16.2's own carve-out and the wire's precondition.

Result: ALL EIGHT S0/S1 beats PASS to a live gpt-5.4 wire, reply painted in the transcript. A ps sampler proves every substrate process image in the chain is the driven yog under a namespace prefix (yog lernie prompt, its successor yog lernie advance, the adapter yog bz --json --provider codex) with ZERO host-binary processes; ops argv rows stay logical by design (§8.2), so the physical proof is ps + the seeded world/tools/{bl,lernie,bz} shims.

Findings (filed, not papered over):
- bl-44a5 — yog exec/env layer the world PATH but never seed world/tools; before a first Start the hatch fell through to the operator's host bl on every machine that had one, and dies ENOENT in the room.
- bl-2930 — the epic's real hole: the embedded bl refuses prime (W9 ruling) and its remediation names a host bl, which a clean room by definition lacks. So no project can be primed with only yog on PATH; the balls section renders 'No projects yet — add one with: yog exec bl prime' and that gesture cannot work. clean-room run-s3s4s6 is its acceptance test.

Recorded: docs/drive-logs/2026-07-26-w14-cleanroom.md; STORIES 'The standing done-bar: the clean room' + the S9 amendment; DESIGN §16.7 W14 result row + the §16.4 W9 falsification note.