+++
title = "world tools: add the yog shim — a responder (and any world agent) cannot call 'yog gesture' today"
created = 1786843213
updated = 1786843213
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["implementation"]
+++
Split from bl-02c2's premise check (Hessian, 2026-08-12), which found this rung is NOT gated by the monitor-calibration gate that blocks the rest of that ball. Verify premises against HEAD before editing; DESIGN wins over this body.

The gap, verified at that check: `world::tools::ROSTER` is exactly six entries — `bl`, `lernie`, `bz`, `bl-delivery`, `bl-tracker`, `tool-control` — and `cli_outbound::resolve::Binary` has no `Yog` variant. So there is no `yog` on the world's PATH: a VISION §4.9 responder cannot call `yog gesture`, and V5's promise (an agent operating yog's headless surface) has no clean-room path — in `make drive-cleanroom` (only `yog` and `git` on PATH) there is no host yog to fall through to. A shim also pins the call to THIS yog rather than a possibly-stale installed one.

Scope: add `yog` to `world::tools::ROSTER` and a `Binary::Yog` variant to `cli_outbound::resolve`, resolving to the running yog's own executable (current_exe is the honest source — the shim must pin THIS binary). Adding the shim fires nothing and decides nothing: it is a pure capability fact. Tests per the house floor (100% coverage); check whether an existing drive beat should prove the shim resolves in the world PATH. Amend DESIGN where the roster is recorded (§16) if it enumerates the six.

Explicitly NOT here: any ladder wiring, any verdict→response config, the attention hook — all of that stays on bl-02c2 behind the operator's calibration pass.