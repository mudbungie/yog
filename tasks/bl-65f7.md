+++
title = "first quality audit: capture the shot-sheet and score it against docs/QUALITY.md; one ball per violation"
created = 1786161490
updated = 1786162826
claimant = "Shotwright"
priority = 1
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
docs/QUALITY.md (landed bl-b3ac) is the interface-quality authority: rubric §1 (G/H/F/L criteria), shot-sheet §2, protocol §3, currency §4. This is the first audit; follow §3 exactly.

1. Build and isolate: make release; scratch world; claimed seat. Never the live world.
2. Populate with the drive harness's own verbs (stories.sh seed + the cheapest beats that materialize each sheet row; run-s5s8 needs no wire). A row unreachable because a functional beat is red is a NAMED skip citing that red's ball — never a silent absence.
3. Capture every §2 sheet row at the three sizes via yogdrive.sh shot.
4. Score every (row, criterion) pair pass/violation/n-a, pixels as witness.
5. File one ball per violation: symptom in the title; criterion id, shot filename, and reproduction gestures in the body. Search history first — regressions name the closed ball they resurrect.
6. Record docs/drive-logs/YYYY-MM-DD-quality-audit.md: sha, tuple, coverage incl. named skips, the scorecard table, filed balls.

Triage, not repair: this ball fixes nothing. Re-audits after fix waves are filed as fresh balls citing this one's log.

Acceptance: the log and scorecard on main naming the audited sha; every violation a filed ball; every skip named.