+++
title = "drive harness: the composer click coordinate (575,684) is stale — 2 S1 beats red on main"
created = 1785132695
updated = 1785133276
claimant = "waxier-keys"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
DONE — dissolved, not re-measured: bl-30e3 gave §11 a composer-focus key, so the coordinate is gone rather than corrected.

## What changed
scripts/drive/stories.sh: all three composer-box clicks — `bare_start` (700,116 bootstrap), `message_it` (575,684) and `prompt_existing` (575,684) — are now `key Escape` then `key i`. §11's `i` hands the keyboard to whichever composer is painted (bottom composer or empty-world bootstrap) **without touching the target** (`n` would clear the selection); Escape first because a binding is suppressed while a text box holds the keyboard, so a bare `i` into an already-focused box types an `i` into the goal. **stories.sh now carries no coordinate at all** — its STEERING RULE header says so, and the remaining clicks in the sourced beats_* runners are picks (§11 rule 2).

The stale-coordinate diagnosis in the original body was exactly right: the bottom panel stacks header y=686, text edit y=705, verbs y=726, and 684 landed on the header row, so the type went nowhere and `message_enabled` refused the Enter. The s1-05 screenshot of this run shows the same layout — the key path is simply immune to it.

## Proof (live wire, Xvfb, real yog built from this worktree)
`scripts/drive/stories.sh run` — **8/8, ALL BEATS PASS** (was 6/8):

    S0 bare-start: lernie new fired                PASS
    S0 seeded-skip: no lernie prime                PASS
    S0 bare-start: detached lernie prompt          PASS
    S0 payoff: wire reply on disk                  PASS
    S1 message-to-agent: lernie message            PASS   <- was FAIL - no message verb
    S1 restart: idle is pure (INV-1)               PASS
    S1 prompt-existing: new root agent             PASS   <- was FAIL - no new agent
    S1 prompt-existing: no re-mint                 PASS

Deeper than the beats assert: s1-05-message.png shows the transcript carrying the goal, the model's `Wire check OK.` and the S1 deposit `Now respond with exactly: Second wire OK.` **verbatim** — no stray `i` leaked into any payload, which is the thing the Escape-then-i idiom exists to guarantee.