+++
title = "drive harness: the composer click coordinate (575,684) is stale — 2 S1 beats red on main"
created = 1785132695
updated = 1785133230
claimant = "waxier-keys"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Found while landing bl-89a4 (registry adoption); NOT caused by it — reproduced identically on unmodified main (0d914fc).

`scripts/drive/cleanroom.sh <yog> <root> <out> run` scores 6/8, not 8/8. The two red beats are exactly the two that click the composer text box:

    S1 message-to-agent: lernie message      FAIL - no message verb
    S1 prompt-existing: new root agent       FAIL - no new agent

Both call `"$drive" click "$wid" 575 684` (stories.sh, the `message_it` and `prompt_existing` gestures). In the current layout the yog window is 1150x760 and the bottom panel stacks: composer header row y=686, the text edit y=705, the Message/Stop/Scan buttons y=726, activity y=748. y=684 lands ABOVE the text edit, on the header row — so the subsequent `type` goes nowhere, the draft stays empty, `message_enabled` refuses the Enter, and no verb is dispatched. Screenshot s1-05-message.png shows the composer rendered with its placeholder text intact ("message the selected conversation"), which rules out the other documented cause (the panel not rendering because nothing is selected — that path is already re-armed by the retry loop).

This is the same pixel-coordinate drift class stories.sh already records at the conversation list ("it did: e8f7033s `recent | by ball`"). The structural fix is bl-30e3 (bind §11 keys for the nine drive-click gestures, retiring the CLICK (no binding) tags) — that would delete this coordinate entirely. The stopgap is to retarget the click to the text edit (y ~ 705).

Note the S3/S4/S6 suite is unaffected: `cleanroom.sh ... run-s3s4s6` is 15/15 green.