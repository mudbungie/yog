+++
title = "collapsed op badge calls a detached handoff 'ran clean' — OpOutcome lacks a detached variant"
created = 1785647712
updated = 1785648021
claimant = "Vetch"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Leftover scoped out of bl-afa9 (bbc7286, see its journal): the expanded ops row now words a detached spawn honestly ('detached — handed off, no exit to observe'), but the collapsed row's badge comes from theme::op_badge over OpOutcome, which has no detached variant — so the badge hovers 'ran clean' for a handoff whose outcome is unknowable. Fix: an OpOutcome variant (or equivalent) for detached, rippled through the §6 retirement projection and the activity chip counts (a handoff is neither ok nor failed for the ⚠ count — decide and document which bucket, probably neither). Build on src/opslog/exit.rs's ExitKind — the classification exists; this is the badge/rollup half. Verify against current main first.