+++
title = "personal material survives in the current tree: an operator address in a DESIGN example, a third party named in VISION, and verbatim dated dialogue through both docs"
created = 1786677238
updated = 1786677505
claimant = "Calzone"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
tags = ["publication"]
+++
Source: publication audit follow-up 2026-08-13 (item 4), snapshot yog `e758814`
(i.e. after bl-244f deleted the 11 drive logs).

Current-tree personal material:

- `LICENSE:3`: `Copyright (c) 2026 mudbungie <mudbungie@gmail.com>`
- `docs/DESIGN.md:1136`: `"identity_last_used": "<redacted-address>"`
- `docs/VISION.md:9`: `Sources: the 2026-07-30..31 maintainer dialogue
  (the operator ↔ a third party), the third party's ...`
- DESIGN and VISION carry dated verbatim operator dialogue and internal
  decision chronology throughout.

Scope of THIS ball: the current tree only. Convert retained rationale to
impersonal prose — keep the reasoning, drop the who-said-what-when. Replace
personal addresses in examples with obviously-fake placeholders. Third-party
names (e.g. a third party) must not appear at all: a third party did not consent to
publication.

The `LICENSE` copyright line is a deliberate authorship claim, not a leak.
Leave it as-is and report it: whether the maintainer's public identity is
intentional is an operator decision, not an editorial one.

Out of scope, tracked separately: Git history author emails and previous blobs,
and the published crates.io artifacts. Current-tree cleanup does not erase
them.

---

Audit body verified against the tree at e758814. Three of four claims hold; the fourth is understated.

- LICENSE:3 — confirmed, left untouched per the ball.
- DESIGN's `identity_last_used` — confirmed, but at line **1131**, not 1136. Replaced with `op@example.invalid`.
- VISION:9 Sources line — confirmed: third-party name in two places on one line. The two document titles it cites (*fleet-setup*, *Bounding Agent Lifetime to the Unit of Work*) are kept as titles, since §4.1 and §4.7 argue from them; only the person is gone.
- "DESIGN and VISION carry dated verbatim dialogue throughout" — confirmed, and it is not only those two. The same material is in README.md (2), AGENTS.md (1), docs/QUALITY.md (1), docs/STORIES.md, and ~45 Rust doc-comments plus 6 drive scripts, because the doc text was copied into module headers. Scrubbing only the two docs would have left the quotes in `src/`.

Editorial rule applied: attribution and verbatim quotes out, reasoning kept as impersonal prose; ball ids kept (they are task refs, not identity); a ruling that was referenced by its date is now referenced by its ball id or by a name (blast-radius / per-agent / settings-seat / band-order / overlay / focus / everything-is-keyboard-operable).

Left in place deliberately: STORIES' drive-run dates ("the 2026-07-26 W14 clean-room drive") — those date a test run, not a decision, and are the only handle those runs have. The `mudbungie` GitHub handle in Cargo.toml / README / release-plz.yml is the same class as the LICENSE line: a publication identity, an operator call, not an editorial one.
