+++
title = "spec: the conversation row carries a flag, not a count — amend DESIGN §6 and §11"
created = 1786511516
updated = 1786511714
claimant = "Pennant"
parent = "bl-b9e3"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Subtask of bl-b9e3. **Doc-only — no code in this ball.** The design doc rules
first; the paint (bl-b9e3's implementation subtask) follows it, so this lands
before that one is claimable.

Three edits.

**1. `docs/DESIGN.md` §6:1591-1593.** Current text:

> is what is left after the ack. **The conversation list is no longer one of
> these groups** — since bl-cad5 it sorts by recency alone (§11); attention
> there is a badge and a count, not a rank.

The trailing clause is the whole grant for the row's `⚑N` and it is
argument-free. Amend so the row carries a **badge only** — not a count, not a
rank — and say why in this section's own terms: the count is a second encoding
of what §6:1570's carriers (state badge / `✉n` / the `refs/lernie/*` marks)
already render on that row, and §6:1617's queue-depth defence of the *number* is
an argument for the strip's one global total, not for a per-row tally. Cite the
operator ruling by this ball's parent id.

**2. `docs/DESIGN.md` §11:3780** — the conversation-row anatomy. Current text:

> then one row per root agent: state badge (aggregated over the conversation's
> subtree — InFlight > Live > the root's settled state, with the §10 "?"
> uncertainty suffix; both hover the state in words, per the glyph doctrine
> below), the conversation's **name** with its preview weak beside it (§3.3's
> display ladder: the name fact — lernie-stored, legacy goal stamp as fallback —
> is the title, the first payload line is the preview/subtitle, the root id when
> neither exists), age, and the **live-activity indicator** (below).

This enumerates the row's contents and never mentions the attention flag at all,
in either seat. Add it, **in the trailing group** beside age and the ball badge,
and state the rule the seat comes from: the row's prefix group must not grow a
conditional element, because every one of them shifts the title's left edge and
the name column stops being a column. Name the existing right-pinned group
(`bl-9669`'s "trailing metadata pinned right, the title filling what is left")
as where conditional per-row marks belong.

**3. Check the neighbours before writing.** The same left-prefix group holds two
other conditionals — the flight chip and the §10 `?` — so the rule as written in
edit 2 either indicts them too or must be stated narrowly enough not to. Read
`src/shell/conv_list.rs:132-162`, decide which, and write the sentence that is
actually true. Do **not** widen this ball to move them; if the rule indicts them,
say so in the doc and file a follow-up ball naming them.

**Done when:** the two sections read consistently with each other, no other §
still implies a per-row count (grep `docs/` for `attention count` and for
`⚑`, including `docs/STORIES.md:854` — *"each badged with its own attention
count"* — and `docs/drive-logs/` references are history and must NOT be edited),
and the pre-commit gate passes.