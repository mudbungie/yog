+++
title = "decompose: 307 of 1112 files are over the 200-line pre-split band and three sit exactly on the 300 wall — split for real, and stop the doc asserting a census it cannot know"
created = 1787545614
updated = 1787546591
claimant = "Marlinspike"
priority = 1
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
## The census, today

`make line-cap LINE_CAP=289` lists **32 tracked files at 290 lines or more**.
Three are at the wall exactly — one added line fails the gate:

    src/shell/acceptance/elision.rs   300
    src/shell/acceptance/fixture.rs   300
    src/transcript/tests/vm.rs        300

and five more are within four lines of it (`src/boundary/answer/tests/mod.rs`
299, `tests/design_module_map.rs` 299, `src/inboxview/tests.rs` 298,
`tests/design_citations.rs` 298, `src/watch/tests.rs` 297).

## Why this is a defect and not a number

The cap is meant to fire at design time, on the author about to add to a file —
CLAUDE.md is explicit that ≥200 is *"a design-time projection rule"* and that
the remedy is *"split along a real seam (DESIGN §12) — do not shave lines"*. A
file resting at 300 inverts that: the rule fires on whoever happens to touch it
next, at the moment they are finishing something else, and the cheapest way out
under that pressure is exactly the shave the rule forbids. One agent already
reported trimming a doc comment to stay under the wall this week — the count
turned out to be wrong, so nothing was actually shaved, but the reflex is the
one this ball exists to remove.

Two of the three are `src/shell/acceptance/*`, which is the surface the
remaining REMOTE §8.2 window chain (bl-028a, bl-670c, bl-f29c) must add
acceptance coverage to. They will hit this.

## The doc is stale in the same breath

CLAUDE.md, "300-line hard cap" section:

> The band is the aspiration, not the limit: bl-52f8 swept the tree so nothing
> rides the 300 wall, and nothing has since — **ask `make line-cap LINE_CAP=n`
> for today's census rather than trusting a count written here** (this line has
> been wrong before: it named `transcript/rows.rs` at 266 as the tree's one
> ≥250 file long after bl-2335 split that file in two).

The parenthesis is the tell: this sentence has gone stale once already and has
now gone stale again, in the identical way. *"Nothing rides the 300 wall, and
nothing has since"* is false — three files are ON it. Fix the claim by removing
the standing assertion rather than by updating the number: the line already
tells the reader to ask `make line-cap`, so the assertion beside it earns
nothing and is the part that keeps rotting. One fact, one home, and the home is
the command.

## Scope

Split the three at 300 along real seams and add their DESIGN §12 rows (a
module-map test fails on an absent row; note a test module is covered by its
production module's row and never earns one). Sweep the 299–297 band while you
are there if the seams are obvious; do not force a split that has no seam —
say so instead.

Do **not** widen the cap, and do not add an allowlist. Verify the census
yourself with `make line-cap LINE_CAP=289` before editing: this body is a
snapshot and the tree is moving under several agents.

---

**Widened by operator ruling.** 300 is a hard wall, not a target — files should
normally be considerably smaller. Today's census, measured rather than quoted:

    >=200 lines   307 files      >=250 lines   130 files
    >=280 lines    61 files      ==300 lines     3 files
    tracked non-exempt          1112 files

So **28% of the tree sits in the band CLAUDE.md calls a design-time pre-split
rule**. That is not a band any longer; it is the median drifting upward. Note
CLAUDE.md's own number — *"94 of 395 source files sit in that band today"* — is
stale in both terms, which is the second stale count in this one section and the
reason the fix below is to delete the assertion rather than to update it.

This ball is a first wave plus a plan, not a tree-wide sweep in one claim: a
split is rename-heavy and conflicts badly, and several agents are working this
repo concurrently. Take the wall and the near-wall band, then sequence the rest
as balls sized to land independently.

---

Wave one landed in this ball; the remainder is sequenced as eleven balls that touch no common file and land in any order.

Waves, in the order they should be taken (the ordering rule is *which surface is edited soonest*, not which is worst):

    bl-dd00  1/10  src/shell/acceptance/          10 files  (§8.2 chain adds coverage here next)
    bl-c088  2/10  src/boundary/ production        7 files
    bl-a473  3/10  src/boundary/**/tests/         11 files
    bl-eae9  4/10  src/wire/, tool_host, engine    7 files  (after bl-4e31)
    bl-49cc  5/10  src/shell/ outside acceptance  11 files
    bl-3b22  6/10  src/app/ + src/transcript/     13 files
    bl-fae3  7/10  git_tree/nav/start/config_edit 15 files
    bl-5aae  8/10  the paint tier                 10 files
    bl-063b  9/10  fleet tier + substrate tail    21 files (splittable in two)
    bl-7547 10/10  tests/ + scripts/               8 files

and beside them bl-9d34, which is not a split: src/boundary/reply/tests/mod.rs is 297 lines because five of its row-encoder beats already exist in tests/rows.rs — three verbatim, two as strict subsets there. Deleting the stale copies takes that file to ~95 lines and dissolves the length without a seam. Wave 3 defers both reply-test files to it.
