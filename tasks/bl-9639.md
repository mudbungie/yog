+++
title = "one-time sweep: DESIGN sentences that describe a mechanism nobody wrote"
created = 1786685187
updated = 1786686888
claimant = "Voussoir"
priority = 3
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["docs"]
+++
Filed from Anvil's bl-1015 finding (2026-08-13): DESIGN §10 claimed macOS probe results are 'refreshed eagerly on watcher events touching the agent' — only the sweep half existed; Anvil implemented the missing half rather than trimming the doc. Same pattern hit twice more today: bl-70b8's body claimed tests/integration/support/clock.rs 'had to be created' when it already existed, and bl-bb20's rungs claimed coverage that had no substrate beats.

Scope: one bounded read-through of docs/DESIGN.md hunting sentences that assert a present-tense mechanism ('X does Y', 'X is refreshed/gated/derived') and verifying each against src/. A finding becomes either a small implementation ball or a doc trim — whichever the surrounding rulings support; do not implement inline. This is judgment work, not mechanically checkable (bl-70b8 shape 5 refusal stands); it is a sweep, not a standing gate. Record per-section findings in this ball's body as you go so a kill mid-sweep loses nothing.

---

SWEEP OPENED (Voussoir, 2026-08-14). Method: docs/DESIGN.md is 8498 lines, so it is swept in section order in six slices (§0-§3 / §4-§7 / §8-§9 / §10-§11a / §11b / §12-§16), each slice read whole and each mechanism claim checked against src/ before it counts. Verdicts are recorded here per slice as they land, so a kill mid-sweep loses only the slice in flight. Judgment stays conservative per the ball: a sentence that is true but worded differently in code is NOT a finding, and neither is normative prose ('must', 'never'), a retired/declined/rejected mechanism, or a claim about lernie/balls/brazen rather than about yog's own tree.

FINDINGS ALREADY IN HAND (§12, module map), found while landing bl-b6d0 in the same tree:
1. DUPLICATE STALE ROW — the map carried TWO rows for one module: `src/inboxview/{mod,render,wire,tests}.rs | 213+86+54+292` and, immediately below it, `src/inboxview/{mod,render,tests}.rs | 180+75+200` — the second a leftover from before `wire.rs` landed (bl-6233), with counts stale by ~40%. VERDICT: trimmed (deleted) in bl-b6d0's delivery, since it is the row for the file that ball was editing. The machine check could not catch it: every path a duplicate row names still exists, so both directions of `tests/design_module_map.rs` pass.
2. §12 says 'Rows stay sorted by module path' — the `src/inboxview/…` row sits between `src/git_tree/…` and `src/fan/…`. Unverified how widespread; being counted.
3. §12's shell/acceptance rows state a budget of '≤250 each' while inbox_composer.rs (297), elision.rs (294), fixture.rs (291), settings.rs (275), name_column.rs (269), walls.rs (268), naming.rs (264), echo.rs (259), unfold/mod.rs (255), legible/mod.rs (255) all exceed it — 11 files over, none over the 300 hard cap. Verdict pending: this is a budget claim, not a mechanism claim, and the doctrine sentence beside it ('anything projected ≥200 is pre-split at design time, not at the cap') is the ruling the numbers have drifted from.

---

SCOPE NOTE on the three motivating instances in the filing. Only ONE of them is a DESIGN sentence and therefore in this sweep's scope: §10's eager-liveness half (bl-1015 — checked in slice §10-§11a below). The other two were BALL-BODY claims, not doc claims: bl-70b8's body said tests/integration/support/clock.rs 'had to be created' when it already existed (it is in the tree today, arriving with 4dca48ef 'yog: the public tree, at the 2026-08-13 publication audit'), and bl-bb20's rungs claimed coverage with no substrate beats behind them. Ball bodies are covered by a different guard — bl-1043's ruling that nothing scans the task store — so this sweep does not chase them; it reads docs/DESIGN.md only, as the filing scopes it.

---

SLICE §0–§3 — 3 findings, 1 mismatch. VERDICTS:
- §2 I9 ('All ordering is derived: projects sort by path, **workspaces by ball id**, agents by descent order') — TRIMMED. Nothing sorts workspaces by a ball id: `binding::roots` sorts by path (src/binding/mod.rs:172) and the tab bar's named strip by name (src/nav/tabs.rs:137). Left over from the retired path-convention binding — since §3.2 moved binding to the claimant a workspace holds N balls and has no id to sort by. Now reads 'workspaces by path (the tab bar's named strip by name)'.
- §2 I3 + §5.2 ('leftovers older than 24 h are swept at startup' for `.<name>.yog-tmp-<pid>`) — BALL FILED, bl-e47c. Only the stage-dir half exists (`sweep_staging`, src/config_edit/branch/edit.rs:232, called from src/engine.rs:68); no code ever enumerates a `.yog-tmp-*` file. Doc left standing deliberately: it is an invariant, so the ball rules implement-or-amend rather than this sweep deleting a durability promise unilaterally.
- §3.6 step 2 ('yog's own file, one ordinary **debounced** write (§4.1)') — TRIMMED. §4.1 itself says 'There is no debounce, no pending-write state, and therefore no flush' (bl-b54e), and src/ui_state/prune.rs says 'One write-through save (§4.1)'. The sentence cited §4.1 for a mechanism §4.1 had retired. Now reads 'one ordinary write-through save'.
- §3.3 ('the fix is both halves: `inboxview::header_line` reads `nav::convs::id_floor` like every other seat') — TRIMMED, and it was my own debt: bl-b6d0 (delivered 4c7852df, one ball earlier in this same wave) moved that seat onto `display_name_of` — the whole ladder — so the sentence named the wrong call within hours of being true. §5.1 #11 records the new rule; §3.3 now says 'stopped spelling the chain', which is what bl-3aa1's clause was actually about and stays true under either seat.

---

SLICE §4–§7 — 1 finding, 1 mismatch. VERDICTS:
- §5.1 row #5 ('Work-worktree path | formula + git | bl-delivery formula recompute; **`git worktree list` ground truth**; `bl claim` stdout cross-check') — TRIMMED. The outer two steps exist (`binding::work_worktree_path`, src/binding/mod.rs:76; `start::exec::cross_check_claim`, src/start/exec.rs:192, which compares `bl claim`'s stdout against the canonical and `<id>-<claimant>` formula candidates and refuses with `Drift`). The middle one does not: `git worktree` is not among the subcommands yog spawns anywhere outside test fixtures. Trimmed rather than filed, because the surrounding ruling forbids it — a third reading of one fact could only disagree with the two that already agree, and `bl claim`'s stdout is the authority balls itself prints. The row now states the two readings and says why there is no third.
- §7.1 and §7.2 both cite `src/fs_watcher/drift_tests.rs` — TRIMMED (path fix, two seats). No such file; the tests live in src/fs_watcher/tests/drift/{mod,loss,refs}.rs — the packed-refs regression §7.1 points at is `refs.rs::a_packed_ref_deletion_now_reaches_the_watcher`. The sibling path in the same sentence, src/app/tests/drift.rs, does exist and does hold what it claims.
Everything else in the slice verified TRUE: the ui.json write-through/elision/echo-hash path, the four ops sentinels and CAP 4096, `clip_goal`'s literal elision, ack-failures/clear-trail and the single `since_ack` derivation, ExitKind/detached rows, the monitor and fleet row shapes, `context::of_conversation`'s max(input, cache_read+cache_write), Mark's Sweep<Poll<Watch<Desync merge, `stale_label`, `drift::lateness`, `Snapshot::growth`, the sentry's one-check-per-tick and the pilot's unarmed early return, WoundGrace, `Watcher::is_stale` on (dev,ino), and the packed-refs allowlist.

---

SLICE §8–§9 — 1 finding, 1 mismatch. VERDICTS:
- §8.2 ('Short verbs show a busy indicator (RAM — the underlying fact is the `ops.jsonl` line plus substrate state both instances see)') — TRIMMED. No busy state exists anywhere: `ShellState` (src/shell/ram.rs:81) has no per-verb flag, and every short verb goes `shell::dispatch` → boundary `Action` → `actions::verbs::run_logged` (src/actions/verbs/dispatch.rs:72), a piped run to completion on the calling frame thread. There is no asynchronous window in which an indicator could paint, so this is not a gap to fill but a sentence describing a design yog does not have: short verbs are piped inside the gesture, only the long ones detach (§8.1), and the feedback is the durable ops row the activity chip and the §7.3 banner read back. Rewritten to say exactly that. NOT filed as a ball: the piped/detached split is the deliberate ruling, not an oversight.
- §8.5 ('**The verb table is the single source.** `boundary::help::TABLE` …') — TRIMMED (name fix). No `TABLE` item exists; the roster is three consts (`table::ACTIONS`, `table::standing::STANDING`, `table::queries::QUERIES`) composed by the function `boundary::help::table()` (src/boundary/help.rs:28). The mechanism the sentence describes — one source, read by refusals, completion, /help and the parity tests — is fully implemented; only the name had gone stale.
Everything else in this slice verified TRUE, and it is the best-backed span of the document: cli_outbound resolution + run_env + the YOG_NAME/YOG_WALL binding, the detached sink with /dev/null degradation and the reaper thread, the claim cross-check, `["yog-step","mint"]`, the seen-key prune on conversation delete, `deposit::mint`'s O_EXCL reply-slot reservation, search cancellation between conversations and its MAX cap, the whole capability family (src/control/, boundary/control/floor.rs), the confinement birth gates, §9's pristine-refresh + hash-guard pipeline, the context_window served-vs-declared split, and the cadence Number bounds shared with src/app/cadence.rs.
