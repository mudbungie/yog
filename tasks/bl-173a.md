+++
title = "Z2: binding rework — names root + claimant join"
created = 1784523545
updated = 1784523545
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-9769"
on = "claim"
+++
DESIGN §3.1/§3.2/§3.5 / §15 M6 Z2. Replace src/binding path arithmetic with flat readdir of $XDG_DATA_HOME/yog/workspaces/ (leaf = name; foreign + replay classification unchanged) and the claimant join (ball bound iff claimant = a local workspace name). Rework the §3.5 join-state table and roster grouping (balls group under their bound workspace); migrate watch root BallRoot -> NamesRoot (flat, §7.1). Delete the mirrored-path code. Files: src/binding/mod.rs, src/projects/balls.rs, src/watch/mod.rs, src/app/mod.rs.