+++
title = "beat: the name column is a column — assert the title's left edge is unmoved by attention"
created = 1786511517
updated = 1786512142
claimant = "Pennant"
parent = "bl-b9e3"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-85d0"
on = "claim"
+++
Subtask of bl-b9e3. Blocked on the paint subtask.

The operator's complaint was **alignment**, not the glyph — *"it makes the list
not align"*. The paint subtask can land, look right in one screenshot, and
regress the next time anything is added to the row's prefix group. Nothing in
the suite asserts the name column is a column.

**Write the beat.** Two conversation rows in the same list that differ **only**
in attention (one flagged, one not) render their titles at the **same x**. Assert
it on geometry, not on the painted string — the existing panel-geometry
regression under `src/acceptance/geometry.rs` is the pattern and the seat
(DESIGN §12's module map calls it *"the §11 panel-geometry regression"*), and
`src/acceptance/tabs.rs` already precedes you in asserting a seat by geometry
*"since the auth banner paints the same text"*.

The assertion is about the *prefix group*, so state it that way where you can:
the flag now sits in the trailing right-pinned group, and the thing being
protected is that the title's left edge is a function of the row's fixed prefix
alone. A beat that only compares two specific fixtures passes vacuously the day
someone adds a conditional to the prefix and forgets the fixture — see
`memory: harness-decay-not-code-decay`. Prefer an assertion that fails when a
*new* conditional prefix element appears, if one can be written without a knob;
if it cannot, say so in the ball's journal and take the two-fixture version.

**Also check the drive beats.** `docs/drive-logs/` records S3/S4/S6 beats
observing a row `carrying ⚑1` beside a state badge (e.g.
`2026-07-26-s3-s4-s6-wire-green.md:191`). If any live beat under `scripts/drive/`
matches on the literal `⚑1` or on the flag's pixel position, it breaks when the
count and the seat change — find them and update them to the new spelling. The
drive-logs themselves are history: do not edit them.

**Done when:** the new beat fails against the pre-paint tree (verify by
stashing) and passes after, `make test` is green, and coverage is 100%.