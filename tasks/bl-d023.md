+++
title = "subagent forking runaway: each child under energize arrives bearing the parent's last instruction ('create a new subagent') as its own goal — diagnose the descent tree, fix the goal composition"
created = 1785731736
updated = 1785731745
claimant = "fork-untangler"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator report 2026-08-03, verbatim: 'there's something hilariously broken in the way that the subagent model forking is working. Look at the tree under energize (have an agent do it). I stopped it, but I think it was going to make a session set of infinite depth, because each one showed up with the last instruction, which was to create a new subagent.'

Symptom: under agent energize, each descent child was created carrying the parent's LAST instruction as (or in) its own goal — and since that instruction was 'create a new subagent', every generation re-executed it: unbounded recursive dispatch until the operator stopped it.

The work: read the actual tree under energize (agents/* refs in the workspace, each child's dispatch commit: goal.md, name, argv in ops logs, and each child's transcript first messages). Establish (a) the literal goal text each generation received, (b) which component composed that goal — the lernie dispatch tool builtin (child dispatch is lernie's, yog is not at a child's compose), lernie's context assembly (does the assembled context leak the parent's tail instruction as the child's first user message?), or a yog-side goal passed at the root — and (c) why the parent's last instruction became the child's marching orders. Then fix at the composition fault, where it lives (likely lernie — file/claim/close a ball in that store, record release ordering here as prose).

Distinct question to answer while in there, not to over-build: whether any depth/spawn guard should exist is bl-56d5's territory (spend ceiling gates spawns) — do not invent a depth cap here unless the diagnosis shows the recursion was NOT a goal-composition bug. One bug, one fix.

NOTE: another agent (bl-8068, message-medic) is concurrently reading energize's workspace read-only for the messaging failure — coexist; both reads are read-only on the workspace.