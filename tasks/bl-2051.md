+++
title = "fold the bl family to Action::Ball(balls::Verb) — the roster split bl-92d3 designed"
created = 1788329698
updated = 1788329698
priority = 1
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
bl-92d3 (closed) did the design; this ball executes it. Its conclusion, carried forward:

The boundary rosters sit against the 300 wall (census then: action.rs 295, reply.rs 297 — re-run 'make line-cap LINE_CAP=249' for today's numbers). An enum has no file seam, but DESIGN sec 12's own row names the escape hatch: 'a family whose members every layer beneath already reads as a pair folds to ONE variant over that family's own Verb'. Four families have taken it (monitor, fleet, the routing leg, the sec 3.8 fan). The one left untaken is the bl family — Close, Assign, Release, Create, Update — and every layer beneath already reads it as a family: codec/balls.rs, line/balls.rs, reply/balls.rs, answer/balls.rs. Folding those five to Action::Ball(balls::Verb) frees roughly 25 lines in the roster and removes nothing.

Scope discipline from bl-92d3, verbatim in spirit: this is a shared-surface refactor touching the codec, the line reader and writer, both address tables and the dispatch match — do it ALONE, no other boundary work in flight beside it.

Reply's hatch is thinner — its receipts do not group the same way — so MEASURE before assuming the same move works there. If Reply has no honest fold, say so in the close note rather than forcing one; leaving reply.rs near the wall with a recorded reason beats a fake seam.

Check whether this fold is a wire-visible change: if the serialization of the five verbs changes shape, that is a PROTOCOL bump and the doc must say so; if the fold is roster-internal with the wire text unchanged, state that explicitly in the close note.