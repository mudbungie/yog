+++
title = "yog links lernie =0.0.1 but 0.0.2 is the published latest — bump the registry pin so W11 consumes upstream's current release"
created = 1785201112
updated = 1785201114
claimant = "crushes"
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
yog's `Cargo.toml` pins `lernie = "=0.0.1"` (DESIGN §16.7 W11). Upstream published 0.0.2 on 2026-07-27; 0.0.1 is now stale by ~17 changes including the 2.11 lost-wakeup fix and the dispatch id/workspace guard.

Bump the pin to `=0.0.2` and relock. Parity check holds: the crates.io index shows lernie 0.0.2 still declaring `brazen =0.0.4`, the exact version yog pins, so exactly ONE brazen still resolves (§16.7 'skew death is structural'). Update the Cargo.toml comment that names 0.0.1 as the release carrying `Fx::adapter_target`.

yog's consumption surface is two lines: `lernie::run` (src/multiplex.rs:76) and `lernie::cmd::{self, Fx, Outcome, prelude}` (src/multiplex/lernie.rs:30).