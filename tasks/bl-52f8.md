+++
title = "decomposition sweep: 102 of 279 source files ride at or above 200 lines, 52 within 50 lines of the hard cap — split along real seams to a 200-line aspiration"
created = 1785460174
updated = 1785645101
claimant = "Umber"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
tags = ["refactor"]

[[blockers]]
id = "bl-f1ee"
on = "close"

[[blockers]]
id = "bl-7336"
on = "close"

[[blockers]]
id = "bl-cb3e"
on = "close"

[[blockers]]
id = "bl-efe4"
on = "close"

[[blockers]]
id = "bl-2ccc"
on = "close"

[[blockers]]
id = "bl-3d12"
on = "close"

[[blockers]]
id = "bl-c384"
on = "close"

[[blockers]]
id = "bl-d511"
on = "close"

[[blockers]]
id = "bl-8ddb"
on = "close"

[[blockers]]
id = "bl-d266"
on = "close"

[[blockers]]
id = "bl-197c"
on = "close"
+++
Operator-raised, 2026-07-30: *"a decomposition sweep across the whole repo targeting a 200 line aspirational cap. Things shouldn't ride near the line; the point is good segmentation."*

## The measurement (2026-07-30, `git ls-files '*.rs' | xargs wc -l`)

279 tracked `.rs` files. `make line-cap` passes — nothing exceeds 300 — but the distribution shows the cap is being *used as a target* rather than a limit:

| band | files |
|---|---|
| **300 (exactly at the cap)** | **1** — `src/app/mod.rs` |
| **250–299** | **51** |
| **200–249** | **50** |
| 150–199 | 42 |
| <150 | 135 |

**102 files (37% of the tree) sit at or above 200 lines; 52 sit within 50 lines of a hard gate.** That is the complaint: files ride the line, so any ball that touches one inherits a split obligation it did not sign up for, and the 300-line gate becomes a tripwire rather than a backstop. bl-2194 hit this live — its clean-rest test had to go into `src/app/tests/derive.rs` because `src/app/tests/focus.rs` was at 294/300 and splitting it was outside that ball's scope.

The ≥250 band by subsystem:

    shell 8 · git_tree 7 · app 7 · start 3 · opslog 3 · nav 3 · fs_watcher 3
    config_edit 3 · ui_state 2 · transcript 2 · xdg/world/tests/steps_view/
    multiplex/model_pick/inspector/files_view/delete/cli_outbound/attention 1 each

## The deliverable

Bring the tree to a **200-line aspirational target** by splitting along real seams. Explicitly:

- **200 is aspirational, not a new gate.** The hard cap stays 300. Do not add a second enforced threshold — a gate at 200 is just the cap moved, and AGENTS.md already argues that case against a 250 gate verbatim: *"A warning that fires on a fifth of the tree is noise, and a gate there is just the cap moved to 250."*
- **Mandatory core: every file ≥250 comes down** (52 files). That band is where the tripwire actually bites.
- **Opportunistic: the 200–249 band** (50 files) — take the ones with an honest seam; leave the ones where splitting would invent a boundary that isn't there.

## The binding constraint — segmentation, not line-shaving

AGENTS.md, verbatim: *"Over the cap? **Split along a real seam** and add the row to DESIGN §12; **never shave lines to duck the limit.**"*

A split that is not a real seam is worse than the long file: it adds a module boundary, an import, and a name, and buys nothing. The doctrine that governs the judgment, from the global AGENTS.md:

- *"Minimize interface. Make it deep and narrow enough that the consumer needs nothing behind it. Components meet only at the interface, never pairwise; the cost is coupling, not count."*
- *"Minimalism, subtraction in design. Build less. Code is context and context is inertia."*
- *"Single source of truth. Every fact has one authoritative home."*

So the sweep's first question on each file is **not** "where do I cut this in half" but **"why is this file this long?"** Three honest answers, three different actions:

1. **It does two things** → split at the seam; that is the target case.
2. **It does one thing thoroughly** (an exhaustive match, a big table, a codec) → possibly leave it, and say so. A 260-line total match arm-per-variant has no seam.
3. **It is long because of duplication or dead prose** → **delete, don't move.** Subtraction beats relocation. Report lines removed separately from lines relocated.

Test files count. Inline `mod tests` and `src/**/tests/*.rs` are under the same cap and the same reasoning — a test module that has grown to 290 lines usually mirrors a production file that grew two responsibilities.

## Definition of done

**Amended 2026-07-30** after the seam analysis below. The original read *"No `.rs` file ≥250 lines"* — that is **not honestly achievable**, and demanding it would force exactly the invented boundaries this ball forbids. AGENTS.md already licenses the exception verbatim: *"≥250 is a **design-time projection rule** — it fires on the author about to add to a file, not on the file's existing state."*

- No `.rs` file ≥250 lines **without a recorded justification**, and a written justification for every file left in 200–249.
- Each new module has a **DESIGN §12 row** (path, budget, one-clause responsibility). See the sequencing warning below.
- `make check` green: fmt → lint (clippy + line-cap + rules-audit + cargo-deny) → **100% coverage**.
- **No behavior change.** This is a pure restructuring; if a split reveals a bug, file it separately rather than fixing it inline.
- Report: files split, seam chosen per file, lines *deleted* vs *relocated*, and the justified leave-alone list.

## Seam analysis (pedantic-sweep, 2026-07-30) — read-only, complete for all 52 mandatory files

**Scope of this analysis: ONE subsystem of five. Do not read it as a tree-wide verdict.** An earlier interim report claimed tree-wide tallies (28 SPLIT / 16 LEAVE / 8 DELETE); the analyst **retracted them as fabricated** — four of the five wave analyses never returned. Only `git_tree` is real. Everything recorded here is either that one result or a fact verified by direct command.

### Composition of the 52 mandatory files — this reframes the work

- **25 are pure production files** (no tests at all) — the genuine seam candidates.
- **21 are dedicated test files.**
- **6 are production plus a large inline `mod tests`** (e.g. `src/git_tree/fd_probe.rs` = 131 lines of code + 165 of tests).

**Roughly half the mandatory band is test bulk driven by the 100% coverage floor, not by two responsibilities.** A sweep that treats all 52 as two-headed modules will be wrong about half of them.

### git_tree (7 files): 2 SPLIT, 5 LEAVE, 0 DELETE — verified

- **SPLIT** `src/git_tree/tests/fixture.rs` (272) — mixes git-shape builders with four methods writing plain files git never sees (`steps/`, `inbox/`); those four move to `disk_fixture.rs`. Directory precedent exists: `tests/git.rs` and `tests/config_fixture.rs` were carved off the same struct.
- **SPLIT** `src/git_tree/mod.rs` (270) — module wiring + platform `cfg` versus seven inert view-model types; the types move to `model.rs`, re-exported so the public surface stays byte-identical.
- **LEAVE** `fd_probe.rs`, `lsof.rs`, `terminal.rs`, `marks.rs`, `tests/repo.rs` — a `/proc` scanner with nine failure exits, an `lsof -F` codec, the §4.4 settled-tail classifier, a closed mark enum, 16 tests of one function. No duplication found between `fixture.rs` and `repo.rs`; that split is already clean.

**A 5-of-7 LEAVE rate in the first subsystem analysed is what forced the DoD amendment above.** If git_tree is at all representative, a meaningful fraction of the 52 cannot be honestly split. The true fraction is **unknown** — four waves remain unanalysed and must be re-run.

### The biggest win is subtraction, not relocation

1. **A headless egui paint harness is copy-pasted across 8 files** — `collect_text` + `screen`/`input`. The self-test `collect_text_descends_shape_vec_and_ignores_non_text` appears **verbatim in 4 files**. The 100% floor *multiplies* the cost: every copy needs its own coverage. One home in `src/test_support/` removes **~300–360 lines repo-wide**.
2. **`config_edit/brazen/mod.rs` and `config_edit/lernie_global/mod.rs` are near-duplicate editors** — 5 identically-named methods, 3 byte-identical test names. A shared `Draft` type removes ~55 code + ~190 test lines and drops **both** files under 250 **with no split at all**.

Total: roughly **500–600 lines deleted** against ~30 new modules relocated. Subtraction is the headline, per doctrine.

### Three hazards that would have bitten during execution

1. **`src/shell/state.rs` is a forbidden filename.** `rules/locks-outside-state.yml:28` carries `ignores: ["**/state.rs"]` — a new shell module with that name would be permanently and silently exempted from the lock-chokepoint rule.
2. **Four shell extractions are ui-free and will look like they belong in covered modules. They do not.** `src/shell/*` is tarpaulin-excluded; relocating them converts headless-unreachable orchestration into 100%-coverage debt. **Every shell seam must stay under `src/shell/`.**
3. **§12 is already stale and incomplete, independent of this sweep.** 25 existing `src/*.rs` files have **no §12 row at all** — including `src/app/focus.rs` (291 lines) and three files in the mandatory band. Budgets have drifted badly: `src/xdg/mod.rs` budgeted 150 / actual 288; `src/opslog/live.rs` budgeted 160 / actual 252. **This is bl-43cd's territory, not this ball's — flagged there.**

### Correction to this ball's sequencing assumption

**§12 is not one-row-per-module.** It uses compound brace-list rows (47 rows total for the whole tree), so the sweep *edits existing rows* rather than adding 50–100 new ones — a materially cheaper rebase than this ball originally assumed.

## SEQUENCING WARNING — collides with bl-43cd

**bl-43cd is the DESIGN.md rewrite (a fable is taking it).** Its investigation found §12's module table is the single hottest structure in the file — 37 of 68 all-time commits, 46 diff hunks inside the table — and recommended shrinking its rows.

This sweep **adds a §12 row per new module**, potentially 50–100 of them, to the exact table bl-43cd is rewriting. Markdown tables are one line per row, so the two collide by construction.

**These two must not run concurrently.** One of:
- land this sweep first, and let bl-43cd rewrite a §12 that already reflects the new module set (recommended — bl-43cd then rewrites the final shape once, rather than a shape about to change); or
- land bl-43cd first, and have this sweep append rows to whatever structure the rewrite leaves.

The operator sequences this. Do not start against a §12 another agent is mid-rewrite on.

## Not in scope

- Raising or lowering the 300-line hard cap.
- Adding a 200-line gate or warning (see above).
- Any behavior change, dependency change, or API redesign beyond what a seam split mechanically requires.
- `docs/*.md` — exempt from the cap and owned by bl-43cd.

## Doctrine follow-up to consider

AGENTS.md currently names 250 as the pre-split projection band: *"`make line-cap LINE_CAP=249` lists the ≥250 pre-split band — run that before you extend a module, not after."* If 200 becomes the aspiration, that line should say 199/200. Amend it as part of this work so the doc and the practice agree.
---

# Outcome (2026-08-01, Umber) — the record the DoD requires

Delivered on `work/bl-52f8`, which carried 11 prior sub-ball commits (bl-f1ee,
bl-7336, bl-cb3e, bl-efe4, bl-2ccc, bl-3d12, bl-c384, bl-d511, bl-8ddb,
bl-d266, bl-197c). **That work was kept, not discarded** — it was verified by
build + full test suite after merging current `main` into the branch, and this
pass finished the residue on top of it. `main` had moved twice since the branch
was cut (34be74b DESIGN rewrite, 95b679e integration-test consolidation); the
merge produced one real conflict of substance — `tests/glyph_coverage.rs` had
been renamed to `tests/integration/glyph_coverage.rs` on main while the branch
split `scan.rs` out of it — resolved by re-rooting the split under
`tests/integration/glyph_coverage/`.

## Result

| band | before (2026-07-30) | after |
|---|---|---|
| ≥300 | 1 | **0** |
| 250–299 | 51 | **1** |
| 200–249 | 50 | 69 |
| total `.rs` | 279 | 326 |

**51 of the 52 mandatory files came down.** One remains, justified below.

## What this pass added on top of the sub-balls

Ten files were still ≥250 after the merge. Nine were split; one is justified.

| file | before | seam chosen | after |
|---|---|---|---|
| `src/git_tree/fd_probe.rs` | 296 | implementation vs its test corpus — `X.rs` + `X/tests.rs`, the repo's own named seam (39 pre-existing instances on `main`) | 133 + 164 |
| `src/git_tree/lsof.rs` | 292 | same | 176 + 117 |
| `src/git_tree/terminal.rs` | 283 | same | 138 + 146 |
| `src/git_tree/marks.rs` | 274 | same | 127 + 147 |
| `src/opslog/live.rs` | 252 | same (and matches `opslog/line.rs` + `line/tests.rs`, landed by bl-d511) | 135 + 118 |
| `src/world/marks/tests.rs` | 278 | the *runner* seam: `plan`/`read`/`apply` against a canned in-process `ConfRunner`, vs the real `BlConf` subprocess (argv recorder, binary resolution) → `tests/{mod,runner}.rs` | 238 + 51 |
| `src/git_tree/tests/repo.rs` | 267 | `from_repo`'s **skeleton** (trunk, agents, marks, descent) vs its **step-content projection** (§4.4 stream events → `streaming_text`, tool calls) → `tests/steps.rs` | 176 + 100 |
| `src/start/tests/exec.rs` | 264 | the **`bl`-facing** executors (`create`, `claim`, cross-check) vs the **workspace-and-name** executors (`lernie new` ensure, `on_mint`, `resolve_worktree`) → `tests/ensure.rs` | 145 + 126 |
| `src/fs_watcher/drift_tests.rs` | 254 | the **observation** seam the file's own doc already draws: loss announcements arriving on the event channel (queue overflow, WD exhaustion, a replaced root inode, the desync lead) vs git **ref delivery** to a live watcher (`packed-refs`, write-then-rename coalescing) → `tests/drift/{mod,loss,refs}.rs` | 74 + 125 + 78 |

### One correction to the prior seam analysis

The verified git_tree wave recorded **LEAVE** for `fd_probe`, `lsof`,
`terminal`, `marks` on the grounds that each does one thing thoroughly. That is
true **of the code** and is why none of them was cut internally. But the
*file* holds two artifacts — the implementation and its test corpus — and
`X.rs` + `X/tests.rs` is a seam this repo already names in 39 places on `main`.
Extracting the inline `mod tests` is therefore neither an invented boundary nor
line-shaving: nothing was reworded and nothing moved between responsibilities.
The fifth LEAVE, `git_tree/tests/repo.rs`, did have an internal seam after all
(skeleton vs step content) and was split.

### One structural note

`src/fs_watcher/drift_tests/` was re-rooted to `src/fs_watcher/tests/drift/`
because `rules/no-assert-outside-tests.yml` (and `no-lint-suppression.yml`,
`locks-outside-state.yml`) exempt test code by the path globs `**/tests/**`,
`**/tests.rs`, `**/*tests.rs` — a `drift_tests/` **directory** matches none of
them. Re-rooting was preferred over adding a sixth glob to three rule files:
**the enforcement config was not touched**, and no new naming convention was
introduced.

## Mandatory band: the one file left ≥250, with its justification

**`src/transcript/rows.rs` — 251 lines. LEAVE.**

Answer to "why is this file this long?": *it does one thing thoroughly.* It is
the §11 one-line row projection — an exhaustive `match` with an arm per
`EntryKind` (6) and per `Block` (3), each arm carrying the §11 doctrine comment
that explains its prefix, class and tone. There is no second responsibility to
carve off:

- **Types vs functions** is the only clean cut available, and it is an invented
  boundary: `Row` carries `class` and `tone`, and the projection must set both,
  so the "types" module and the "projection" module would be one fact split
  across two files — the drift the single-source-of-truth rule forbids.
- **The expansion policy** (`AutoExpand`, `RowClass`, `expanded_for` — `auto
  XOR overridden`) reads as separable doctrine, but `push_entry` assigns
  `RowClass` on every arm, so the two meet everywhere rather than at an
  interface.
- **`split()` + `PREVIEW_CAP`** (the preview/foldable-body rule) is genuinely
  self-contained, and genuinely 30 lines. A module for it buys a name and an
  import and nothing else.

At 251 it is one line into the band. Shaving two lines to duck a threshold is
exactly the move AGENTS.md forbids verbatim ("never shave lines to duck the
limit"), so it stays and is recorded here instead — which is what the amended
DoD provides for.

## Aspirational band (200–249): 69 files left, with justification

The 200-line target is an aspiration, and this band is where the honest answer
to "why is this file this long?" is most often *(2) it does one thing
thoroughly*. Every file below was considered as part of the sweep and left
deliberately. Grouped by the reason:

**(a) Test corpora already sitting at their own seam — 31 files.** Each is
already the sibling of exactly one production module, or one concern-file
inside a subsystem's `tests/` directory, and is long because of the 100%
coverage floor rather than a second responsibility. Splitting further would
partition one module's test corpus by nothing but line count.

`src/actions/tests.rs`(218) `src/app/balls/tests/internal.rs`(230)
`src/app/tests/drift.rs`(206) `src/app/tests/focus.rs`(224)
`src/cli_outbound/tests/detach.rs`(221) `src/cli_outbound/tests/run.rs`(243)
`src/config_edit/branch/edit/tests.rs`(230)
`src/config_edit/brazen/tests/mod.rs`(208)
`src/config_edit/lernie_global/tests.rs`(220)
`src/git_tree/tests/fixture.rs`(200) `src/inspector/tests.rs`(229)
`src/keymap/tests.rs`(221) `src/login/tests.rs`(219)
`src/model_pick/tests/grammar.rs`(220) `src/model_pick/tests/query.rs`(203)
`src/names/tests.rs`(248) `src/nav/menu/tests/mod.rs`(208)
`src/projects/join/tests.rs`(218) `src/start/tests/run.rs`(219)
`src/steps_view/tests/render.rs`(204) `src/test_support.rs`(218)
`src/theme/tests.rs`(227) `src/transcript/tests/rows.rs`(230)
`src/transcript/tests/vm.rs`(234) `src/ui_state/tests.rs`(202)
`src/watch/tests.rs`(227) `src/world/marks/tests/mod.rs`(238)
`src/world/tests.rs`(213) `src/xdg/tests.rs`(234)
`tests/integration/support/recorder.rs`(207) `tests/multiplex_bl.rs`(246)

**(b) One responsibility done exhaustively — tables, closed match sets, single
codecs — 33 files.** These are the case the ball explicitly names: "a 260-line
total match arm-per-variant has no seam."

`src/actions/verbs.rs`(248) `src/app/balls.rs`(224) `src/app/balls/starts.rs`(220)
`src/app/derive.rs`(225) `src/app/mod.rs`(238) `src/budgets/mod.rs`(230)
`src/bz_host.rs`(226) `src/cli_outbound/mod.rs`(205)
`src/cli_outbound/streamed.rs`(203) `src/config_edit/branch/edit.rs`(244)
`src/config_edit/brazen/mod.rs`(245) `src/config_edit/lernie_global/mod.rs`(227)
`src/config_edit/pipeline.rs`(230) `src/files_view/render.rs`(224)
`src/git_tree/cmd.rs`(232) `src/git_tree/descent.rs`(219)
`src/git_tree/lock_probe.rs`(209) `src/git_tree/probe_cache.rs`(201)
`src/git_tree/streaming.rs`(241) `src/inboxview/mod.rs`(232)
`src/keymap/mod.rs`(219) `src/main.rs`(207) `src/names/mod.rs`(214)
`src/nav/menu.rs`(215) `src/opslog/mod.rs`(217) `src/projects/join.rs`(232)
`src/start/exec.rs`(230) `src/start/mod.rs`(239) `src/steps_view/render.rs`(210)
`src/theme/mod.rs`(219) `src/watch/mod.rs`(226) `src/world/marks.rs`(244)
`src/xdg/mod.rs`(240)

**(c) Shell orchestration — 5 files.** `src/shell/*` is tarpaulin-excluded, and
every seam must stay under it (hazard 2). Each is already the thin egui face of
one §-owned surface; the shell wave (bl-efe4) took the seven files that were
≥250 and these are what remains.

`src/shell/acceptance/mod.rs`(242) `src/shell/config_edit/mod.rs`(215)
`src/shell/input_bar.rs`(212) `src/shell/inspector.rs`(227)
`src/shell/workspace.rs`(202)

## Deleted vs relocated

Subtraction landed where the analysis promised it:

- **The headless egui paint harness**: `fn collect_text` existed in **8 files**
  on `main` (`files_view/render.rs`, `inboxview/render.rs`, `inspector/tests.rs`,
  `jsonview/tests.rs`, `shell/acceptance/mod.rs`, `steps_view/tests/render.rs`,
  `theme/tests.rs`, `transcript/tests/render.rs`) and exists in **1** now
  (`src/paint_probe.rs`). Verified by `git grep -c "fn collect_text"` against
  both refs.
- **`config_edit`**: `src/config_edit/draft.rs` (117) is the shared `Draft`; the
  two near-duplicate editors are now 245 and 227, both under 250 with no split.

Net measurement, `main` → `work/bl-52f8`: **45174 → 45537 total `.rs` lines
(+363) across 305 → 326 files.** That net is honest and worth stating plainly:
the deduplication removed several hundred lines of real content, and the 21 net
new module files each cost ~17 lines of module doc, imports and a `mod` decl.
**The sweep bought segmentation, not a smaller tree** — which is what the ball
asked for. Nothing was line-shaved; every reduction is a moved seam or a
deleted duplicate.

## DESIGN §12 and the doctrine follow-up

- §12 was edited **in the post-rewrite (bl-43cd) structure**, as compound
  brace-list rows kept sorted by module path: 24 rows edited or added covering
  every new production module (`app/view`, `app/focus`, `attention/roster`,
  `cli_outbound/chunk`, `config_edit/draft`, `fs_watcher/fold`,
  `git_tree/{fd_probe,terminal,model}`, `model_pick/grammar/roles`,
  `multiplex/bl`, `nav/convs/row`, `paint_probe`,
  `shell/{bootstrap,dispatch,ram,top_bar,start_rows,config_edit/branch_pane}`,
  `shell/model_pick/ram`, `start/identity`, `ui_state/fields`, `xdg/brazen`,
  `tests/integration/glyph_coverage/scan`), with budgets refreshed to actuals.
- The §12 preamble now carries the **test-sibling principle** — "a test module
  is covered by its production module's row and never earns one of its own" —
  so 21 new test modules did not become 21 new rows. That is a principle, not a
  feature, and it also describes what the table already did.
- The §12 preamble's projection threshold moved 250 → 200.
- **AGENTS.md**: both the cap bullet ("anything projected ≥200 is pre-split at
  design time") and the `make line-cap` paragraph (`LINE_CAP=199` lists the
  ≥200 pre-split band; 74 of 339 source files sit there) now say 200. The
  Makefile's `line-cap` comment — the target that is "the ONE definition of the
  cap" — was updated to match, so the two cannot drift.
- Hazard 1 was respected: **no file named `state.rs` was created under
  `src/shell/`**.

## Gate

`make check` run in full before close: `fmt-check` clean, `lint` clean
(line-cap 339 files all within 300 · clippy `-D warnings` · rules-audit src
clean + fixtures flagged · cargo-deny advisories/bans/licenses/sources ok),
`coverage` **100.00%, 5352/5352 lines**. No behavior change; no bug found, so
none filed.