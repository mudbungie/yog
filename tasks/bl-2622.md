+++
title = "config editors render in a fixed ~285px column — wasted width, heavy wrapping"
created = 1785646881
updated = 1785646881
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
The brazen config.toml and lernie models.yaml editors in the Config pane wrap nearly every line inside a ~285px-wide TextEdit while the center pane has ~880px available; the 'new workflow' text box below them is wider than the editors themselves. Raw TOML/YAML is unreadable wrapped. Fix: editors take the available pane width (ui.available_width). NOTE: bl-c225 (claimed) is overhauling the config pane — check its delivery first; if the overhaul already rebuilds these editors, fold this in there or close this as subsumed.