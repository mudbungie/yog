+++
title = "config editors render in a fixed ~285px column — wasted width, heavy wrapping"
created = 1785646881
updated = 1785647713
claimant = "Rowan"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
RE-VERIFIED after bl-c225's config overhaul landed (main 18ec0c4): the structured controls are wide and fine, but the demoted raw editors ('raw config.toml' collapsible, and the lernie models.yaml raw view if it survives elsewhere) still render in a fixed ~285px TextEdit that wraps nearly every TOML line while the pane has 2-3x that width available. Fix: raw editors take the available pane width (ui.available_width / desired_width(f32::INFINITY)). Scope is now just the raw views — the c225 field controls are not affected.