+++
title = "boundary wave 2: the config family joins the Action enum (§9 applies, marks knob, model pick), per DESIGN §8.5's staging declaration"
created = 1785650504
updated = 1785650504
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
DESIGN §8.5 (landed by bl-8aab) declares the staging: the §9 config editors, the §16.3 marks knob and the model-pick commit are actions by the boundary taxonomy but not yet variants — each funnels through its own single chokepoint (Editor::apply, edit::drive, world::marks::apply) and logs to ops.jsonl, so no gesture has two implementations meanwhile. This ball lands them as variants: config applies carrying the full staged text, SetMarks{mode}, PickModel{workspace, role, provider, model}; codec spellings + round-trip tests; the consumer asks brazen for provider rows (retiring the 'empty table gates nothing' headless interim §8.5 records). Login stays as-is: its headless spelling already exists verbatim as 'yog bz --login' (§16.7 W10). Verify §8.5 on main before editing — it is the ruling this implements.