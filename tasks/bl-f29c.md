+++
title = "an unreachable entry paints on its own rows, never the shell; pane facts stay local, world facts land at the host (REMOTE §8.2, §7)"
created = 1787544620
updated = 1787544620
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"

[[blockers]]
id = "bl-670c"
on = "claim"
+++
REMOTE §8.2 + §7 as amended (bl-aaec): the paint and the fact routing that
make multi-server legible at the glass.

Needs bl-670c (the per-channel threads land the refusal on the slice; this
ball paints it).

What to build:

- **Per-entry refusal paint.** An unreachable channel paints its sentence on
  its own workspace rows and tabs — a row state ('unreachable: <the
  channel's own sentence>'), the §3.5 join-state shape one noun up — never
  the whole shell. `shell::refusal` keeps its one caller (the loopback
  wire, bl-dc14: the one wire the window cannot exist without).
- **World facts ride the workspace's channel** (REMOTE §7 as amended):
  verify seen/pins for a workspace held elsewhere land in the HOST's world
  document (the acts route via bl-670c's poster; the standing reads land
  per slice), and that attention (DESIGN §6) ranks across channels over
  each host's own facts — no cross-host clock or ordering is promised
  (REMOTE §8.2's non-solves).
- **Pane facts stay local.** The pane document is written only through the
  loopback channel — one glass, one home (REMOTE §7). Sweep every writer of
  pane keys; add the regression that a pane gesture never crosses an entry
  channel.
- Paint assertions via `crate::paint_probe` only (rules-audit).

Line cap: whichever tab/roster modules gain the unreachable state — run
`make line-cap LINE_CAP=199` before extending, split along the row-state
seam if projected past 200.