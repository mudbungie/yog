+++
title = "REMOTE §9.2 — Reply gains a strict decode side, with encode-decode round-trip tests over the whole Reply surface"
created = 1786683972
updated = 1786685491
claimant = "Luffing"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"

[[blockers]]
id = "bl-6233"
on = "claim"
+++
docs/REMOTE.md §9 step 2 of the client/server split (bl-b9a2). The reply codec today is encode-only (one documented exception: the prepare reply re-enters as Action::Prompt). A thin client must decode every Reply variant. The hand-codec discipline extends — serde derive stays a non-dependency (REMOTE.md §3; the ui_state stance). Precedent for strictness: the gesture codec's strict decode. Deliverable: decode for all Reply variants + round-trip tests over the full surface. Verify current codec file/type names against the tree before editing; this body drifts. Boundary-surface work: serialize with other boundary balls (the needs-edge on the §9.1 ball enforces order).