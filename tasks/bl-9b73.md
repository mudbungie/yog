+++
title = "transcript density: every message/tool-use renders one collapsible line; user-facing responses auto-expand, all else auto-contracts; automatics are config knobs"
created = 1785287537
updated = 1785287537
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator, live UX session 2026-07-28, verbatim:

> the chat is very inefficient in vertical space. each message should be one line, tool use, one line, etc, such that you can expand or contract them. actual responses to the user should automatically expand fully, everything else should automatically contract. these automatics should be config knobs.

So: every transcript row (assistant text, tool use, tool result, system) collapses to ONE line with expand/contract; the auto-state on arrival is: actual responses to the user = fully expanded, everything else = contracted; both defaults are configuration knobs. Check DESIGN §11 transcript spec and amend it; expansion state per row is instance-local RAM (§5.3) unless design says otherwise; the KNOBS (auto-expand classes) need a durable home — check where config-of-the-UI belongs per DESIGN (severability: policy in a knob, not hardcoded). Respect glyph doctrine (§11) for the fold affordance (▶/▼ folds pass on convention).