+++
title = "typed target binding at fire: pass the project worktree as lernie's working-directory parameter; retire the goal-prose operative channel"
created = 1785823807
updated = 1785823807
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
tags = ["implementation"]
+++
Source: bl-2b8c ruling (VISION §4.10; DESIGN §3.3 amended). Upstream mechanism: lernie bl-d0b4 (creation-time working-directory parameter seeding the cwd mark, plus per-step observed-OID provenance). Cross-repo gate: do not claim until a lernie release carrying bl-d0b4 is published; the first act is the exact pin bump.

## Scope

1. Bump the lernie pin to the release carrying bl-d0b4.
2. The fire passes the binding typed, per payload rung: ball rung passes the claim-derived work/<id> worktree path (recomputed, never stored — DESIGN §3.3); path rung passes the directory box's value; bare rung passes nothing. One channel.
3. The composed goal keeps the ball title and body (payload, model-facing content) and drops the worktree-location preamble ("The project repository checkout for this work is…") — location stops being prose. The `Ball <id>:` headline stays: it is the §3.2 conversation→ball join, not a location channel.
4. Retire the per-target current_dir on the initial spawn (DESIGN §3.3's recorded misleading redundancy): the binding is the one operative channel; current_dir no longer varies by target.
5. Validation surfaces unchanged where they exist (§3.4 field refusal pre-spawn); a binding whose directory vanishes mid-flight renders as lernie's own loud tool decline — yog invents no second check.
6. Update DESIGN §3.3's interim-channel paragraphs to shipped-state.

Blocks bl-aa8b: frozen project-instruction discovery anchors at the typed target, so it waits for this channel.