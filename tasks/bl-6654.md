+++
title = "BLOCKED on the lernie 0.0.7 publish (lernie bl-d0b4 is unreleased) — typed target binding at fire: pass the project worktree as lernie's working-directory parameter; retire the goal-prose operative channel"
created = 1785823807
updated = 1786513164
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
tags = ["implementation"]
+++
Source: bl-2b8c ruling (VISION §4.10; DESIGN §3.3 amended). Upstream mechanism: lernie bl-d0b4 (creation-time working-directory parameter seeding the cwd mark, plus per-step observed-OID provenance). Cross-repo gate: do not claim until a lernie release carrying bl-d0b4 is published; the first act is the exact pin bump.

## Scope

1. Bump the lernie pin to the release carrying bl-d0b4.
2. The fire passes the binding typed, per payload rung: ball rung passes the claim-derived work/<id> worktree path (recomputed, never stored — DESIGN §3.3); path rung passes the directory box's value; bare rung passes nothing. One channel.
3. The composed goal keeps the ball title and body (payload, model-facing content) and drops the worktree-location preamble ("The project repository checkout for this work is…") — location stops being prose. The `Ball <id>:` headline stays: it is the §3.2 conversation→ball join, not a location channel.
4. Retire the per-target current_dir on the initial spawn (DESIGN §3.3's recorded misleading redundancy): the binding is the one operative channel; current_dir no longer varies by target.
5. Validation surfaces unchanged where they exist (§3.4 field refusal pre-spawn); a binding whose directory vanishes mid-flight renders as lernie's own loud tool decline — yog invents no second check.
6. Update DESIGN §3.3's interim-channel paragraphs to shipped-state.

Blocks bl-aa8b: frozen project-instruction discovery anchors at the typed target, so it waits for this channel.

---

GATE UNSATISFIED at 2026-08-11 — verified, not claimed (Girder).

The ball's scope 1 ("Bump the lernie pin to the release carrying bl-d0b4") has no release to bump to. Evidence:

- crates.io index for `lernie` lists 0.0.0 … 0.0.6 and nothing more; 0.0.6 is what yog pins today (`Cargo.toml`: `lernie = "=0.0.6"`, lockfile checksum a2428cb2…).
- The vendored published source `~/.cargo/registry/src/index.crates.io-*/lernie-0.0.6/src/cmd/prompt.rs` has four `#[arg]`s and no `cwd` among them; `grep -rn cwd src/cmd/` in that tree is empty. `--cwd` does not exist in any published lernie.
- bl-d0b4 IS written: `~/dev/lernie` commit 5cc594c "seed the agent's working directory at spawn: --cwd on lernie prompt and lernie dispatch [bl-d0b4]", adding `--cwd <PATH>` to both `lernie prompt` and `lernie dispatch` (validated through `workspace::cwd::resolve` before any branch/ref exists; nothing inherited by children). Its CHANGELOG entry sits under `[Unreleased]`.
- `git tag --contains 5cc594c` in ~/dev/lernie is EMPTY (latest tag v0.0.6 predates it), lernie's own `Cargo.toml` still reads `version = "0.0.6"`, and `main` is **19 commits ahead of origin/main** — bl-d0b4 is not merely unpublished, it is unpushed. The publish is not in flight, so DESIGN §16.7's one interim git-rev exception does not apply either (and taking it would re-block `make publish`).

Unblock action: cut and publish lernie 0.0.7 from ~/dev/lernie main (push first), then this ball's scope 1 is a real pin bump.

Also note, for whoever picks this up: the *same* release gates **bl-cd38** (consume lernie's mint). bl-404d (`lernie::mint`, the wordlist, the Rng trait) is in that same unpublished 19-commit run — published 0.0.6 has no `mint` module and no wordlist. bl-cd38's written gate says "check crates.io for the release (> 0.0.5)", which 0.0.6 satisfies textually but NOT in substance; an agent trusting that phrasing will claim cd38 and find no API. Both balls should be bumped together on the 0.0.7 pin.

yog-side premises all VERIFIED ALIVE at HEAD (no drift):
- the prose channel to retire is `ball_preamble` in `src/start/goal.rs:115-121` ("The project repository checkout for this work is the git worktree at:"), asserted in `src/start/tests/goal.rs:70` and quoted verbatim in DESIGN §3.3 (docs/DESIGN.md:587-595).
- scope 4's per-target `current_dir` is `driver_cwd` in `src/start/goal.rs:148-161`, threaded through `compose_prepared` into `Prepared.cwd`; DESIGN §3.4's table has the matching "Initial prompt-process cwd" column (docs/DESIGN.md:647-650).
- the claim-derived path scope 2 wants is `crate::binding::work_worktree_path`, already used by `canonical_worktree` (`src/start/goal.rs:89-97`).
- scope 6's interim-channel paragraphs are docs/DESIGN.md:333-353 and 622-628.
- VISION §4.10 item 2 is intact and is the governing contract.

One doc defect found while verifying: AGENTS.md rule 6 line 84 still records the pin trio as `lernie = "=0.0.3"` while Cargo.toml has been at `=0.0.6` since the 0.0.6 adoption. Filed separately.
