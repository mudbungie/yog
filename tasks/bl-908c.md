+++
title = "load-sensitive test flakes: app::tests::drift and start::tests::run::prepare_* fail under machine saturation"
created = 1785646709
updated = 1785648076
claimant = "Bracken"
priority = 1
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Seen twice today (2026-08-02) by different agents under multi-agent load: app::tests::drift::construction_arms_the_watch_before_taking_the_first_snapshot and the start::tests::run::prepare_* group fail when the machine is saturated (concurrent bl-install-main compile, parallel tarpaulins), pass clean solo. Also inotify instance exhaustion (limit 128) flaked watch:: tests in one run. Root causes likely: timing assumptions vs real scheduler, and shared kernel inotify budget. Fix the tests' assumptions (or gate on real events instead of time), don't fatten timeouts blindly; the inotify budget may need per-test watcher cleanup or a documented ulimit bump.