+++
title = "descent rows spell the full ancestry-chain id — nameless rows floor at the terminal id segment; the tree already tells the lineage"
created = 1785736978
updated = 1785736978
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator report 2026-08-03, verbatim: 'Look at the agent names. These are insane. Unparseable.' — with a screenshot of agent skimmer's descent tree: every child row titled with the FULL chained id (<agent-id>-<agent-id>-...), one ancestry segment per generation, dozens of nameless fan-out children. (The fan-out's CAUSE is being investigated separately by the operator's own agent — this ball is display only; do not touch dispatch mechanics.)

Two defects, one seat:
1. The surface rendering these rows (find it from the screenshot's anatomy: the skimmer agent pane with the budget line, notified/abandoned legend rows, indented member tree — likely the inspector/descent member rows; verify against the tree, this may be a seat bl-df72's invariant scan doesn't cover or a newer surface) titles rows by raw agent_id. Every naming seat must ride the display ladder (Agent::name_fact via member_title, bl-df72/6da63d8) — these children are nameless with junk goals, so the ladder floors to the id, which leads to:
2. The id floor itself: a lernie child id EMBEDS the full ancestry chain, and the tree's indentation already states the lineage — repeating it per row is a second spelling of a derivable fact. Rule: when the ladder floors to the id, the row shows the TERMINAL segment only (the agent's own ts-hash, e.g. <agent-id> — or the existing short-oid idiom applied to it), full id on hover. One floor spelling, every seat (member_title's floor changes once; bl-df72's scan idiom extended to catch this surface if it wasn't covered).

Amend DESIGN §3.3's ladder floor accordingly. Test: chained-id fixture renders terminal segment + hover carries the full id; named/stamped rows unaffected. The bl-aca4 design (auto-mint on omission, in flight) will make nameless children rare going forward — this ball makes the floor honest for the ones that exist.