+++
title = "an existing workspace's grant still names multi_tool (and any name later retired or unmapped), so the park recurs there: the start-time grant fold prunes names the pinned litany no longer ships, and the intrinsic map judges an envelope by its invocations"
created = 1788934515
updated = 1788934566
claimant = "Cantaloups-Y15"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["usability-r3"]
+++
Follow-up to bl-d06c (888a8b29), which guards the SHIPPED worker grant against unmapped names. The operator's park happened on a workspace born before the pin bump: its config lineage still grants multi_tool, litany's grant gate passed it, the control classified it opaque and held it, and on the operator's pass the envelope RAN (so the tool is not gone from the executor either — verify litany 0.0.13's tool set against DESIGN_CODE_EXECUTION §5's retirement claim and correct whichever doc is wrong). Ship: (1) bl-0460's start-time fold, which already asserts clients into the worker grant, also prunes names the pinned litany does not ship, so an old workspace converges on the shipped set at its next start; (2) if litany still executes multi_tool, the intrinsic map carries a row for it as an envelope: each invocation judged as if called alone (by name in the map, or by command line), strictest wins, unknown inner name → opaque — the sound version of bl-d06c's original ruling, hung on a name the map holds.