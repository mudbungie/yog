+++
title = "an existing workspace's grant still names multi_tool (and any name later retired or unmapped), so the park recurs there: the start-time grant fold prunes names the pinned litany no longer ships, and the intrinsic map judges an envelope by its invocations"
created = 1788934515
updated = 1788935048
claimant = "Cantaloups-Y15"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["usability-r3"]
+++
Follow-up to bl-d06c (888a8b29), which guards the SHIPPED worker grant against unmapped names. The operator's park happened on a workspace born before the pin bump: its config lineage still grants multi_tool, litany's grant gate passed it, the control classified it opaque and held it, and on the operator's pass the envelope RAN (so the tool is not gone from the executor either — verify litany 0.0.13's tool set against DESIGN_CODE_EXECUTION §5's retirement claim and correct whichever doc is wrong). Ship: (1) bl-0460's start-time fold, which already asserts clients into the worker grant, also prunes names the pinned litany does not ship, so an old workspace converges on the shipped set at its next start; (2) if litany still executes multi_tool, the intrinsic map carries a row for it as an envelope: each invocation judged as if called alone (by name in the map, or by command line), strictest wins, unknown inner name → opaque — the sound version of bl-d06c's original ruling, hung on a name the map holds.

---

Fact established against litany-v0.0.13 rather than a doc: litany does NOT execute multi_tool. cmd::BUILTIN_TOOLS is eleven names and that is not one; `litany tool multi_tool` answers "unknown built-in tool", naming the pool apply_patch, bash, cd, dispatch, load_skill, message, python, read_file, read_tool_output, remember, search_history; no module, schema or skill ships; and tool_host::subject::performs (BUILTIN_TOOLS minus the engine acts) does not hold it either, so yogs router refuses a granted one in band on the worktree lane. This balls premise "on the operators pass the envelope RAN" is therefore the wrong claim, not either DESIGN doc: litany DESIGN_CODE_EXECUTION section 5 and yog DESIGN 8.6 are both right. Item (2), the intrinsic envelope row, is not warranted and is not built; VISION 4.11 gained the verification note.

Item (1) shipped, with one deviation. The prune keeps a name outside the derived shipped set when the workspaces own capability.yaml carries a rules row naming its class. Reason: REMOTE section 5.4s worktree lane routes a GRANTED, unqualified name to the one registered machine that advertises it and consents to workspace-cwd execution, so a bare foreign name in a grant is a supported operator configuration and a blind prune would revert it at every start. The rules row is the existing explicit signal separating a stale grant from a deliberate one, and it is one such a grant needed anyway: without it every call classifies opaque and parks, which is this very defect. The predicate is "prune what nothing can execute and nobody has vouched for", floor = the engines whole shipped pool.

Driven on a scratch engine at 127.0.0.1:7784 under a hermetic world, with a workspace born granting multi_tool and box2_shell. BEFORE, with main 888a8b29 as the engine: /prepare left both in the grant, and yog tool-control on a multi_tool envelope answered hold, "classified opaque (multi_tool is not a tool this control implements and its input carries no command line...)" — the operators sentence verbatim. AFTER: /prepare converged the grant to the eleven shipped names plus clients; with a rules row for box2_shell committed on the lineage, box2_shell survived and multi_tool still went.
