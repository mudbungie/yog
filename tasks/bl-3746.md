+++
title = "inspector work-diff surface: show what an agent actually changed (pure git read; feeds V3 comparison)"
created = 1785649521
updated = 1785649850
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-2b8c"
on = "claim"
+++
Source: coding-harness comparison survey 2026-08-01. VERIFY premises against the tree before editing.

## Gap

No yog surface answers "what did this agent change?" The Files tab shows bounded worktree previews, not deltas; the operator drops to a shell for `git diff`. A commit-aware bounded diff remains a useful pure-read inspector and is a building block for VISION V3 comparison.

## Corrected authority premise (bl-e249)

The original task claimed lernie's `git diff <fork-point>..<tip>` is the project work product. That is false for yog path/ball starts today:

- the target project is the external path/balls worktree named in the goal;
- pinned lernie tools default to the separate `<workspace>/agents/<id>` worktree;
- lernie's tool commit stages only its own worktree.

So the lernie ref contains transcript/context work, while project edits may live only in the balls worktree/branch. There are two valid diffs with different meanings; never label one as the other. bl-2b8c must first rule the authoritative project target, child/fan isolation, and adoption/delivery join.

## Scope after bl-2b8c

- Render the ruled project-work diff for ball/path work and name the compared refs/commits.
- If an agent-context diff remains useful, label it separately; do not merge two authorities into one view.
- Reuse the existing ball/conversation binding and pure Git reads; no diff cache or yog-owned work registry.
- Decide placement per DESIGN §11 and amend DESIGN.
- Bound large diffs per file with honest counts/markers; exact raw access remains reachable.
- Cover missing/deleted/reclaimed worktrees and foreign roots without guessing.