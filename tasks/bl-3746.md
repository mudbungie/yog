+++
title = "inspector work-diff surface: show what an agent actually changed (pure git read; feeds V3 comparison)"
created = 1785649521
updated = 1785649521
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Source: codex comparison survey 2026-08-01 (openai/codex @ 2b5bdcf). VERIFY premises against the tree before editing.

## Gap
No yog surface answers "what did this agent change?" The Files tab (src/files_view/) shows bounded worktree previews, not deltas; the operator drops to a shell for `git diff`. Codex treats the per-turn net diff as a first-class fact: a turn-diff tracker aggregates all file mutations into one event (codex-rs/core/src/turn_diff_tracker.rs), and the TUI renders `/diff` with syntax highlighting (codex-rs/tui/src/diff_render.rs).

## Why it is yog-shaped
lernie commits every tool side effect (ARCH §3.3 commit-per-side-effect), so the net work-product diff is `git diff <fork-point>..<tip>` — a pure read of the workspace repo, no new verb, I1-clean, same class as V1's "derivation and rendering only". VISION V3 verbatim: "The group renders the comparison the operator actually judges by: response diff, usage per candidate, wall time" — this surface is that comparison's building block, landable now without the fork verbs.

## Scope
- Per-conversation (and per-member) net diff in the inspector; derivation in a pure VM module, egui glue thin, per the house split.
- Decide placement per DESIGN §11 (tab vs. section of Files); amend DESIGN where declared.
- Verify how the fork point is derived (governing-config commit vs. dispatch ref) before building; do not guess.
- Bounded rendering (large diffs summarized per file with counts — honest markers, never silent truncation).