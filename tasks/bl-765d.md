+++
title = "capability policy surface: workspace capability config, hold answers as boundary actions, parked-branch attention"
created = 1785824595
updated = 1785824595
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
tags = ["capability"]

[[blockers]]
id = "bl-fec8"
on = "claim"
+++
Implements VISION §4.11 items 4–7 and DESIGN §8.6 (ruled by bl-0cea). Needs bl-fec8 (the control shim defines the fact shapes it reads). Blocks bl-66fb (V4's armed loop arms only over a live noninteractive policy).

## Build
1. **Standing policy config, per workspace, severable**: the class→verdict table, the bash ruleset, and the optional `confinement: required` flag. Absence IS the shipped safe defaults (read/target-write/process=pass; open-world=hold; destructive/secret=refuse) — the cadence.yaml pattern: defaults in code consts, config is the override, deleting it deletes policy, not code. Settle the exact home per DESIGN §8.6 (the workspace config branch beside workflow.yaml, edited through the §9.3/§9.5 machinery, is the ruled default — the control reads the LIVE config tip, never the frozen governing commit, because it acts for the operator and revocation must bind now). §9.5 rows, not a fourth raw-text editor.
2. **Boundary actions** (§8.5 variants, compile-gated headless spellings): ApproveHold (once: an ops row scoped to the held tool_use id — ids are provider-unique so once-grants need no consumption and cannot race; rule: a policy edit), DenyHold (same two scopes), policy edits. Answering a hold also fires `lernie advance <ws> <agent>` — the explicit user action continuing (I7); re-adjudication does the rest.
3. **Boundary query** PendingHolds + attention: parked conversations derived from `refs/lernie/held/*` across workspaces (fs-watched pure read, I1); each attention item names the tool, an input summary, the computed effect class, and the control's reason. A parked drone renders as the ball waiting at its gate, never as a dead conversation. NO modal anywhere: attended and unattended are one flow — attendance is latency, not a mode.
4. **Ruleset seed**: the shipped default bash ruleset (build vocabulary: git-local/cargo/make/coreutils classified read or target-write, root-scoped) as world config, operator-editable, severable.
5. **Confinement refusal**: a workspace whose policy declares `confinement: required` refuses to arm (and to fire drones) on a platform with no confinement layer — refuse, never silently fall back; no UI affordance renders for a layer that does not exist (no capability theater).

## Audit / races (ruled)
- Every pass leaves lernie's ordinary record (transcript tool_result + steps/); refuse is a committed in-band decline; hold is the mark. The control writes nothing; policy WRITERS write ops rows (row = audit = the fold's memory, the §4.9 monitor pattern).
- Revocation binds at the next consult; a verdict already passed runs its one call — recalling it mid-window is the bl-b98d wedge, deliberately not attempted.