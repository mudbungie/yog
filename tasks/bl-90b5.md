+++
title = "live-activity indicators: conversation name pulses in the chat list + an in-pane indicator while anything is in flight; distinct looks for inference/tools/subagents, priority inference > tools > subagents"
created = 1785645851
updated = 1785645851
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator feature request 2026-08-02, verbatim intent: 'I'd like the name of the conversation to pulse in the chat list, and something in the active conversation pane, while anything is in flight in the conversation. It should look different for tools vs inference vs subagents. The priority list for what to display is inference > tools > subagents.'

Spec: (1) chat-list row: the conversation's display name pulses while that conversation has ANY in-flight work; (2) the open conversation's pane carries an indicator too (place it where the eye already rests — header or composer edge); (3) three visually distinct states — inference (model call running), tools (tool executing), subagents (dispatched child running); when several are simultaneously in flight show ONE, by priority inference > tools > subagents. In-flight facts must be DERIVED from what's on disk/process state (steps in progress, tool result absent — bl-47ec is landing exactly that derivation for tool rows; build on it, don't duplicate), never a stored flag. Pulse = egui repaint animation; make sure it requests repaint only while something is actually in flight (no busy-loop painting when idle). Amend DESIGN (§7/§11 as fits).