+++
title = "tool hosts II — the clients tool, loading, and routing: agents see and drive a tool host's advertised tools (REMOTE §5)"
created = 1786757988
updated = 1786759094
claimant = "Fairlead"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"

[[blockers]]
id = "bl-4e08"
on = "claim"
+++
Step 7 of the client/server split, second half (REMOTE §5). lernie 0.0.9 carries the tool-injection seam (lernie bl-9001: Fx.tool_injection, trait ToolInjection { tools() -> Vec<InjectedTool>, route(RoutedCall) -> Option<RoutedCapture> }; injected outranks elected; adjudication untouched and runs before routing; the executor owns the per-invocation disk record).

Deliverable:
- Bump the lernie pin to =0.0.9 (Cargo.toml is the pin authority).
- yog implements ToolInjection at the seam where it constructs lernie's Fx: tools() = the client-management tool (always) plus the conversation's loaded set; route() = the clients ops answered from registry+presence, and loaded remote names routed down the tool host's live wire connection with a deadline — a vanished or refusing client is an in-band error result, never a hang. Adjudication (yog tool-control) is unchanged and fails closed.
- The client-management tool, ONE tool in the stable prefix named clients, three ops (its subject is the roster — loaded remote tools still surface individually, the no-multiplexer ruling holds): {op:list} -> the workspace's registered clients and which are live now; {op:get, client} -> one client's detail and its advertised tools (names+descriptions); {op:load, client, tools:[...]} -> the named definitions become callable from the next step on. Every reply a dated observation appended in-band; never a prefix mutation; refusals in-band. The loaded set is durable — a world file beside the conversation, read by tools() each assembly; v1 offers no unload (a fresh conversation starts clean); record that line in REMOTE §5 citing this ball, plus the clients-tool schema.
- Routing transport: the tool host's live connection carries the invocation down and the capture back — decide the frame shape on the existing framing (it already carries streams) and record it in REMOTE §3/§10.
- The client-side executor: the tool-host end maps an advertised name to a local command from its own config, and the advertisement is generated from that same config — single source, the two cannot drift.
Verify shapes against the tree; this body drifts. Boundary-surface work: serialize.