+++
title = "yog: hand the rendezvous material to a client — wire-certs writes rendezvous.pub beside pairing.salt and every client leaf, and the §8.4 enrollment envelope carries the engine pubkey and the salt"
created = 1790393065
updated = 1790393065
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
Found by lernie bl-65ee (the seat half of REMOTE §13.3–§13.4, yog bl-653a): the engine mints rendezvous.key (a 32-byte ed25519 SEED) and pairing.salt beside ca.pem (src/wire/provision.rs, rendezvous_for), but nothing emits the PUBLIC key, and a client needs exactly (engine rendezvous pubkey, pairing salt) to derive the presence/inbox keys and verify presence. Today an operator would have to derive the public key from the seed by hand.

Deliver: (1) provision::mint / rendezvous_for also writes rendezvous.pub (hex, 32 bytes, mode 0644) beside rendezvous.key, and re-derives it if absent on a box that has the key (idempotent, same recipe as the mint — REMOTE §1.4 posture: out of channel, operator's box); (2) the client-leaf issuing path (yog wire-certs / provision/issuing.rs, wherever a client's leaf + ca.pem bundle is written) includes rendezvous.pub and pairing.salt in that bundle so one out-of-channel hand-off carries everything a seat or foot needs — read lernie src/channel/material.rs (Material.pairing) and rendezvous.rs for the exact filenames the seat reads (rendezvous.pub, pairing.salt) and match them; (3) the §8.4 enrollment envelope (REMOTE §8.4; grep enroll in src/wire and src/boundary) carries the engine pubkey and the salt as two new fields, absent when the box has no rendezvous material — an EDITION, not a PROTOCOL bump: make corpus, edition ledger paragraph. Amend REMOTE §8.4 and §13.2, DESIGN §12. Tests: pub derived from a known seed matches a pinned hex; bundle carries both files; envelope round-trip present and absent; a loopback-only box (no material) emits neither and the envelope omits the fields.