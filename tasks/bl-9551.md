+++
title = "panes paint on top of each other: the conversation view overlaps its own composer, pin banner and fork composer at the default window size"
created = 1786163318
updated = 1786163318
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
QUALITY.md §1 criterion **G4** ('Resize sanity … nothing overlaps at either') and **L3** ('Legible always'). Audited sha 4b0e75c, run /home/u/.cache/yog-drive/quality-20260807T214407Z/out.

SYMPTOM. Once the centre pane's stacked accessories exceed its height, egui does not scroll or clip them — it paints them over one another. Independent text runs share pixels and all of them become unreadable.

WITNESS 1 — the DEFAULT 1150x760 window, no small size needed: `Q-S6-activity-default.png` and its crop `crop-s6-overlap.png`. Six layers collide between y=440 and y=545: fork member cards #2/#3, the inbox deposit row, the 'as of d4e4c4e' pin banner, 'Fire', 'ball bl-059c Close Release', '· user: Print the numbers 1 through 400…', 'move to: clientwork', and the composer box. In the tighter crop the composer's hint 'message growing' is painted directly through the transcript's user row.

WITNESS 2 — `probe-s6-stirred.png`: same collision at default size with the activity accessory CLOSED, so the activity trail is an aggravator, not the cause.

WITNESS 3 — the minimum supported size, 420x320 (`src/main.rs:116` min_inner_size): `Q-S12-small.png`, crop `crop-s12-overlap.png`. Four runs on the same pixels, wholly illegible.

REPRODUCTION (every gesture is a §11 binding or a §8.5 line; one pick is a coordinate, tagged):
  1. scratch world + seeded project, launch yog
  2. bare i / type a goal / Return  -> a conversation on the wire
  3. bare Down to select it; bare 1 (Transcript)
  4. CLICK the auto-expand-conversation checkbox off, so the step spine's rules are reachable
  5. CLICK a spine rule -> pins the notch and raises the fork composer
  6. CLICK the fork composer's '+' twice -> x3 members
  7. lay the §6 attention marks (git update-ref refs/lernie/budget-exhausted/<agent>, refs/lernie/conflicted/<agent>, and an inbox/<agent>/<child>-001.md deposit) -> two attention lines join the header
  8. bare a -> activity accessory
The pane is now over-subscribed and everything from step 5 down paints on top of everything else.

TRIAGE ONLY — this ball is filed by the audit, not fixed by it.