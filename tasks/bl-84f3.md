+++
title = "S3/S4/S6 drive coverage: close, second-conversation + by-ball toggle, attention strip — no new machinery"
created = 1785124355
updated = 1785125212
claimant = "waxier-84f3"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
From the bl-f69b S2+ inventory: scripts/drive/stories.sh covered only S0/S1. STORIES.md's own "Drivable next" split said S3's close, S4's second conversation + grouped-by-ball toggle, and S6's attention strip + activity chip need NO new harness machinery. Add drive beats for those rows, steering by DESIGN §11 bindings (pixels only where no binding exists, tagged CLICK (no binding)), and run them green against the live wire on Xvfb.

## STATUS — done, green, ready to close

**Delivered (no Rust changed; scripts + docs only):**
- `scripts/drive/beats_s3s4s6.sh` (new) — the S3/S4/S6 beat body, sourced by
  stories.sh; `stories.sh` gains a `run-s3s4s6 <scratch> <out>` verb and its own
  ball world (a throwaway `git init` repo primed through `yog exec bl prime` +
  one `bl create` — the story's own §8.4 gesture, not new machinery).
- **15 beats, all PASS on the live `gpt-5.4` wire**, verified twice at load
  average 84-89: the §8.1 `lernie new`-then-`bl claim` order (as ops line
  numbers), claim `--as` the workspace name, the detached prompt's cwd **is the
  ball worktree**, the reply on disk, `bl close --as` the bound sphere, the ball
  gone from `bl list` **and** the closed listing carrying `claimant: <that
  workspace>`, a second conversation with no re-mint, `lernie stop` on a live
  stream, the `seen` watermark in `ui.json`, the by-ball toggle writing nothing
  and spawning nothing, a second sphere minted, the activity chip a pure read.
- S0/S1's eight beats still green (same hardening applied to them).
- `docs/drive-logs/2026-07-26-s3-s4-s6-wire-green.md` — dated log: per-beat
  evidence, the ops trail, the click table with a reason per coordinate, what the
  screenshots show, and honest per-row verdicts for what is NOT driven.
- `docs/STORIES.md` — world-2 beat list, the fifteen-beat run-log paragraph, the
  timing rule, and the negative-assertion caveat.

**The finding worth keeping:** a clock is never the thing to wait on. Three reds
were one mistake in three costumes — an 8 s sleep vs a model four tool calls
deep; a 5 s sleep vs `lernie stop` reaping a *streaming* driver (an ops row lands
when its child finishes); and a blind click at 2 s that hit blank panel because
the balls section had not painted, silently skipping the whole ball rung. The
runner now has exactly two waiting primitives — `await <predicate>` for verdicts
and `until_landed <gesture> <predicate>` which re-fires a gesture until the
substrate agrees it landed — and every remaining `sleep` gates a screenshot, not
a verdict. `yogdrive.sh launch` also searches 60 s, not 30, for the window.

**Left undrivable, recorded rather than faked** (log + STORIES.md carry the
detail): drivable-but-not-driven S3-T2, S4-T2, S6-T2; fixture-blocked S3-T4
(a failing pre-commit hook), S4-T4's uncoloured-id case, S4-T6, S4-T7's pins,
S6-T1's other four predicates, S6-T5's `M ⚠` half. No Rust gap was found, so no
gap task was filed.

`cargo test` green in the worktree (exit 0, 28 test binaries).