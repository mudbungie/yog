+++
title = "yog: the rendezvous loop and the punch — publish presence, poll the inbox, simultaneous open, held-connection serving"
created = 1788232801
updated = 1790143919
claimant = "Urinalyses-M"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"

[[blockers]]
id = "bl-4d56"
on = "claim"

[[blockers]]
id = "bl-df31"
on = "claim"
+++
REMOTE $13.2–$13.4 engine side: the mint grows an ed25519 rendezvous keypair and a pairing salt (out-of-channel, beside ca.pem); the engine publishes its sealed presence item hourly, polls the salt-derived inbox at 15s, verifies+unseals before ever emitting a SYN, and TCP-simultaneous-opens from its fixed punch port (listen-and-connect on one port, v6 first). A punched stream carries the inner mTLS wire directly and feeds the same serving code an accepted connection feeds; held connections ping at 25s idle. GATED on $13.7 ruling 1: socket2 vs sys.rs for SO_REUSEADDR — socket2 recommended, needs explicit operator approval either way before claiming.

---

bl-df31 landed the DHT client this ball consumes: src/dht, module root src/dht.rs, REMOTE 13.2 as amended carries the interface. Consume exactly this and nothing behind it: Dht::new(Box<dyn Transport>, Vec<SocketAddr>, Config) — Udp::bind("0.0.0.0:0") is the production transport, bootstrap addresses are RESOLVED by the caller (the mainline bootstrap hostnames are the caller's fact, the client takes SocketAddr), Config { alpha, k, round, max_queries } with BEP 5 defaults. Verbs: lookup(NodeId) -> Vec<Node> closest first; get([u8;32] key, Vec<u8> salt) -> Option<Mutable> newest item that VERIFIES; put(Mutable) -> usize acks, zero is Err naming the first refusal. Keys: Keypair::{generate, from_seed([u8;32]), public, sign(salt, seq, value) -> Mutable}; Mutable::{target, verify}; target_of(key, salt). Value is a byte string capped at BEP 44's 1000 bencoded bytes — seal presence/inbox into that. Everything is Err(String). A walk BLOCKS the calling thread up to a round per hop (2 s default) and at most max_queries datagrams, so the hourly republish and the 15 s inbox poll belong on the thread 13.4 gives them, never a request path. The walk was measured on loopback only; the first live mainline walk is this ball's first act — expect to tune Config on evidence (13.7 ruling 3). The client never listens: the punch socket is yours, not the DHT's.
