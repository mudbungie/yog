+++
title = "REMOTE 9.8 residual: the last four in-process acts — Message, the Prepare/Prompt doors, the 3.6 deletes, the slash line's act arm"
created = 1786765706
updated = 1786765706
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"

[[blockers]]
id = "bl-f297"
on = "claim"
+++
bl-4841 (dd3d6438) moved 13 of the window's acts onto the wire under minted receipts (REMOTE 9.8) and left four in process, each because its reply gates a frame-side fact in the same breath: Message (draft clear + the 3.4 echo), Prepare/Prompt (workspace adoption, the held start claim, mint-seed spend), the two 3.6 deletes (modal + the ui.json prune), and the slash line's act arm (whether the typed line clears). AppModel::dispatch is exactly the size of that list — its deletion is this ball's finish line, and with it the window's dialogue is wire-only in full (REMOTE 1.2 closed to the letter).

The design inputs are all in REMOTE 9.8: the echo's trigger moves from the synchronous Ok to the receipt when its flow migrates (ruling 3); the ui.json race the four writes share is the deposit consumer's existing one, adopt-not-fight (ruling 4); fire-and-hold survives as structure. Work each act as its own increment on the 9.8 shape (fire writes the sentence, receipt drops the mark), never leaving the window half-broken. Verify against the tree; this body drifts. Boundary/shell surface: serialize (needs-edge on bl-f297 enforces it).