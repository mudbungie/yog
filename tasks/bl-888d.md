+++
title = "macOS build out of a container: cross-compile the aarch64-apple-darwin artifacts reproducibly"
created = 1788067965
updated = 1788067965
priority = 3
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"

[[blockers]]
id = "bl-223f"
on = "claim"
+++
The macOS side of the containerized-build ruling: a Linux container that cross-produces the aarch64-apple-darwin binaries (osxcross or zig-cc toolchain — evaluate against the no-C-toolchain-dependency posture and record the choice), so the mac artifact comes off the same reproducible line as the Linux images rather than a hand-run laptop build. Applies to the components a mac would run (seat and thrall foremost; the server where wanted). DESIGN §10 is the portability authority to amend with whatever the toolchain choice constrains.