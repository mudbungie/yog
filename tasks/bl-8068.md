+++
title = "inter-agent messaging still fails — diagnose from agent energize's logs and fix where the fault lies"
created = 1785731681
updated = 1785731681
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator report 2026-08-03, verbatim: 'inter-agent messaging still isn't working. have a subagent check the agent energize's logs to diagnose and fix.'

Context that bounds the diagnosis: lernie 0.0.4 (pinned since bl-5134, commit 7a5c911) resolves message targets by exact id first, else unique living name; yog passes --name at fire only since bl-08f2 (commit 316fef8, landed 2026-08-03) and only for dispatches made by a binary carrying it. 'energize' is a one-word name, so it was minted by a post-bl-d12f binary — but its PEERS may predate the name fact, and a message addressed to a fact-less peer's name will still fail with no-agent-in-workspace. Other candidates: the running yog binary predating the fixes (close.post auto-install timing), the message tool erroring for a different reason entirely (read the actual is_error payloads), or a lernie-side resolution bug.

The work: find agent energize (grep agents/*/name and goal files across ~/.local/share/yog/workspaces and any lernie workspace roots), read its transcript/messages/*-tool.json errors and step records, identify the exact failing call and its error, and fix where the fault lies. If the fault is a stale binary or a pre-fact peer, the fix may be operational (reinstall/re-dispatch) — then this ball's deliverable is the diagnosis recorded plus whatever yog change prevents the confusion recurring (e.g. surfacing a clearer error). If the fault is lernie-side, file the lernie ball, work it there, and record the release ordering as prose here. Verify everything against the actual bytes; trust the logs, not this body.