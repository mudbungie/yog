+++
title = "the drill-in record picker cannot open stderr.log or driver.log: a long driver error is unreadable in-window"
created = 1786843884
updated = 1786845119
claimant = "Picker83d6"
priority = 3
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
DESIGN's own incident row names the residual: the Altitude-2 record picker lists the five JSON records only (steps_view/drill.rs RECORDS = meta/request/staging/response/tools), so the whole of a long stderr.log is not browsable in-window — the only view is the wound banner's 4KiB tail narrowed to 3 lines. Since lernie 0.0.9, steps/<agent>/driver.log exists beside the step dirs and is likewise unreadable in-window (bl-ace6 surfaces its tail in a banner; this ball makes the full text reachable). Add stderr.log to the per-step record set, present only when non-empty, and give driver.log a seat on the agent's steps surface. Gate: tests, docs (§11 Altitude 2 record list), alignment.