+++
title = "a blank goal sends: bl-6191's 'a blank goal never sends' invariant was carried by actions::goal_present, which nothing called, so no door refuses an empty prompt"
created = 1788483899
updated = 1788483904
claimant = "Spellbind-S"
priority = 3
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["boundary"]
+++
Found closing bl-33e9. DESIGN §8.1 step 2 said: *one predicate decides whether a fire may go at all, `actions::goal_present`: a blank goal never sends, and it is checked inside the shared `prompt` body so every spelling is gated once between them (bl-6191).* Nothing read that predicate — not `boundary::dispatch::doors`, not `start::execute_prompt`, not `start::prompt` — so the invariant has had no enforcement in this crate; bl-33e9 deleted the callerless predicate and amended the sentence rather than restore it.

A blank goal reaching a model is spend for nothing and a conversation whose first entry is empty. The engine is the one home for the refusal (a seat may also grey its button, but that is a view). Shape: the shared `prompt` body — the one place every spelling (`/prompt`, the start door, a re-prompt) passes — refuses a goal that is empty after trimming, with a legible sentence, before any wall is touched or any ops row is written. Restore the §8.1 step 2 sentence to name the door that gates it. A beat per spelling proving the refusal, and one proving whitespace-only counts as blank.