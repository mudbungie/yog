+++
title = "engine rename: lernie becomes litany"
created = 1787977101
updated = 1788060105
claimant = "OrderRepinner"
priority = 3
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"

[[blockers]]
id = "bl-37fd"
on = "claim"
+++
Upstream rename of the loop-engine crate to litany; yog re-pins. The crates.io name is held at 0.0.0.

The one priced decision inside: LERNIE_HOME and the state-home layout. Keep the old variable and the name diverges from the thing forever (the two-representations drift smell); rename it and it is a state migration. Resolve explicitly in the engine's own design doc — do not drift into either.

Doc cascade: the engine's ARCH / TAXONOMY / tool-injection docs are cited by name from yog's DESIGN and REMOTE — update the citations in the same change that lands the pin bump.

Version fence (per bl-37fd): the lernie name's engine line ends at 0.0.x; its final release notes state where the engine continued. Implementation lands in the engine's own repo and store; this ball tracks yog's side — the pin, the citations, and the coordination.