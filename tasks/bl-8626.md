+++
title = "macOS main is red in the wire era: ~15 failures in test_support wire cert minting plus two stragglers — macOS-only, unrelated to git identity"
created = 1786763574
updated = 1786763574
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
Found by bl-ff85's CI verification (2026-08-14): after the first green Linux CI run on main in the streak, the macOS job stays red and it is NOT the git-identity class. The bulk: ~15 failures landing in src/test_support/wire.rs around line 115 — the runtime test-certificate minting the wire suite (bl-b6fa) leans on, which suggests the macOS runner's TLS tooling (LibreSSL openssl vs OpenSSL flags, or path/permission differences) refuses the same invocation Linux accepts. Plus two stragglers: multiplex::landing::tests::the_repair_is_idempotent and start::instructions::tests::a_non_utf8_path_is_skipped. Reproduce from the CI log first; fix the minting invocation portably rather than gating macOS out (bl-1015 fought for that job's first green). Verify against the tree; this body drifts. NOTE: serialize with any in-flight wire-surface ball.