+++
title = "Z5: failed actions are rendered facts — attempted ops rows + surface failures"
created = 1784524503
updated = 1784524503
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
DESIGN §15 M6 Z5 (bl-d7a1 amendment) / §4.2 as amended / §7.3 failed-action row / STORIES INV-2, S0-T3 (ops+VM halves), S1-T4. Ops rows for ATTEMPTED actions: synthetic lines for spawn failures (delete verbs.rs missing-binary un-logged carve-out), ["yog-step",<name>] rows for non-spawn step failures. OpRow carries cwd/stderr/stdout (expandable ops pane). Start + short-verb failures render at originating surface in ichor red, argv + stderr tail; surface holds last failure as §5.3 RAM item. Delete every eprintln! in src/shell/ (INV-2 mechanized: grep count zero). Files: src/opslog/mod.rs, src/actions/verbs.rs, src/shell/* touches, failure view-model.