+++
title = "monitor rung: revoke tool auto-approval as a per-conversation capability floor"
created = 1785824598
updated = 1785893229
claimant = "floor-writer"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
tags = ["capability"]

[[blockers]]
id = "bl-765d"
on = "claim"
+++
VISION §4.9's fifth rung, now ruled by bl-0cea (VISION §4.11 item 7): an alignment verdict becomes one INPUT to the capability policy — the monitor rules whether work serves the goal; the capability boundary rules what an agent may ever do; neither substitutes for the other.

## Build
- A boundary action RevokeAutoApproval(conversation): writes the per-conversation floor override — every class above read adjudicates to hold — as an ops-row fold the control shim (bl-fec8) already reads. RestoreAutoApproval is the symmetric row; latest row wins the fold.
- Wire it as a ladder verb in the §4.9 monitor's cadence.yaml wiring (a tier-0 direct wiring or a responder grant, both config).
- Needs bl-765d (the fold and its readers).

## Coordination — by ruling, not by editing its code
yog bl-8da1 (agent invigilator) is implementing the monitor now and floors the responder grant at `flag` precisely because this rung had no substrate; its recorded "NO revoke-auto-approval — waits on bl-0cea" resolves HERE. When this lands, bl-8da1's ladder config gains the rung; do not patch the monitor's internals ahead of it.