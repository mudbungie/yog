+++
title = "Y11: multi-workspace AppModel + navigator + attention strip (M2 shell)"
created = 1784349558
updated = 1784349558
parent = "bl-4e66"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-dd2a"
on = "claim"

[[blockers]]
id = "bl-32d3"
on = "claim"

[[blockers]]
id = "bl-c865"
on = "claim"

[[blockers]]
id = "bl-a212"
on = "claim"
+++
DESIGN.md §15 Y11. AppModel holding the snapshot map (HashMap<PathBuf, GitTree> + workspace classification), fed by the WatchSet; the roster panel (pinned -> projects -> ad-hoc -> replays, ball ids from path arithmetic only — no bl calls yet), the attention strip with totals and jump; per-instance focus with startup derivation; pins/collapsed durable via ui_state. --repo deleted; optional --workspace initial-focus flag. Milestone M2 shell: browse every workspace, attention-sorted, two instances converging on seen/pins. Files: src/app/mod.rs (~230), src/shell/{navigator,workspace}.rs (excl.), src/lib.rs.