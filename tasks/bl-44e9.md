+++
title = "REMOTE read path: the conversation-list and inspector folds want the answer at a different altitude; the tab bar wants name-keyed focus"
created = 1786765941
updated = 1786767917
claimant = "Futtock"
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
bl-f297 closed the read-path residual's two soft classes and left two hard ones. REMOTE 9.7 carries the reasoning in full; this was the work.

WHAT LANDED

1. THE FOLD CLASS, closed. Reply::Conversations answers the whole descent forest with its per-row rollups, in paint order; nav::convs::visible(rows, expanded) is the seat's pure selection out of it — a one-pass depth cut over rows, no snapshot. visible_rows split into forest_rows (the derivation) and visible (the fold). A seat with no fold selects the root subset, which is byte for byte the list the query used to answer, so a machine reader filters depth == 0 and nothing else moved. DESIGN 8.5 is NOT amended: the fold never crosses.

The 11 list reads it over the wire; AppModel::conversations, visible_conversations and conversation_groups are gone. One seat read serves the paint, the up/down walk and the left that pages to a parent.

Reply::Rail's notches carry the budget as a rollup rather than their own spend, so resolving a pin reads one field off the notch instead of summing the prefix — the pin is a view that derives nothing. Query::Files gained at: Option<String>, a SELECTION naming which tree (the one pinnable tab whose subject is a different tree); headless it is /files [<path>] [--at <commit>].

2. THE ECHO, which the ball did not anticipate and which is the finding. Migrating the list would have deleted 3.4 from the one surface it exists for — bl-adcb's "a seat that reads over a wire has no optimism" is true of the board and wrong here. Ruling: a seat's optimism reaches whatever that seat actually reads. echo::rows::with_echo folds the same value onto an answered list, beside compose in the one module. A start with no row yet leads the list faded in the operator's own words; a target already answered is freshened to the echo's age and nothing else moves.

3. THE HARNESS. A driven frame settles its own reads now, as 9.8 already ruled for acts, to a fixed point on AppModel::awaiting (is any standing question unanswered). A Link may never be settled twice without a frame between, so a landed read costs two extra passes and cannot cost fewer.

4. SEARCH, as 9.7 chose: the Searcher holds a seat and asks the engine, minted by Engine::searcher beside the asker and poster and riding WindowWire. A refusal lands as an unreadable source (Found already means exactly that), so it never reads as no-matches. The abandon predicate went with the walk — the engine finishes what it was asked and a superseded answer is dropped on publish.

5. CONFIG EDITORS: confirmed out of scope, as 9.7 records.

WHAT DID NOT LAND, AND WHY

Name-keyed focus (class 2) was attacked and put down. A name resolves against the enumeration, and the 3.4 raise focuses a workspace the enumeration has not read: today the frame holds the path so the bare rung fires into the wall just raised, and name-keyed that focus is unresolvable for one derivation, with Enter in that window firing into the previous workspace. Migrating the tab bar does not dissolve it — the engine resolves the same name against the same snapshot. Two answers named in 9.7; picking between them is design. Filed as bl-7407.

Prepared::binding stays named: its condition (one seat's read path being the only read path) is not met — the composer's reads are not migrated.

The 11 inspector family stays in process, now unblocked rather than blocked: every one of the seven has a query, the pin is selection over an answer, and the four per-snapshot memos come out with it. Filed as bl-13f9, which also carries config-frozen-at's missing spelling and the Files preview's remaining seat-side branch.

Files added: src/shell/convs.rs, src/app/echo/rows.rs (+tests), src/test_support/convs.rs, src/test_support/clock.rs, src/search/worker/tests.rs, src/boundary/answer/tests/confirm.rs. DESIGN 12 rows added for each non-test one.