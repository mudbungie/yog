+++
title = "at the documented minimum window size (420x320) text is hard-cut mid-glyph with no ellipsis and four of six inspector tabs are off-window"
created = 1786163325
updated = 1786163325
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
QUALITY.md §1 criterion **G1** ('Nothing clipped … deliberate elision shows an ellipsis and the full value is reachable'). Audited sha 4b0e75c, run /home/u/.cache/yog-drive/quality-20260807T214407Z/out.

420x320 is not an arbitrary small size: it is yog's own `min_inner_size` (`src/main.rs:116`) and DESIGN.md states it outright — *'The window's own min_inner_size is the same question one level up: **420x320**, lowered from 700x500 — yog must fit a tiled quarter-screen, not dictate one.'* So it is the smallest size the surface is CLAIMED to support, which is what QUALITY.md §2 asks the small capture to be.

SYMPTOM at that size, witnessed in `Q-S1-small.png` / crop `crop-s1-small-left.png`:
  - left-panel ball rows are cut mid-character with NO ellipsis: 'bl-b5ce: epic: the inspecto', 'bl-9ade: rename the spend col', 'gate bl-059c: inspector: r'. Exactly one row in the same list ('bl-059c: inspector: ra…') does get an ellipsis, so the elision is inconsistent as well as absent.
  - the conversation header is cut with no ellipsis: 'growing 2026-08-0', 'budget 2611 tok (in 18', 'model · gpt-5.4 · frozen'.
  - the inspector tab strip shows 'Transcript  Steps  In' — **Inbox, Files, Config and Work are off-window and unreachable by pointer**. (F1 is NOT violated: bare 1–6 still reach them, `src/keymap/tabs.rs:35-46`. This is a G1 finding only.)
  - the composer's verb row is cut: 'Message' and 'Stop' are gone.

Also at this size, `Q-M-ws-small.png` / crop `crop-qm-small.png`: the new-workspace modal is wider than the window — its right border is off-window and the prompt label ends flush against the window edge.

REPRODUCTION: launch, start one conversation, then `xdotool windowsize <wid> 420 320`. No other gesture needed.

TRIAGE ONLY — filed by the first quality audit, not fixed by it.