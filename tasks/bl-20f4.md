+++
title = "drive harness selects the conversation by stale pixel coordinates"
created = 1784955871
updated = 1784958562
claimant = "Exultantly-20f4"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Found by the bl-8e07 real-substrate evaluation (docs/drive-logs/2026-07-24-s0-s1-wire-blocked.md).

REPRO
1. build yog in a worktree; PATH=$PWD/target/release:$PATH
2. scripts/drive/stories.sh run <scratch-data> <out>
3. beat "S1 message-to-agent: lernie message" FAILS — "no message verb".

EVIDENCE
- ops.jsonl for the run carries THREE `lernie prompt` rows and zero `lernie
  message` rows; the message beat fired a new root instead of a message.
- stories.sh:86 clicks (74, 56) to select the conversation. Since the
  conversation-ball association wave (e8f7033, bl-de16) that point is the
  `recent | by ball` toggle of the conversation list header; the first
  conversation row is at y ~= 77. Screenshot s0-04-transcript.png shows the
  cursor on the toggle, the list switched to the grouped view, and the center
  still reading "select a conversation — or start one below".
- With no conversation selected the composer targets "new conversation", so
  Enter correctly fires `lernie prompt`. Confirmed by hand on the same world:
  clicking (140, 77) selects the row, the composer relabels to "message the
  selected conversation" with a Message button (x1-failed-conv.png). The beat
  would have passed against a correct coordinate.

FIX DIRECTION (not done here)
Stop steering by pixels. DESIGN §11 gives keyboard bindings for exactly this:
up/down step the conversation focus, digits 1-5 pick an inspector tab. Driving
selection with xdotool key Down (and tabs with digits) is layout-independent
and cannot drift when a header row lands. Same treatment for the workspace-tab
click at (1063, 12).

STORIES.md now records the lesson in its real-substrate-drive section
("Selection must not ride on pixels").