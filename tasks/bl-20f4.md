+++
title = "drive harness selects the conversation by stale pixel coordinates"
created = 1784955871
updated = 1784958795
claimant = "Exultantly-20f4"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Found by the bl-8e07 real-substrate evaluation (docs/drive-logs/2026-07-24-s0-s1-wire-blocked.md).

REPRO
1. build yog in a worktree; PATH=$PWD/target/release:$PATH
2. scripts/drive/stories.sh run <scratch-data> <out>
3. beat "S1 message-to-agent: lernie message" FAILS — "no message verb".

EVIDENCE
- ops.jsonl for the run carries THREE `lernie prompt` rows and zero `lernie
  message` rows; the message beat fired a new root instead of a message.
- stories.sh:86 clicks (74, 56) to select the conversation. Since the
  conversation-ball association wave (e8f7033, bl-de16) that point is the
  `recent | by ball` toggle of the conversation list header; the first
  conversation row is at y ~= 77.

FIXED — the steering rule is now "drive the §11 binding wherever one exists"

Per-action audit of every coordinate gesture in scripts/drive/:

| gesture | before | after |
|---|---|---|
| focus the workspace | click (1063, 12) — workspace tab | REPLACED by `key Down` |
| select the conversation | click (74, 56) — hit the `by ball` toggle | REPLACED by `key Down` |
| focus the bootstrap composer box | click (700, 116) | KEPT — no binding focuses a text box; coordinate re-checked |
| focus the docked composer box (x2) | click (575, 684) | KEPT — same reason; re-checked in BOTH composer modes |

One `xdotool key Down` replaces both retired clicks: ↓ steps the flattened
roster and lands via `focus_agent`, which sets the focused workspace AND the
selected agent. Every surviving click is tagged `CLICK (no binding)` in
stories.sh with the widget it targets and why no key covers it; yogdrive.sh's
`click` verb header now says it is the last resort. STORIES.md's "Selection
must not ride on pixels" paragraph is rewritten from a should into the rule
the runner follows.

VALIDATION (real drive, this worktree's release build, Xvfb :99, scratch world)
Full log: docs/drive-logs/2026-07-25-s0-s1-keyboard-selection.md

| Beat | run A (`bz` 0.0.4, as installed) | run B (`bz` 0.0.3, pinned) |
|---|---|---|
| S0 seeded-skip — no `lernie prime` | PASS | PASS |
| S0 bare-start — `lernie new` fired | PASS | PASS |
| S0 bare-start — detached `lernie prompt` | PASS | PASS |
| S0 payoff — wire reply on disk | FAIL (bl-f69b) | FAIL (bl-f69b) |
| S1 message-to-agent — `lernie message` | FAIL (no conversation exists) | PASS |
| S1 restart — idle is pure (INV-1) | PASS | PASS |
| S1 prompt-existing — no re-mint | PASS | PASS |
| S1 prompt-existing — new root agent | FAIL (no conversation exists) | PASS |

No previously-passing beat regressed. Run B's ops.jsonl reads `lernie new` /
`lernie prompt` / `lernie message <agent-id>` / `lernie prompt`
— the message names the root the first prompt minted, and that agent's
messages/ holds 001-user.md + 002-user.md. s0-04-transcript.png shows the row
selected, the transcript painted and the composer reading "message the selected
conversation" with the mouse still parked at the S0 composer click — the key
alone did it.

Run A cannot evaluate the fix: the bl-f69b skew has become fatal EARLIER than
on 2026-07-24 — with the installed `bz` 0.0.4, `lernie prompt` now refuses at
the version gate and creates no agent directory at all, so the world has zero
conversations and both conversation-shaped beats fail for want of a subject.
Run B put a scratch-local `cargo install brazen --version =0.0.3 --root
<scratch>` on PATH (the user's installed bz untouched) to get a world with a
conversation in it. The reply still never lands there either ("adapter stream
ended without a terminal `end`", 0-byte response.json), so the S0 payoff stays
red under both binaries. Both failures are bl-f69b's and out of scope.