+++
title = "README and story docs still describe the pre-W11, pre-wall substrate"
created = 1786510256
updated = 1786510256
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
tags = ["docs"]

[[blockers]]
id = "bl-49c6"
on = "claim"
+++
The published/operator docs contradict the current binary and DESIGN §16.2:

- `README.md` says lernie is still an external binary and must be installed. Cargo pins embedded lernie `=0.0.6`; `yog --help` exposes `yog lernie`; `src/multiplex.rs` says W11 is filled.
- `README.md` says brazen config, credentials, and model cache stay ambient-shared. They now live under each workspace wall.
- `README.md` says the world bl shim refuses prime, sync, and install. The W9 refusal is gone; `yog exec --cwd <scratch-repo> bl prime --as sparrow` exits 0.
- `docs/STORIES.md` still says `yogdrive.sh` links scratch ambient credentials because brazen stays shared.
- `scripts/drive/beats_s7.sh` still claims the embedded lernie pin is `=0.0.3`.
- repo `AGENTS.md` also names `lernie = "=0.0.3"` while Cargo uses `=0.0.6`; that file must not be edited without the operators explicit authorization.

Sweep user-facing substrate claims after bl-49c6 establishes the corrected drive layout. Derive every version from Cargo rather than copying a stale pin where possible. Do not edit AGENTS.md unless the operator separately authorizes it.