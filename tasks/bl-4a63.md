+++
title = "W5: phase-1 version gate + toolchain pane"
created = 1784435200
updated = 1784435200
parent = "bl-1a3c"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-c68f"
on = "claim"
+++
DESIGN §16.6 W5. At startup, probe each host tool where a probe exists (bz --version; bl and lernie lack --version today — gate on presence/resolvability) and compare against the tested tuple (constants in the module); surface the toolchain state as a read-only UI pane; mutating verbs refuse on a detected mismatch while rendering continues. Effect- and clock-injected per the house pattern. EXPLICITLY phase-1-scoped: phase 2's exact-pinned crates make the version definitional and delete this gate (record that in the module doc, §16.4). Files: src/world/toolgate.rs (~150), a shell pane (excluded). Gate as always.