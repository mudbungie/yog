+++
title = 'the composer says "message the selected conversation" — it should name the conversation'
created = 1785201348
updated = 1785374811
claimant = "entrance-2f30"
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
tags = ["usability"]

[[blockers]]
id = "bl-68d9"
on = "claim"

[[blockers]]
id = "bl-1b5a"
on = "claim"

[[blockers]]
id = "bl-df65"
on = "claim"
+++
From the 2026-07-27 UX-testing session (the operator):

> the prompt, "message the selected conversation" should be "message
> <session name>"

RESOLVED by bl-df65 (superseding the interim bl-68d9 answer): a
conversation now has a REAL minted name (`You are <name>.` stamped in its
goal, DESIGN §3.3), and its display name is the §3.3 three-rung ladder —
stamped name → first payload line → root id — one function, built by
bl-1b5a (this ball needs it). DESIGN §11:

> **The composer names its message target by the conversation's display
> name** (bl-2f30): the `→ message <display name>` target line and the
> box's hint are two spellings of one fact, both from the §3.3 one
> function — never the raw agent id alone (the id survives weak in the
> center header).

## Today

`src/shell/input_bar.rs` — the hint text is the generic "message the
selected conversation", and the target line above it prints the raw agent
id (`→ message <agent-id>`).

## When unblocked (bl-1b5a delivered)

Use bl-1b5a's display-name ladder function in both places — the
`→ message <x>` target line AND the hint text (`message <x>`). The
selection can be a CHILD agent (`selected_branch` is the selected member,
not the root): resolve the conversation via `nav::convs::root_of` and
display *its* name; the message still targets the selected agent.

Acceptance: with a conversation selected whose goal is stamped
`You are <workspace>.`, the target line reads `→ message <workspace>` and
the hint matches (id nowhere in the composer); an unstamped legacy
conversation falls through the ladder (first payload line, then id); with
none selected the hint stays "start a conversation". `make check` green.