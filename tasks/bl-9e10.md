+++
title = "drive beat for the unseeded first turn: S0 step 5 has never been driven by any wire beat"
created = 1786683730
updated = 1786685938
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"

[[blockers]]
id = "bl-dd7f"
on = "claim"

[[blockers]]
id = "bl-3b62"
on = "claim"
+++
Ruled at bl-9b52 (operator ruling relayed 2026-08-13; question 4 decided by the architect): yes, the unseeded first turn gets a beat.

Every wire beat in scripts/drive/ seeds the wall before launch (bl-1851), so the harness has only ever driven S0 step 3 (reply streams) and never step 5 (auth-failed step renders with a Login affordance one click away, §13.3/§8.3). bl-55d8 (rendered reason for empty response.json + non-empty stderr.log) is CLOSED, so the assertion surface exists.

Blocked on the remedy balls landing (this ball --needs them) so the beat asserts the ruled end state, not a moving surface: drive a genuinely unseeded wall, first dispatch dies on credential, assert the rendered reason AND the remedy affordances (Login for auth-kind per bl-3b62; §9.1-editor path for config-kind per bl-dd7f). Vacuous-assertion rules apply (bl-f16e): prove the beat bites by reverting the gesture.

---

gate probe: bl-1007's scoped scan is now live at the machine layer too (userconf patched 2026-08-13)

---

gate probe 2: confirming scan lines on stderr

---

gate probe 3: post-unwire — the machine-layer scoped gate is the one scan
