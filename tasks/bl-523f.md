+++
title = "the config family carries its workspace inside target, and the workspace-slot table does not read it: a config act at a renamed remote workspace edits the wrong wall's file"
created = 1788068544
updated = 1788146042
claimant = "OrderCrier"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
Found during the seat severance (seat store bl-4a36 is the twin; fix both sides or neither — the two tables must stay in agreement). The boundary's workspace-slot table reads a gesture's workspace address from top-level workspace or prepared.workspace; the config family instead carries its workspace inside target, which the table does not read. Consequence in yog's own window today: a config act aimed at a workspace held on another box under a local rename resolves to no entry, falls through to the local engine, and edits the local wall's file — a silent wrong-target write, the §8.2 rename crossing every surface except this one. Fix is one row in the table (read the config family's target as the address) mirrored exactly in the seat's table, with a test on each side proving a renamed remote config act lands remotely.