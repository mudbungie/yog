+++
title = "DESIGN 12's module map is missing 23 source files, and 'minted workspace' survives in src prose that 3.1 forbids"
created = 1786677690
updated = 1786683641
claimant = "Dredge"
priority = 3
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
tags = ["publication"]
+++
Found while verifying bl-a4e0's "DESIGN is kept in sync with the code" bullet
against the tree. Both halves are the same failure — a doc-to-code drift nothing
machine-checks — and both were left out of bl-a4e0 deliberately: the first is 23
review-worthy rows, the second is an editorial sweep bl-2368 just finished a
pass over.

**1. §12 has no row for 23 tracked non-test source files.** DESIGN §12 is the
module map and the line budgets, and AGENTS.md says "Over the cap? Split along a
real seam and add the row to DESIGN §12" — so a file with no row is a split that
never got recorded. Reproduce by expanding §12's brace spellings and
suffix-matching against `git ls-files src`. Today's list:

    src/app/memo.rs
    src/cli_outbound/{exec,resolve,stream,streamed,sys}.rs
    src/config_edit/{effects,pipeline}.rs
    src/config_edit/branch/edit.rs
    src/git_tree/{descent,detect,enumerate,lock_probe,probe_cache,probe_stack,state,tools}.rs
    src/projects/{join,runner}.rs
    src/shell/{fire,config_marks}.rs
    src/shell/config_edit/status.rs
    src/shell/acceptance/masthead.rs

Note `src/cli_outbound/sys.rs` in particular: AGENTS.md rule 3 pins every
`unsafe` in the crate to that file, and the module map does not list it.

The deliverable is either the 23 rows, or — the more elegant answer, and the one
worth attacking first — a test that makes the map an invariant the way
`tests/design_citations.rs` made citations one: every tracked non-test file
under `src/` resolves to a §12 row, both directions (a row naming a file that no
longer exists fails too). §12's own brace spellings are the parsing problem to
solve; if they cannot be parsed reliably, that is an argument for changing how
§12 spells paths, not for skipping the guard.

**2. "minted" still describes workspace names in src prose.** DESIGN §3.1 is
explicit: "A workspace name is chosen or `home`, never minted" — minting names
conversations (§3.3). The doc comments say otherwise, including in the module
that owns the question:

    src/binding/mod.rs:6,37,48,128,164   (the §3.1 names-root authority)
    src/boundary/answer.rs:237           ("ws's minted name")
    src/actions/verbs/balls.rs:120, src/app/echo.rs:67, src/app/focus.rs:65,
    src/app/derive/sweeps.rs:63, src/shell/delete.rs:186, src/shell/ram.rs:248

All are doc comments and test prose — no behaviour, no coverage impact. bl-a4e0
fixed the same falsehood in README; this is the rest of it.

Source: bl-a4e0 verification pass, 2026-08-13, at tree d736e9e.

---

Both halves done at HEAD 5dd5940d (Dredge), plus the guard the ball asked for first.

**Re-derived, and the count had drifted: 35 unlisted files, not 23.** The ball's list was right as far as it went; since filing, these appeared with no §12 row: src/actions/mod.rs, src/actions/verbs/dispatch.rs, src/boundary/sugar/argv.rs, src/config_edit/mod.rs, src/fs_watcher/mod.rs, src/git_tree/cmd.rs, src/login/{mod,auth}.rs, src/test_support{.rs,/workspace.rs,/world.rs}. Every one now has a row with its actual line count and a clause taken from the module's own doc header. src/cli_outbound/sys.rs is listed as the ball asked — 'the crate's single confined unsafe ... the one audited home rules/unsafe-outside-sys.yml leaves it (AGENTS.md rule 3)'.

**One defect in the other direction, which the ball predicted:** §12 spelled 'src/theme/{mod,badges,mark,role,tests}.rs' but src/theme/tests.rs is now a DIRECTORY (tests/mod.rs 283 + tests/tone.rs 30) — a row naming a path that does not exist. Respelled with true counts.

**The elegant answer, taken:** tests/design_module_map.rs (168 lines) holds the map as an invariant in both directions — every production .rs under src/ has a row; every src/ path a row spells exists — with brace expansion (a{b,c}d, x{.rs,/y.rs}, nested), test corpora excluded per §12's own rule, and a non-vacuity guard in design_citations.rs's style. §12's prose now says the table is machine-checked and that a brace spelling the guard cannot expand reads as a missing row.

**One code change to make the parse honest:** src/fs_watcher/roots_tests.rs was a test corpus attached with #[path], violating §12's stated 'X.rs pairs with X/tests.rs' seam and unrepresentable in the guard. Renamed to src/fs_watcher/roots/tests.rs and the #[path] attribute deleted.

**Part 2 — two of the listed sites were false positives.** src/app/echo.rs:67 and src/app/focus.rs:65 say 'the minted §3.3 name', which is the CONVERSATION mint and correct; left alone. Fixed the genuine workspace-name sites: binding/mod.rs:6,37,48,128,164, boundary/answer.rs:237, actions/verbs/balls.rs:120, app/derive/sweeps.rs:63, shell/delete.rs:186, shell/ram.rs:248 — plus five the ball's list missed, found by sweeping 'mint' against workspace/sphere/wall: app/balls/tests/prepare.rs:91, shell/acceptance/walls.rs:232, shell/acceptance/modal.rs:20+162 (a test FUNCTION named ..._and_mints_nothing, now _and_creates_nothing), shell/acceptance/smoke.rs:26, shell/acceptance/fixture.rs:75, start/mod.rs:115. 'Minted' now survives in src/ only for conversation names (§3.3), the wall-clock stamp, and §3.1's own 'not a mint' denial.

make lint green (line-cap, leak-scan, clippy --all-targets -D warnings, rules-audit, cargo-deny); full cargo test green.

Not fixed, for the record: §12's line-count column remains hand-maintained and mostly stale — the new rows carry true counts at HEAD, the old ones were left alone. That is bl-0012's second defect and still wants a ruling (delete the column, or generate it). The guard checks paths only, deliberately: a count guard would have to agree with make line-cap, which is already the cap's one authority.
