+++
title = "config form fields truncate mid-token at a maximized window while 1700px of the pane sits empty beside them"
created = 1786163325
updated = 1786514723
claimant = "Ember"
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
QUALITY.md §1 criteria **G1** ('Nothing clipped') and **G4** ('wastes no dead field at the large one'). Audited sha 4b0e75c, run /home/u/.cache/yog-drive/quality-20260807T214407Z/out.

SYMPTOM. The §9 config editors lay their value boxes at a fixed width that ignores the window. Maximized to 2560x1700 the forms occupy a column roughly x=260..560 — about 300px of a 2300px-wide pane — and values are still cut off inside those boxes.

WITNESS: `Q-S5-max.png`, crop `crop-s5-caps.png`. The `claude-sonnet-5` row's `capabilities` field reads:

    tool_use_native, prompt_caching, streaming, stop_

— hard-cut mid-token, no ellipsis, at the LARGEST capture, with ~1700px of unused pane immediately to its right. G1 and G4 in one row: the space that would un-cut it is right there and unused.

REPRODUCTION:
  1. scratch world, launch yog, CLICK '⚙ Config' in the side panel (mode selection is a view, so a coordinate is lawful here per the harness STEERING RULE)
  2. scroll the centre pane to the top
  3. `xdotool windowsize <wid> 2560 1700`
  4. read the 'lernie global config' section's claude-sonnet-5 capabilities field

NOTE for the fixer: at 420x320 the same forms are unusable for the opposite reason (see the min-size ball filed alongside this one) — the boxes want to be width-responsive in both directions, not merely wider.

TRIAGE ONLY — filed by the first quality audit, not fixed by it.