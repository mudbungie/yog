+++
title = "attention strip: '⏭ next' is a mystery no-op when nothing stirs — state its job, disable it when there is no target"
created = 1785287538
updated = 1785306261
claimant = "entrance-e266"
priority = 4
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator, live 2026-07-28, did not know what the next button does, and never saw the strip say anything but 'nothing stirs'. src/shell/navigator.rs top_bar: '⏭ next' jump_next_attention sits beside the ⚑ strip; with zero attention it silently does nothing. Fix: disabled when strip_total()==0, hover text stating its job ('jump to the next conversation needing attention'), and consider whether the pairing (strip legend + next) reads as one unit. Cite the §11 glyph doctrine. Also worth verifying attention states actually fire in live use (auth-failed flags it — anything else?) — if attention is near-unreachable, say so on this task.