+++
title = "REMOTE read path: the accessory tail — every remaining in-process read is an unspelled Reply (mark seats, flight strip, the ball-and-spend family, fire-time gates, altitude-0 chrome)"
created = 1786770233
updated = 1786770241
claimant = "Gunwale"
priority = 2
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
bl-48ae (acec70d2) corrected REMOTE 9.7's residual: the named surfaces have all crossed or been ruled on, and what remains is the 11 accessory tail nobody listed — none blocked, none needing a ruling, each a Reply that does not exist yet. From that landing, the set: mark_seats, flight_strip, the 3.2/3.5 ball-and-spend family (conversation_ball, ws_balls, ball_spend, conversation_spend, conversation_context, focused_join, bound_ball, roster_ball_rows), focused_pending, agent_titles, config_tip, the fire-time gates (delete_confirmation, agent_delete_confirmation, move_targets, conversation_names), and the altitude-0 chrome (tab_bar, strip_total, activity, staleness, has_alarms). startable/resumable stay excluded (the acts side of bl-adcb's line).

Deliverable: spell each as a boundary Query/Reply (all serializations, the compile gates enforce), answer at the chokepoint, migrate the surface, delete the accessor — the standing three-move discipline. Natural groups if one ball cannot hold it: (a) the ball-and-spend family, (b) altitude-0 chrome + staleness/alarms, (c) fire-time gates (NOTE: a fact that gates a gesture is read off a landed answer, never awaited at click — bl-48ae's rendering ruling; and fire-time confirmations re-derive fail-closed at the chokepoint, which they already do engine-side), (d) the misc singles. Where a fact is already carried by a landed answer (the forest, Clients, Board), fold it there instead of minting a duplicate query — one fact one home. Honest-scope license; record what remains in REMOTE 9.7 citing this ball; file the follow-up if surfaces remain. Verify against the tree; this body drifts.