+++
title = "inbox-composer surface: pending deposits queue above the draft, derived fold line, structural snap-down"
created = 1785733453
updated = 1785733610
claimant = "inbox-composer"
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-929d"
on = "claim"
+++
Implements the bl-929d inbox-composer contract — VERIFY docs/DESIGN.md §11 (the inbox-composer bullet) on main before editing; where this body and DESIGN disagree, DESIGN wins. The composer surface becomes a queue: the target agent's pending inbox deposits (§5.1 #11, the derivation the ✉n accessory and Inbox tab already read — a third seat, never a second representation) render oldest-first above the text box, one line each with the jsonview fold arrow; the input is the queue's last item and has no arrow. The typed draft stays the per-target RAM draft (OPERATOR-RULED: deposit at send — it is not a substrate fact until Enter; Enter fires the unchanged verb). The fold line is derived, never stored: position = content height (items + wrapped draft), floor = bare input row, cap = half the pane with tail-anchored scroll via src/tail.rs; empty inbox is the general path (a queue of zero items, no flag). Snap-down animation is render-layer viewport ephemera (§13.1) triggered STRUCTURALLY by the pending count dropping on delivery commits — one path covers Enter, a live driver's drain, lernie scan, another instance's send. No frame-time IO: arrivals surface via the existing off-thread enumerate over the watched inbox/ root. No new Action variant, no new verb, no new knob (bl-8aab); pending-item fold overrides are RAM keyed by inbox path (§5.3) and die with the pending row. In-flight strip seat preserved: above the fold line, never inside the pending region.