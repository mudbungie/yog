+++
title = "spec: every conversation row is a subtree — amend DESIGN §11 anatomy/altitude-0, cross-ref §5.1 #8, reconcile the expand key with the keys doctrine"
created = 1786511694
updated = 1786512496
claimant = "Fretwork"
parent = "bl-fa82"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-b9e3"
on = "claim"
+++
Subtask of the expander epic (read the parent's body first — BOTH operator
rulings are quoted there verbatim and are the authority). **Doc-only — no
code in this ball.** The doc rules first; derivation and paint follow it.

**1. §11 altitude 0, the list definition + row anatomy (docs/DESIGN.md:3778-3787).**
"one row per root agent" becomes: one row per *visible* member of the descent
forest — a row is the subtree rooted at its agent, collapsed rows speak for
their whole subtree (the existing aggregations: state InFlight>Live>settled,
attention flag, flight class, age = subtree-latest), and the default
all-collapsed state IS the former list, byte for byte. Add to the trailing
metadata group: the **subagent field** — ▶/▼ arrow + direct and total counts,
hover stating what each number means — present only when the agent has
descent-id children. Child rows: same anatomy, indented per depth, with a
reply-elbow glyph (the operator's "little chat-reply line"; pick the glyph —
↳ or └ — and record it; note the crate has no elbow idiom yet, only two-space
monospace indent, src/shell/members.rs:52-55). State that the row's prefix
group still must not grow a conditional element (bl-b9e3's rule) and that
indentation moves the whole row, establishing a per-depth title edge.

**2. Expansion state is viewport ephemera (§13.1/§5.3).** A keyed set
(agent id) in ShellState, precedent activity_open (src/shell/ram.rs:165-168)
and Ephemera (src/inspector/mod.rs:36-45) — deliberately NOT egui
CollapsingHeader memory and NOT the durable ui.json collapsed array
(src/ui_state/fields.rs:79-93 — that idiom is for named sections; N-per-row
ids would accrete stale keys). Record this in §13.1's viewport-state list.

**3. The keyboard — the operator ruled it (parent body, second ruling).**
- ↑/↓ moves the selection through the **visible** list rows in paint order.
  A collapsed subtree is skipped: down from a collapsed parent lands on the
  next row at the same level. The walk NEVER expands anything.
- → expands the selected row. ← collapses it; ← on a child (or a row with
  nothing to collapse) pages up to the parent row.
- This REPLACES the current ↑/↓ roster-walk semantics for this seat
  (attention-ranked over every generation and workspace,
  src/attention/roster.rs:89-109, src/app/focus.rs:136-140). Amend §6's
  roster statements and §11's row hover text ("↑ / ↓ walks the roster onto
  it", src/shell/conv_list.rs:186-190) to match; decide and record what the
  attention-ranked order still serves (a jump-to-attention gesture, the
  strip?) and what a deliberate jump does when its target is inside a
  collapsed subtree — the WALK never reveals, but a jump is not the walk;
  decide, write it down.
- Reconcile the keys doctrine (src/shell/keys.rs:10-15 and its §11
  statement): "which row to expand" stays a pointer-choice statement, but
  ←/→ on the SELECTED row is 'a key fires a verb on the target the selection
  already names' — legal under the doctrine's first rule. Verify ←/→ are
  free in keys.rs (no workspace/pane binding collision) before writing them
  into the doc; if taken, name the conflict and the resolution. Hovers must
  name the combos (bl-478d).

**4. Cross-refs.** §5.1 #8 (docs/DESIGN.md:1441): membership stays the strict
descent-id tree; altitude 0's expansion is now a rendering of it; the context
edge stays bl-5cf8's open question — update that row's trailing cross-ref.
§6: the member-count `(N)` is replaced by the subagent field; check no §
still describes `(N)`. Grouped-by-ball view: expansion applies within groups.
Boundary: conversations answer stays root-rows; if the derivation adds a
`direct` fact to ConvRow, decide here whether the boundary encodes it (a
machine surface gains a fact cheaply — recommend yes) — record the decision.
**Altitude 1's members tree (src/shell/members.rs):** record in §11 that it
is now a second rendering of the same membership, file ONE follow-up ball
naming the decision (keep/retire), touch nothing.

**5. Pre-split (§12).** src/nav/convs.rs is at 248, row.rs 208,
conv_list.rs 217 (shell cap 250). Name the new seams in the §12 table:
src/nav/convs/expand.rs (the visible-rows flatten) and, if paint projects
over 250, a conv_list split. Update the stale §12 figures you touch.

**Done when:** §11, §5.1 #8, §6 and §13.1 read consistently; the walk and
←/→ semantics and the doctrine amendment are recorded; grep docs/ for 'one
row per root', the member-count phrasing, and 'walks the roster' finds no
stale statement (drive-logs are history, never edited); pre-commit gate
passes.