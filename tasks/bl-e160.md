+++
title = "attention escalates to a desktop notification when the window is not focused"
created = 1785649521
updated = 1785649521
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Source: codex comparison survey 2026-08-01. VERIFY premises before editing.

## Gap
The attention strip (DESIGN §6) is yog's core promise — "does anything need me?" — and it is invisible whenever the window is buried or minimized. Codex ships notification backends (terminal bell, OSC 9, coalescing by priority — codex-rs/tui/src/notifications/) precisely for approval-needed/turn-complete while the user is elsewhere.

## Scope
- When the attention count RISES (a new attention fact by the §6 predicate, not a re-derivation) and the window lacks focus, emit one OS notification naming workspace, conversation, and rule.
- Dedupe rides the existing seen watermarks — no second attention model, no new state class.
- Off the frame thread (bl-ee0a isolation); no substrate write, I7 untouched (a notification is render output, not a mutation).
- Severable knob in ui.json (§4.1 discipline): on/off, default decided in the ball.
- Verify: eframe focus signal availability; a notification dependency (e.g. notify-rust) against the pinned-deps/cargo-deny policy — if the dep fails the audit, the ball's answer may be "no".