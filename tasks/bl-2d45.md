+++
title = "S6 stop beat is stale: it aims one down-arrow at a cleared selection and a running-first sort, and neither exists since bl-49cb/bl-cad5"
created = 1786162680
updated = 1786162680
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
tags = ["drive"]
+++
Found by the 2026-08-07 re-baseline drive (bl-c63e), docs/drive-logs/2026-08-07-ladder-rebaseline.md. Reproduced identically on two runs of `scripts/drive/stories.sh run-s3s4s6` against release `aafa438`.

Beat: `S6 stop: lernie stop dispatched` (STORIES S6, §8.2), `scripts/drive/beats_s3s4s6.sh:143`.

    S6 stop: lernie stop dispatched                FAIL — no stop verb

## Premise, quoted from the beat

> ↓ from a cleared selection lands on the §6 sort s head — the in-flight root (running outranks settled) — and `x` is §11 s Stop on that selection (§8.2)

## What actually happens

Screenshot `s6-07-inflight.png`: the selection is on **quarrel** — settled, already `delivered`, 22s old, badged `bl-5812` — while **talon**, the in-flight root at 1s still streaming its `Print the numbers 1 through 2000, one per line.` reply, sits ABOVE it unselected. `x` correctly finds nothing to stop and writes no `lernie stop` row.

The `bare` idiom is NOT at fault: `s6-08-stirs.png`, taken after all five `x` retries, shows the composer draft still EMPTY (placeholder `message quarrel`). The `x` presses were real bare keys, not swallowed text.

## The premise moved twice, both on 2026-08-01

1. `91974d9` "sending a start goal leaves the view on the new-conversation placeholder, not the started conversation [bl-49cb]" — a start now SELECTS the conversation it started. `src/app/focus.rs:62`: "Claim the conversation a fired start just minted (§3.4): **a start focuses what it started**, and what it started is a conversation, not merely the workspace it resolved." docs/DESIGN.md:666 makes it explicit: "The rule runs all the way down to the **conversation** (bl-49cb)". So the preceding beat s new conversation was ALREADY selected and the single down-arrow moved OFF it.

2. `e193bca` (bl-cad5) — the conversation list no longer ranks running above settled at all. `src/nav/convs/row.rs:107`: "§11 sort (amended, bl-cad5): recency alone — last action of any kind, descending — then root id for a deterministic tail (I9). Attention and liveness are badges here, not ranks". docs/DESIGN.md:1565: "**The conversation list is no longer one of these groups** — since bl-cad5 it sorts by recency alone (§11); attention there is a badge and a count, not a rank."

So "the §6 sort s head" is not a thing this list has any more, and the beat s one down-arrow is aimed at a ranking that was deleted.

## Repair

The beat needs a way to put the selection on a KNOWN in-flight conversation rather than inferring it from a rank. Options, none endorsed: address the conversation through the DESIGN §8.5 control boundary (`gesture`, which names its target outright and needs no seat or selection) rather than by walking the roster; or drive down-arrow until the header names the intended root. Prefer the boundary — VISION §4.8: "story beats address the headless surface, and screenshots become what they always should have been, visual confirmation, not the transport."

Fix the beat comment too: it cites a §6 rank that DESIGN now explicitly says does not apply here.