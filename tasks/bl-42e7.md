+++
title = "text size (zoom) not preserved between launches"
created = 1785644945
updated = 1785644945
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator report 2026-08-01: adjust text size in yog, quit, relaunch — size resets. Persist it with whatever mechanism yog already uses for window/app persistence (eframe persistence or yog's own config under the world dir — find the existing single source of truth for persisted UI state and add zoom to it; do not invent a second store). Regression test the round-trip.