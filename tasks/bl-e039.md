+++
title = "W4: the no-marks knob — shared store default + stealth / custom branch"
created = 1784435200
updated = 1784435200
parent = "bl-1a3c"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-c68f"
on = "claim"
+++
DESIGN §16.6 W4. The nested balls clone tracks the project's shared balls/tasks store branch by default (the coordination point with ambient bl); a per-project knob repoints it through bl's own config surface — stealth (bl conf set task-remote none) and/or a custom task-branch (bl conf set task-branch <name>) — surfaced in yog's UI as a project setting that DRIVES bl conf (invoked in the world env, cwd = project), never a yog config file (severability: the knob lives in bl's landing). Render the stealth trade plainly (claims invisible to ambient bl list; custom branch = parallel task universe). Read current knob state via bl conf <key> (parse provenance from stderr or use the dump). Files: src/world/marks.rs (~120), a config-pane shell touch. Gate as always.