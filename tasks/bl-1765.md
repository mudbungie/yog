+++
title = "a ball with no conversation renders the whole workspace's spend as if it were the ball's own, indistinguishably from a real per-ball figure"
created = 1786163332
updated = 1786515029
claimant = "Hessian"
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
QUALITY.md §1 criterion **H1** ('One fact, one rendering. A fact paints once per surface'). Audited sha 4b0e75c, run /home/u/.cache/yog-drive/quality-20260807T214407Z/out.

READ bl-afc4 FIRST — DO NOT 'FIX' THIS BY INVENTING A LINKAGE FACT. bl-afc4 (closed) carries an operator ruling: *'the ball-to-conversation link is the goal stamp, which exists for board-started drones; a ball claimed mid-conversation records no conversation link (STORIES' honest limit) — ACCEPT workspace-granularity attribution for unstamped claims; do NOT invent a linkage fact.'* The FALLBACK is ruled correct. This ball is about the fallback being **unlabelled**: nothing on any surface distinguishes 'this ball spent N' from 'this ball has no stamped conversation, so here is the workspace's N'.

MECHANISM (read, not guessed): `src/board/rows.rs:95` `stamped_roots` returns the roots whose `goal_ball` is this ball — empty for a ball with no conversation. `crate::spend::select` then hits its empty-roots branch, `if roots.is_empty() { return bills.to_vec(); }`, whose own in-crate test asserts *'no roots is the whole workspace'*. So the empty-selection sentinel ('no filter') is being read where the caller means 'this ball has nothing'.

WITNESS 1 — two ball rows carrying the identical figure, `Q-S13-default.png` / crop `crop-s13-board.png`:
    gated   bl-b5ce   epic:  budget 8610 tok  (in 4468 · out 2606 · …
    claimed bl-059c   spend: budget 8610 tok  (in 4468 · out 260…
bl-059c was claimed seconds earlier and had no conversation at all; 8610 is the workspace total, spent entirely by an unrelated bootstrap conversation ('growing') bound to no ball.

WITNESS 2 — the same number twice on consecutive lines of ONE header, `probe-s7-sel.png` / crop `crop-s7-budget.png`:
    bl-059c:  budget 8610 tok  (in 4468 · out 2606 · cache r 1536 · w 0)
              budget 8610 tok  (in 4468 · out 2606 · cache r 1536 · w 0)
byte-identical, one labelled with the ball and one not. That is H1 literally.

WITNESS 3 — the control that proves it, `Q-S18-spawn-default.png`: once the armed fleet started a conversation ON bl-7cdd, that ball reports its OWN spend (2091 tok) while bl-059c, still unstamped, reports 10701 = 8610 + 2091, i.e. the whole workspace including another ball's drone.

REPRODUCTION: seed a project with a ready ball and an epic; launch; bare s to claim the top ready row WITHOUT sending the start goal (Escape it); read the balls board's spend rows against a workspace whose spend came from an unbound conversation.

SUGGESTED SHAPE (not a mandate): the fallback needs its own word on the row — the figure is the workspace's, so say so — rather than a per-ball label over a workspace-granularity number.

TRIAGE ONLY — filed by the first quality audit, not fixed by it.