+++
title = "run the fleet on yog: the admiral loop, drones, and the operator surface (VISION §4 re-scope)"
created = 1785479112
updated = 1785646838
claimant = "Riffle"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
The fleet demo (souls, per-role tool grants, mock Slack tool triples, cycle scripts, live e2e) proved lernie hosts the fleet-setup design as pure configuration; it lives at ~/ops/fleet. This ball is yog driving the real thing. **Re-scoped 2026-08-01 by docs/VISION.md (bl-e722, commit 153210d) §4.1** — which is the authority for what follows; where this body and VISION disagree, VISION wins and this body gets amended.

## Standing decisions (operator, 2026-08-01; VISION §4.1 confirms them verbatim: *"Its recorded decisions stand: yog becomes the clock; cadence lives in yog; lernie stays schedulerless."*)

1. **yog becomes the clock.** Timers in yog's backend (off the frame thread per the bl-ee0a isolation doctrine, landed 0ebb445); lernie ARCH §8 stands.
2. **Cadence lives in yog** — persisted yog settings, surfaced as typed rows in the overhauled config pane (bl-c225, landed 18ec0c4).
3. **Version upgrades and cross-repo work authorized** (operator verbatim: *"spider out into lernie and balls and brazen as needed"*).
4. World state wiped 2026-08-01 (bl-8f17); bring-up starts from fresh workspaces.

## The re-scope (VISION §4.1) — what the clock drives

Bounded agent lifetime is adopted: an agent lives one ball. The five-role cast maps:
- **Coordinator → the admiral**: yog's level-triggered loop (§4.3) + the operator conversation.
- **Shepherd → dissolved.** Disambiguation vanishes with bounded lifetime; verification becomes close gates and derived reads; reaping becomes timestamp/pid comparisons.
- **Sensor → survives only where an outward surface exists** (Slack); read-only relay, one speaker per surface.
- **Builder → the drone**: one ball, claim → work → commit → discharge gates → close → exit.
- **Steward → the human + yog's config surfaces**; doctrine is skills/templates seeded at spawn, proposals are balls.

## The clock questions — resolved by the §4.3 reframe

- **Watchdog vs reactor: dissolved.** The loop is level-triggered — each tick reads the board (Catalog load) and liveness (locks, pids, leases), brings drone count to match ready work under a policy cap, reaps by comparison. Ticks are derived reads, no model calls; model spend happens only at spawn, when ready work exists. The ≥2-calls-per-idle-cycle problem belonged to the dissolved shepherd's cycles. The sensor is the one surviving cycle-shaped consumer, and only where an outward surface exists.
- **Overdue surfacing: mostly dissolved.** *"A missed tick is self-healing because the loop converges from whatever state it finds."* Remaining surface: render last-tick/next-tick and loop-armed state; no second clock outside yog (a second clock re-creates the drift problem).
- **I7 preserved**: fleet mode itself is the explicit user action — the operator arms the loop per workspace; an armed loop's spawns are that action continuing.
- Former GAPS now answered: push verification = close gates + the loop's derived reads (ex-shepherd job); Slack = the sensor's outward surface, transport is the deployment's choice (Slack MCP today), yog renders relays as inbox facts.

## Scope of THIS ball (per VISION §6 'yog' and §7)

- **Shipped-scope first (VISION §7.3 verbatim):** *"the clock exists first as cadence for watcher cycles (bl-3381's shipped scope), grows spawn/reap when drones are startable end-to-end from a board click."*
- Then the §4.3 loop proper: arm-per-workspace, tick = read board + liveness, spawn to policy cap with skills/model selected from ball tags at fire (§4.2/§4.6), reap by comparison, surface decisions. The loop spawns and reaps; it never diagnoses.
- Operator surface: channel/inbox visibility, coordinator exchange, per-watcher/loop tick state.
- NOT this ball: the V-rungs, §4.5 spend join, §4.6 policy table — each files as its own ball (VISION §6). Refusals list (§8) binds: no queue, no supervisor, no warm pool, no new lernie primitives.
- Inter-agent addressing: bl-50f3 (claimed @naming-arch) feeds the surface.

## Sequencing (VISION §7)

Recording this re-scope = §7.1, done with this update. Loop cadence scope can start now; fork/config verbs and V2/V3 ride the lernie release train (PR #4 v0.0.4 still open); V4's armed loop lands behind the V-rungs and gates.