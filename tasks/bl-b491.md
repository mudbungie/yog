+++
title = "empty-balls hint truncates the paved-path command: 'add one with: yog e…'"
created = 1785646880
updated = 1785646880
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
At default width the balls section's empty state renders 'No projects yet — add one with: yog e…' — the whole point of the hint is the exact command (S3-T5: the view-model carries the 'yog exec bl prime' hint) and the ellipsis eats it. The command must be readable and selectable/copyable at the default window size: wrap it onto its own line, or elide the middle of the path portion never the verb. Acceptance: default 1150x760 launch, zero projects — the full 'yog exec bl prime' text is visible verbatim.