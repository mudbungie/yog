+++
title = "confined drones cannot deliver a ball bound to a project repo outside the world"
created = 1786846053
updated = 1786936526
claimant = "Confine34b1"
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
bl-bca4 wired the Linux confinement backend (bubblewrap) with a derived writable set of exactly the workspace, the composed world root, and the host /tmp — DESIGN §8.6 records this as a stated v1 bound: a project repo outside the world is read-only to a confined drone, so the drone's own bl close (the ball rung's delivery) fails against the read-only rebind.

Required outcome: derive the project repo into the writable set where a birth carries one, without storing it. The prompt door knows the payload's project at fire time; a revived driver (lernie message / advance) does not, so the derivation must come from facts yog owns per workspace — the §3.2 claimant join or the §3.4 adoption — or the bound must be accepted and stated for revivals too. No new config: the writable set is a derivation, not a field. The support-boundary test in src/control/confine/tests.rs is the place to extend (a write into the bound project repo lands, a write beside it does not).