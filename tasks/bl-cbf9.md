+++
title = "B2: unsafe elimination + sys.rs confinement + unsafe-location ast-grep rule"
created = 1784433623
updated = 1784433623
parent = "bl-97fb"
priority = 1
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Goal: exactly one unsafe remains, confined. (1) spawn_detached: replace the unsafe pre_exec setsid with safe std::os::unix::process::CommandExt::process_group(0) (stable since 1.64). Rewrite the detach test's session assertion to process-GROUP: `ps -o pgid= -p $$` (portable linux+mac; the sid assertion no longer holds — process_group does not create a session; for our purpose — escaping yog's group so terminal signals don't reach the child — the group is what matters; document this semantic delta in the module doc). (2) Test mkfifo: replace both unsafe libc::mkfifo sites (cli_outbound/tests/detach.rs, start/tests/prompt.rs) with Command::new("mkfifo") status-checked. (3) The env-var test unsafety (std::env::set_var is unsafe in edition 2024): add an injected env-lookup seam to Cli::resolve (e.g. resolve_with(lookup: &dyn Fn(&str)->Option<String>) with resolve() delegating to a std::env-backed lookup) so run.rs tests need no ambient env mutation; for the fork-inheritance assertion in detach.rs, keep ambient env if unavoidable BUT then that one test file must be listed in the ast-grep rule ignores — prefer restructuring (pass the var via Command::env explicitly, which tests inheritance of explicitly-set vars without mutating yog's own env). (4) The irreducible libc::kill(SIGTERM) in Stream::drop: move into a new tiny src/cli_outbound/sys.rs wrapper fn with the safety comment; add rules/unsafe-outside-sys.yml (ast-grep, severity error): unsafe blocks/fns banned everywhere except src/cli_outbound/sys.rs — mirror the locks-outside-state shape (match unsafe_block + function_modifiers regex \\bunsafe\\b, ignores: ["**/cli_outbound/sys.rs"]). Create sgconfig.yml (ruleDirs: [rules]) and rules/fixtures/violations.rs with a deliberate violation; smoke-test: `ast-grep scan` (now installed on host) flags the fixture and passes on src/. The fixtures file must be excluded from scan targets or the scan run scoped to src/ — figure out the clean shape (e.g. fixtures live under rules/fixtures/ and the smoke test runs `ast-grep scan rules/fixtures` expecting nonzero). Record the ast-grep version pin in sgconfig.yml comment + Makefile if a target is added. Gate green as always.