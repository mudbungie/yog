+++
title = "W9 lernie-tool-bl agent-tool shim from embedded balls"
created = 1784784299
updated = 1784957319
claimant = "Exultantly-365e"
parent = "bl-b5d1"
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
DESIGN §16.7 W9. Needs W8+W12. AS BUILT (deviates from the original one-liner, DESIGN amended):
- Shim is <yog-data-root>/world/tools/bl (NOT lernie-tool-bl): a /bin/sh re-exec of Cli::exec_words -> 'yog bl "$@"'. lernie's external-tool convention was falsified — it discovers $LERNIE_HOME/tools/lernie-tool-<name>, speaks JSON-on-stdin, and needs a schema + SKILL.md + a providers.yaml role entry. Agents drive bl by bash (§16.4's own premise), so the shim is named plainly and put on PATH.
- PATH becomes the world's THIRD override (§16.2): world/tools:$PATH, idempotent prepend.
- --as $YOG_NAME is applied at Edge::default_actor (YOG_NAME -> USER -> unknown), not by argv rewriting: one rule over every verb, explicit --as still wins.
- §3.3 preamble instruction deleted; identity_preview/identity_preamble collapsed to one function.
- RULING on the W8 exe_dir bound: SCOPE the surface — the bl arm refuses prime/sync/install (exit 91, re-pointed) and prints the by-path remediation. Proper fix filed as upstream ask U-balls-3 (injected plugin-binary seam).