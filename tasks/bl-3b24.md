+++
title = "land the owed fake halves Z10-Z14: the S3-S8 story tests over the fake substrate"
created = 1786161491
updated = 1786161491
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
STORIES.md's test->enabling-task map lists Z10..Z14 as 'to file', carrying the S3-S8 tests over already-built surfaces — they were never filed as balls, so S3, S4 and S6 (and S5/S7/S8) remain half-done from the fake side: real-wire green (2026-07-26/27 logs) with no regression armor. That is the standing 'it worked once' hole the last 177 commits fell through.

Deliver, in tests/integration/ per the house harness (recorder-script substrate via support/, asserting the three surfaces — recorded spawns, ops.jsonl trail, derived view-model — never egui widgets): the S3-T*, S4-T*, S5-T3..T6, S6-T*, S7-T*, S8-T* rows not yet present in the fake half. Read STORIES.md for each row's exact assertion before writing it; verify the row's premise against the current tree (bodies and docs drift).

A test that comes up red has found a defect: file the defect ball and land that one test red-first in the defect's own worktree per the STORIES discipline ('written first, red in-worktree, green at close'); green tests land here. Update the STORIES map rows from 'to file' to their filed/landed state.

This is sized as several rungs of work: splitting into per-rung subtasks (--subtask-of this ball) is expected, not scope creep. Coverage stays at 100% throughout — the gate enforces it.

Acceptance: every named story row has a fake-half test on main (or a filed defect ball owning its red test); STORIES' map reflects reality.