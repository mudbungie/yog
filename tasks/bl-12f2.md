+++
title = "CI red on main: multiplex_lernie e2e test inherits ambient git identity — empty ident on runners"
created = 1785130414
updated = 1785130508
claimant = "waxier-12f2"
priority = 1
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
First CI run of the batteries stack (f3aa713) failed: tests/multiplex_lernie.rs test the_lernie_arm_is_the_thin_binding_end_to_end panics at line 101 — the embedded lernie's git operations hit 'fatal: empty ident name (for <runner@…>) not allowed'. GitHub runners have no user.name/user.email; the test passed locally only via the developer's global gitconfig — an ambient dependency the test's own-process-env design is supposed to exclude. Fix in the test's env setup: set GIT_AUTHOR_NAME/GIT_AUTHOR_EMAIL/GIT_COMMITTER_NAME/GIT_COMMITTER_EMAIL (or GIT_CONFIG_GLOBAL to a fixture config) alongside the existing XDG/LERNIE_HOME scratch env. Check the other integration binaries for the same ambient-gitconfig dependency while there (any test that drives prime/new/claim through real git). Everything else in the CI run was green: git-pin fetches worked, cargo-deny passed (thiserror 1+2 duplicates are warnings only).