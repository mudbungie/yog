+++
title = "chat header must surface config drift: conversation frozen on 5.4 while the workspace default moved to 5.6-sol — say so, and offer the exit"
created = 1785645766
updated = 1785645766
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator report 2026-08-02: changed the workspace default to gpt-5.6-sol (write landed, f36afe8), but the open chat still shows 5.4 in the header and signature. That is the §9.4 freeze working as designed — a conversation is pinned to its governing config commit — but the operator has now been surprised by it twice; the picker's caption ('governs the NEXT conversation; this one stays frozen') is not landing.

Fix the surface, not the invariant: when the workspace default has advanced past the focused conversation's frozen config, the chat header must show BOTH facts — e.g. model line renders 'gpt-5.4 (frozen; workspace now defaults to gpt-5.6-sol)' with hover explaining the freeze — and offer the one honest exit: a 'new conversation with current config' affordance right there (the composer's new-conversation verb already is that; the affordance may just focus it). Do NOT add mid-conversation config adoption — that breaks the frozen-lineage invariant; if implementation reveals a case for it, write the argument into the ball and stop. Derivation not storage: drift = (workspace default oid != conversation governing oid), computed at render.