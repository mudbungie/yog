+++
title = "upstream shipped: lernie --version now prints the linked brazen pin (lernie bl-c1b9)"
created = 1785129941
updated = 1785132867
claimant = "waxier"
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Upstream shipped (lernie fff998e, in published 0.0.1): lernie --version prints 'lernie 0.0.1 (brazen 0.0.4)' from the same brazen_pin() reader the load-time guard uses. DISPOSITION — moot for yog, closing with no diff: the W5 consumer this notification targets was deleted by W13 (bl-07b9); nothing in yog reads lernie --version anymore (only bz --version survives, via multiplex/bz_host). The skew class the probe would have gated is structurally unrepresentable since the batteries epic + registry adoption (one Cargo.lock names one brazen/lernie/balls; every process image is the yog binary those pins built — DESIGN §16.4). The upstream feature remains right and useful for humans diagnosing host tuples.