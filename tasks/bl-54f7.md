+++
title = "the live streaming tail updates at the watcher's cadence, not as characters land"
created = 1786599899
updated = 1786599904
priority = 3
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-915e"
on = "claim"
+++
Operator complaint, 2026-08-12, verbatim:

> text doesn't stream back in directly while the model is thinking/writing. yog
> should be reading directly from the stream-in file in the lernie workspace;
> it should be showing the user every character as it lands.

## Where this stands today

The mechanism exists; its **cadence** is the defect. `src/transcript/mod.rs`
already appends the live tail as a virtual trailing entry:

    /// Build the transcript for `agent_id` in `workspace`. When `in_flight`
    /// (the caller's `AgentState::InFlight` — the latest step's `response.json`
    /// fd is still open, §3.5), the folded live tail is appended as a virtual
    /// trailing entry.
    pub fn build(workspace: &Path, agent_id: &str, in_flight: bool) -> Transcript {
        let dir = workspace.join(AGENTS_DIR).join(agent_id).join(MESSAGES_DIR);
        let mut entries = read_messages(&dir);
        if in_flight && let Some(text) = crate::git_tree::streaming_text_from_disk(workspace, agent_id)

and `src/git_tree/streaming.rs::fold_stream` reads the open JSONL with the right
tolerance:

    /// Lines that fail to parse, and events outside the delta seam, are
    /// skipped — partial-write tolerance is structural (the harness may be
    /// mid-line on disk).

So the file *is* the stream-in file the operator means, and yog *does* fold it.
What the operator is watching is the rate: that fold only happens as a byproduct
of a **whole-workspace derivation**, which is gated behind the §7.2 chain —
`Bridge`'s poll interval, the `DirtySet` announcement, the 100 ms debounce
(`src/app/dirty.rs`), the worker's full re-derive, then `AppModel::refresh`
taking the published snapshot. Characters therefore arrive in clumps at the
watcher's rhythm, not as they land, and during the *thinking* phase — where no
text delta exists yet — there is nothing at all.

## The ask

The focused conversation's live tail should update at frame cadence and paint
every character as it lands. Nothing else changes rhythm.

## Constraints the implementer may not break

- **The frame does no IO.** The standing principle is absolute: the UI never
  freezes, the frame renders snapshots only, all work happens off-thread. Do
  **not** answer this by reading `response.json` inside the render pass. The
  right shape is a dedicated cheap follower for the *one* focused agent's open
  step file, running off-thread at its own fast cadence and publishing into the
  model — the whole-workspace derivation stays on the §7.2 cadence untouched.
- **One fact, one home.** The tail is already derived by `fold_stream`; the
  follower re-uses it, it does not grow a second parser. `Transcript::build`'s
  contract ("everything is a pure function of the on-disk bytes, §3.5 stateless
  re-read") must survive — the follower supplies fresher bytes, not a cached
  string that can disagree with them.
- **Scope it to the focus.** Tailing every agent in every workspace at frame
  rate is the version of this that burns the machine. One conversation is open;
  that is the one that streams.
- **Thinking must be visible too.** The complaint says *"thinking/writing"*.
  `Delta::Thinking` is already folded (`last_delta` tracks it) but the tail
  entry carries `text` only. Decide whether reasoning text streams into the
  tail as its own toned row, or whether the existing thinking indicator is the
  answer — and say which in DESIGN, because "nothing is happening on screen" is
  precisely the complaint, and a `Doing::Thinking` badge that never grows is
  not obviously enough.
- **Watch the interaction with bl-70b8/bl-f16e.** A beat that asserts the tail
  is non-empty passes today. The assertion has to be about *freshness*: bytes
  appended to the open step file are on the composed frame within one repaint,
  without a sweep having run.

## Related

Same family as the start-echo ball (the operator's first complaint of this
session): both are "yog knows something the snapshot does not yet". If one
mechanism can carry both, prefer it — but the echo is about a fact yog itself
caused, and this is about a file yog is reading, so they may honestly be two
things. State the call rather than assuming it.