+++
title = "RAM-ruling revisions: delete dead boot_geometry, rehome show_internal, DESIGN §13.0 clarified-rule amendment"
created = 1784433622
updated = 1784433638
claimant = "filtered"
priority = 1
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
The user clarified the durability rule: "no STATE in RAM", not "nothing in RAM" — views are fine in RAM. Review verdict (assessment on file): the shipped design is validated; three revisions warranted.
1. DELETE dead boot_geometry: ui_state boot_geometry/set_boot_geometry (mod.rs ~229-241) have zero non-test callers — main.rs boots NativeOptions::default() and never reads/writes it. Remove the accessors, their tests, the schema keys in DESIGN §4.1, and the §13.2 carve-out ("Live window geometry" already sits in §5.3's RAM class). Pure subtraction; if window-size persistence is ever wanted, wire eframe's own viewport persistence deliberately.
2. Rehome show_internal: src/app/balls.rs SHOW_INTERNAL_KEY="internal" smuggles a global boolean view-filter into the collapsed section-key set (field overload). Give it a dedicated "show_internal" boolean key in ui.json with its own ui_state accessor pair; drop the sentinel. Update DESIGN §4.1.
3. DESIGN amendment — new §13.0 "The rule is no *state* in RAM, not nothing in RAM": record the user's clarification; generalize §13.1's focus/scroll ruling from a per-item exception into the primary rule (views — which data you look at, how the window is arranged — may be RAM; durable data/state must replicate). Add a one-line §5.3 note that roster `collapsed` is a view persisted for convenience and converged benignly, deliberately unlike the RAM jsonview collapse set — so nobody "fixes" the asymmetry later.
Gate as always (fmt/clippy/300-cap/tarpaulin-100%).