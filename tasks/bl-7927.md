+++
title = "the dir field: editable text box with the default pre-filled, moved to the top config block — not at the bottom by the composer"
created = 1785645908
updated = 1785646457
claimant = "birth-fixer"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"
+++
Operator request 2026-08-02, verbatim: 'the directory should be an editable text box, with the default pre-chosed. It should be at the top, in the config block, not at the bottom by message.' Today the dir (path-rung, §3.4) field rides in the bottom accessories beside the composer. Move it to the top config block (the header area where config-shaped facts live — model line §9.4, frozen oid), render it as an editable text box pre-filled with the default (the rung's current resolution — ~ for bare, the path for path-rung), editing it changes where the next start runs. Kinship: bl-824e (start-time model pick, also a conversation-birth parameter moving to the config surface) — implement the two as ONE coherent birth-config block if a single agent takes both; they must not land as two unrelated widgets.