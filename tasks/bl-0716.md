+++
title = "seat severance and the name flip: the window becomes the lernie crate"
created = 1787977102
updated = 1787977110
priority = 4
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"

[[blockers]]
id = "bl-37fd"
on = "claim"
+++
Extract the seat — the window plus the seat client mode — into its own repo and crate, published under the lernie name from the fence version (0.1.0). The new repo stands up the full gate apparatus (line cap, coverage floor, leak-scan, hooks) and its own store; both READMEs state the fence and its date so the two eras of the name are disambiguated by one rule.

The wire is the boundary the seat already lives behind (REMOTE §1.2: the window is a pure wire client of localhost since bl-ae05), so the extraction moves code, not architecture. The android client speaks the same wire and is unaffected.

Sequencing per bl-37fd: yog keeps its window mode for one deprecated release while the extracted seat ships, then drops it (the server-severance ball). Cost accepted by ruling: the published record carries two eras of the lernie name; the version fence is the disambiguation.