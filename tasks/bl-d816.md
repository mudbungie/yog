+++
title = "DECISION: AgentState collapses kill, crash, and provider error into Stopped — should the badge vocabulary distinguish 'died mid-turn' from 'ended'?"
created = 1786843884
updated = 1786937228
priority = 3
root_commit = "4dca48efee9e480f122f613931435d280a6ddedf"
+++
git_tree/state.rs framing_state deliberately folds kill/crash/explicit-stop/provider-error into one Stopped, and attention rule 2 deliberately fires on rest, not the wound. After bl-ace6 the workspace pane says why a chat is dead, but the CONVERSATION LIST still cannot distinguish 'finished cleanly' from 'died mid-turn on a 400' — both paint ■ stopped. Whether that distinction deserves a state (an Errored badge, or a tone on Stopped) reverses a ruled design (badges vocabulary; attention rule 2's rest-not-wound doctrine), so it is the operator's call, not an implementer's. Options: (a) leave it — the badge answers 'needs me?', the pane answers 'why'; (b) split the badge; (c) tone the row. This ball is the tracked question.

---

Operator ruling 2026-08-16: leave it — option (a). The badge answers 'needs me?' and the workspace pane (bl-ace6) answers 'why'; attention rule 2's rest-not-wound doctrine stands. No badge split, no row tone. Closing with the ruling recorded.
