+++
title = "registry adoption: swap balls git-rev pin and the W11 lernie shim to published crates.io version pins"
created = 1785124114
updated = 1785132707
claimant = "waxier-89a4"
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-50bd"
on = "claim"
+++
Registry adoption: yog's last two git dependencies retired for exact crates.io pins. **Done (waxier-89a4, 2026-07-26).**

## The swap

    balls  = "=0.5.9"    (was git rev 9ee8d016, version = "=0.5.7")
    brazen = { version = "=0.0.4", features = ["native-host"] }   (unchanged)
    lernie = "=0.0.1"    (was git rev a33b2cf1, version = "=0.0.1")

Dep comments rewritten: the interim-git-pin story is gone; each now states the exact-pin rationale and the brazen parity check. `deny.toml`s `allow-git` list DELETED outright (the `[sources]` comment now says registry-only, nothing goes back on the list). `cargo update -p balls -p lernie` refreshed Cargo.lock.

## Verification

- **Lockfile singletons:** exactly one each, all `registry+https://github.com/rust-lang/crates.io-index` — balls 0.5.9, brazen 0.0.4, lernie 0.0.1. **Zero `git+` sources in Cargo.lock** (`grep -c git+` = 0). Brazen parity holds: lernie 0.0.1 links `=0.0.4`, yog pins `=0.0.4`, ONE brazen resolves.
- **`cargo test`:** 29/29 test binaries green, 743 lib unit tests, 0 failures. No API breakage from the few commits between the pinned lernie rev and published 0.0.1 (release chores, README/UX polish, version-guard fix) — nothing to patch around.
- **`cargo publish --dry-run --locked`:** PASSES. Packaged 271 files / 1.9 MiB, verify build clean. This was the point of the ball: yog is publishable again. (`warning: crate yog@0.0.1 already exists` is just the current released version; release-plz bumps.)
- **`make lint`:** clippy -D warnings, rules-audit, `cargo deny check` all green — "advisories ok, bans ok, licenses ok, sources ok" with no `allow-git`.
- **Clean room (`scripts/drive/cleanroom.sh`, Xvfb, live wire):**
  - `run-s3s4s6` — **15/15 ALL BEATS PASS**. The whole ball rung (lernie new -> bl claim -> prompt in the ball worktree -> wire reply -> bl close -> closed listing) driven through the embedded balls 0.5.9 and lernie 0.0.1, with lernie/bl/bz unresolvable on PATH.
  - `run` — 6/8. The two red beats (S1 message-to-agent, S1 prompt-existing) reproduce IDENTICALLY on a binary built from unmodified main, so they are not this change: the composer click coordinate has gone stale. Filed as **bl-0293**.

No fresh drive log written — this is a dependency swap, not new coverage; results live here.

## Carried along (pre-existing CI red, both from bl-2930, neither caused by this ball)

CI on main was already failing before this change (runs for a3bce10 and 0d914fc):

1. **linux `make ci` -> `fmt-check`** — src/multiplex.rs, src/multiplex/tests.rs, src/world/tools/tests.rs, tests/multiplex_bl.rs carried hand-written lines past rustfmts `fn_call_width`. **Fixed here** by `cargo fmt` (mechanical only; all four files still well under the 300-line cap). AGENTS.md merge discipline requires the gate to pass at the end state.
2. **macos `make test`** — `tests/multiplex_bl.rs::the_bl_arm_runs_the_whole_rung_on_the_embedded_balls` asserts a worktree path that macOS `/var` vs `/private/var` normalization likely breaks. NOT fixed (cannot test macOS here). Filed as **bl-34ae**. CI will stay red on macos until it lands.

## DESIGN / AGENTS amendments

- §16.5 balls bullet + lernie bullet: "NOT released ... yog pins git = ..., rev = ..." -> "Released and adopted 2026-07-26 (bl-89a4)", with the retired revs and the bl-4537 history-rewrite recorded as the argument that closed.
- §16.7 **Dependency shape** rewritten as *reached*: all three substrates at `=x.y.z`, zero git deps, no `allow-git`, `cargo publish --dry-run` passes. The interim shape stays lawful but its two prices (publish block; a rev is not a stable name) are now stated as paid, so it is not re-entered casually.
- §16.7 U-balls (a) and U-lernie: both "release it" asks discharged. U-balls-2 (the DEAD-set walk, `reads::history`) is the only one still owed.
- §16.7 W11 "Gated on" line and the §14 new-dependencies paragraph updated to the landed registry pins.
- AGENTS.md rule 6: "registry-only, with no exception in force"; the one interim exception is retained as a described last resort with its costs named.

**main is now registry-clean and publishable.** Cutting 0.0.2 is the operators call — the release PR was deliberately NOT merged.