+++
title = "registry adoption: swap balls git-rev pin and the W11 lernie shim to published crates.io version pins"
created = 1785124114
updated = 1785124114
priority = 2
root_commit = "805ddf08f8a13f1d0c2b0bf7b07d4a1bc438706c"

[[blockers]]
id = "bl-50bd"
on = "claim"
+++
Blocked on upstream publishes. yog's Cargo.toml (batteries epic ref) pins balls to an exact git rev (crates.io release with the promoted read surface not yet out) and W11 (bl-50bd) ships lernie as a git-rev shim because lernie is not yet published at the needed version. When upstream lands: (1) balls: swap git pin to `=x.y.z` registry pin per the Cargo.toml comment; (2) lernie: swap the shim to the published `=0.0.1` (or later) crate; (3) verify `cargo publish --dry-run` passes — crates.io publish of yog is BLOCKED while any git dep remains, and release-plz's release job will fail on every version until this lands. Operator ruling 2026-07-26: shim now, queue this adoption; 0.0.1 ships from pre-epic main which is registry-clean.