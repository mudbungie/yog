# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.0.9](https://github.com/mudbungie/yog/compare/v0.0.8...v0.0.9) - 2026-09-01

### Changes

- the boundary speaks effort and priority: two role-tuning gestures, and the providers rows state the capability — one PROTOCOL bump [bl-23bd]
- the two boundary rosters are at the 300 wall: Action 295 and Reply 297, and an enum has no seam [bl-92d3]

## [0.0.8](https://github.com/mudbungie/yog/compare/v0.0.7...v0.0.8) - 2026-09-01

### Changes

- the boundary still speaks fork-is-the-freeze: /retarget's surface and REMOTE predate litany's follow-the-tip ruling [bl-e654]
- a newly primed project refuses for a whole sweep: bl-6c9e made workspace birth a barrier and left the project noun on the cached set — and no gesture answers which project names exist [bl-3377]
- the terminal seat tells the operator to focus a workspace it cannot focus, and never names --ws: the flags exist only in a refusal that missing-target refusals do not print [bl-e66f]
- /flag raises no attention item: it writes one exit-0 ops row and the §6 predicate reads no ops row, so the monitor's floor grant signals where nobody looks [bl-6f2f]
- /prompt discards the prepared prefill, so a composer-less seat fires a path rung with no working-directory headline and a ball rung with no ball [bl-06a1]
- a conversation refusing at the provider rung is silent at every seat: drivers launch, every model call fails, and the operator reads nothing [bl-9b88]
- a path-rung conversation's deliverable is invisible at the boundary: /files promises work products and lists none, /work-diff is empty, and neither says why [bl-1015]
- the worktree lane's refusal sends the model to the loaded lane, which runs a workspace-subject build in the foot's inherited process cwd — the conversation reports done and the bound directory is empty [bl-68e1]
- nothing ships an implementation of the shipped worker grant's worktree tools, so a default install offers apply_patch/bash/read_file to every model and answers none of them [bl-5710]
- the wire refusals name commands the operator cannot run: 'make wire-certs' on a box with no checkout, and env-only settings spelled as argv that yog wire-certs accepts, ignores and calls success [bl-a0dd]
- /enroll's ':0' refusal prints a wire-certs remedy that always refuses on the box it is printed for, and omits the restart the new address needs [bl-a6b7]
- the provider pane re-derives brazen's credential column from a file probe, so an ambient credential renders as 'not signed in' with a Login button [bl-dba3]
- no conversation search: a client can only scroll [bl-764a]
- roster rows carry no time: a phone's conversation list cannot say when anything last moved [bl-b7d9]
- the /enroll grade word is certificate jargon at the glass — accept and explain the brands [bl-427b]
- a same-name enrollment refuses without teaching the design: one device, one leaf — an operator leaf already serves the seat AND the tool host [bl-7a4a]

## [0.0.7](https://github.com/mudbungie/yog/compare/v0.0.6...v0.0.7) - 2026-08-31

### Changes

- EXIT the litany interim git pin: flip Cargo.toml back to a registry pin and delete deny.toml's allow-git entry when litany next publishes [bl-f91a]
- the follow lane's frame is called a `delta` and carries the whole accumulated answer: 20x wire amplification on a 416-byte reply, quadratic in the answer's length [bl-3655]
- a conversation refused at the provider rung paints as `stopped` with an empty transcript and an empty-stderr trail row: only /steps carries auth_failed, and `stopped` is the word /stop already owns [bl-b43b]
- the advertised set is last-writer-wins on the client identity: a second connection can blank a live foot's tools and neither end is told [bl-1462]
- the mailbox loses an invocation at hand-off: `taken` is a latch set at the drain, so anything posted into a parked read whose peer already died is consumed and never redelivered [bl-e658]
- the total router left the shipped worker grant dead: seven of eight granted tools refuse, taking subagents, agent messaging, skills and the conversation's own worktree with them [bl-77be]
- a conversation's first driver can load a remote tool but can never call one: `prompt` mints the agent, so `Injection::driving` is None and `tools()` declares only `clients` [bl-fd24]
- seat.sh ships a name docker will not run: podman save embeds the localhost/ prefix, docker load restores it, and the unit points at the bare tag — the deploy prints success over a crash-looping engine [bl-0719]
- a boundary Action to mint a client leaf, so a trusted seat can bootstrap a new device [bl-f4e3]

## [0.0.6](https://github.com/mudbungie/yog/compare/v0.0.5...v0.0.6) - 2026-08-30

### Changes

- the deploy path seats a binary unit while the server now runs the image: make deploy would overwrite the cutover [bl-c6e2]
- the config family carries its workspace inside target, and the workspace-slot table does not read it: a config act at a renamed remote workspace edits the wrong wall's file [bl-523f]
- the clients tool can load but never unload: the per-agent loaded document only grows, and a finished host rides every later assembly [bl-3455]
- the server ships as an OCI image: the UI-free binary, its XDG mounts, and the wire material it never carries [bl-10f9]
- server severance: yog ships UI-free and inert by default [bl-7942]
- the codec emits a conformance corpus: one canonical fixture set every client replays, so an implementation miss fails a fixture instead of shipping [bl-32cb]
- the compactor's procedure pair routes to the tool host under the inverted seam, and the engine cannot decide where they belong [bl-dfce]
- engine rename: lernie becomes litany [bl-9905]
- enforce the foot grade: read it off the leaf, mint it, and raise on it at the chokepoint [bl-7ff3]
- version the wire: handshake protocol version, fail-closed on mismatch [bl-a670]
- a start fired at a remote workspace resolves its path with unwrap_or_default and posts the act at an empty PathBuf: silent misfire, bounce to home, no refusal painted [bl-e349]
- decompose 2/10 — src/boundary/ production: the seven §8.5 chokepoint files at 262-293 lines, the surface every §8.2 act routes through [bl-c088]
- a conversation the removed ceiling already killed has no stated way forward: the frozen budgets: still binds it and the exhausted mark parks it, so the §11 wound names no remedy [bl-d710]
- a crashed tool window paints nothing — the third member of the swallowed-error class, and the only one that reads as an idle conversation [bl-abba]
- detached-row failure should be a state predicate, not sink content: apply the driver.log rule to the stderr sink [bl-b95e]
- decompose 9/10 — the §7 fleet tier and the substrate tail: twenty-one files at 251-291 lines [bl-063b]
- decompose 5/10 — src/shell/ outside acceptance: eleven window files at 250-294 lines [bl-49cc]
- decompose 4/10 — the REMOTE transport: src/wire/, src/tool_host/, src/engine.rs, seven files at 251-289 lines [bl-eae9]
- decompose 7/10 — src/git_tree/, src/nav/, src/start/, src/config_edit/, src/model_pick/: twelve files at 253-290 lines [bl-fae3]
- decompose 8/10 — the paint tier: src/theme/, src/ui_state/, src/paint_probe.rs, src/inspector/, src/rail/, src/steps_view/, ten files at 250-294 [bl-5aae]
- decompose 6/10 — src/app/ and src/transcript/: thirteen derivation and chat files at 251-292 lines [bl-3b22]
- decompose 3/10 — src/boundary/**/tests/: eleven §8.5 corpora at 260-296 lines [bl-a473]
- VISION promises model and skill selection from ball tags, but fleet birth discards tags [bl-380f]
- the start pane invites a goal that cannot possibly run: on a wall with no usable provider, the first rung must be provider sign-in, not a text box [bl-1fd0]
- one asker per channel: the window attaches to every entry; poster routes acts, lane and searcher follow the focused channel (REMOTE §8.2) [bl-670c]
- decompose 1/10 — src/shell/acceptance/: ten drives at 250-291 lines, on the surface the REMOTE §8.2 window chain must add coverage to next [bl-dd00]
- the window's model composes one snapshot slice per channel: union roster, origin labels, collision refusal (REMOTE §8.2) [bl-028a]
- a test-thread set_env tears the process environment: a peer thread's spawn sees a nul byte and the run fails on a test that is not broken [bl-419d]
- multiple armed projects have no world-level concurrency or whole-day spend ceiling [bl-a80a]
- src/boundary/reply/tests/mod.rs carries a second copy of tests/rows.rs — five row-encoder beats, three of them verbatim [bl-9d34]
- 307 of 1112 files are over the 200-line pre-split band and three sit exactly on the 300 wall — split for real, and stop the doc asserting a census it cannot know [bl-b278]

## [0.0.5](https://github.com/mudbungie/yog/compare/v0.0.4...v0.0.5) - 2026-08-23

### Changes

- yog seat / yog tool-host resolve the gesture's workspace over entries; flat stays the box's own root (REMOTE §8.2) [bl-4e31]
- the lib suite dies with SIGPIPE roughly one run in four under full parallel load, killing the whole test process mid-run [bl-3792]
- issue an extra client leaf under a stated common name — the host half of provisioning an entry (REMOTE §8.2) [bl-64a7]
- wire entries: read <yog-data-root>/wire/workspaces/<leaf>/ as client-side workspace material (REMOTE §8.2) [bl-5b68]
- yog serve has no graceful stop: it parks with no signal handler, so a SIGTERM kills in-flight turns outright and every restart is destructive [bl-269a]
- output-limit exhaustion is classified as clean quiescence, leaving an empty answer and a Nudge that cannot advance [bl-fb87]
- the deployment defers a restart for a reason that stopped being true: the pinned lernie already settles a crashed tool window, and a wrong reason is how a right mechanism gets deleted [bl-286b]
- mint the follow lane: streamed output reaches the glass at write cadence, the tail's one held read beside the pull asker [bl-73e7]
- yog has no deployment recipe for a headless server: no service unit, no auto-update from the registry, and an unconditional restart kills an in-flight turn [bl-bf35]
- a benign driver notice on the detached sink is rendered as the §7.3 failure: detached rows equate stderr with death [bl-1296]
- remove ball Move end to end: both carriers, the codec arm, reassign, and the submenu machinery it alone justifies [bl-6c28]
- the start window has no coherent address: sends bounce or wedge the claim, the queue is empty, drafts are destroyed, and a double Enter mints one name twice [bl-56c6]
- sidebar content ratchets the panel to its half-window ceiling: the drag snaps back and the clamped seam shows the translucent clear color [bl-0424]
- a tall conversation list evicts the navigator's tail and both delete dialogs bury their own fire button [bl-86a5]
- the queue paints two rows for one sent message: the echo never yields to the real deposit [bl-78d8]
- the speaker label wears body ink on the body's own line — role hue never reaches the prefix [bl-f3fc]
- the conversation preview is the assembled context's head, not the operator's payload [bl-368d]
- a healthy send flashes the orphaned-mail alarm: wound_grace under-covers the rising edge and the send never schedules its own catch-up [bl-18e8]
- two spawn/stream races behind spawn_guard resurface under full-suite load: delete-census fail-closed and the sign-in-stream wall test flake ~1 in 5 [bl-6397]
- the remote teleoperator cannot set priority, blockers, hierarchy or tags—the facts its fleet schedules [bl-dbde]
- REGRESSION of bl-8c2d: run-unseeded still expects CONFIG where the current default correctly returns AUTH [bl-6244]
- /ops help says every gesture is recorded, but boundary queries leave no audit row [bl-5cbc]
- REGRESSION of bl-f5f6: /attention returns an engine path that remote gestures cannot reuse [bl-22ab]
- scoped snapshots drop real armed-fleet facts, so /board shows drones but not the policy running them [bl-8bf6]
- a failed fleet drone wedges forever without a lease and retries the same ball forever with one [bl-3988]
- replacing a live yog executable rewrites tool-control to an impossible '(deleted)' path [bl-f558]
- fleet birth failure strands the claim, records spawn success, and leaves no conversation to repair [bl-ab13]
- help serve still describes the retired pre-self-provisioning wire behavior [bl-6e0c]
- the spend figure charges a cached prompt slice twice on OpenAI- and Google-shaped providers, so $ spend and the §3.5 ceiling read up to 2x high [bl-6621]
- the whole-tree ceiling nobody set: workspaces born before lernie's template fix froze budgets: into config/default, so a yog-dispatched conversation dies against a cap invisible everywhere yog looks [bl-56af]
- a default install has no browser-login row — 'sign the sphere in' resolves to the config editor, not bz --login; is that the intent? [bl-8c2d]
- consume the two-word PascalCase mint: bump the lernie pin once the release carrying it lands [bl-0219]
- compaction now says so at every secondary reader: science states the compacted bound, search names the deleted span, the monitor window quotes the summary (the VISION 4.9 ruling), and the message count is the counter's high-water [bl-fde5]
- the models: entry collapses to the id and its context_window: two columns with no reader and the §9.2 gate over one of them are gone — write less, read the same [bl-3ffa]
- Cargo.toml declares no include/exclude, so docs/ design commentary (DESIGN, VISION, QUALITY, REMOTE, STORIES) ships inside the crate to crates.io [bl-8340]
- V3 seat: the fan group in the window — pick N, compare candidates, judge/synthesize/deliver affordances; graduates the Adjudicator story [bl-77bc]
- the transcript is a readdir of a directory compaction DELETES from, and yog reads no summary/**, so a squashed span is silent — derive the gap, paint what replaced it [bl-7bd2]
- the models.yaml half of a pick was inert: lernie retired the models: table, so the pick is ONE write and context_window's one home is stated [bl-d9cb]
- a fired start is not selected until its branch lands: the echo mints a row nothing focuses, so the operator's own new chat sits unhighlighted behind the birth placeholder [bl-2e8f]
- confinement's writable set derives the bound project repo, so a confined drone can deliver its own ball [bl-34b1]
- a step that dies on a request-shape decline offers no remedy: config_edit::fault classifies only Config-kind failures, and brazen rejects tools with ParseInput [bl-5252]
- one conversation addressing at the boundary: an agent id or the unique stored name, resolved once ahead of both chokepoints [bl-49bc]
- attempt science projection: one derived headless query joining frozen inputs, refs, usage, and outcome [bl-40ab]
- drive harness decay: the S5 collapse and S8-T4 no-write beats still read ui.json for the collapsed set, which bl-8bbc moved to the per-seat pane document [bl-9df2]
- an agent's own marks space owns its worktrees too: the bl arm stands its process in the space, so bl-delivery's territory folds off the space's XDG_STATE_HOME [bl-c21d]
- Ollama's context is the server's, not the model's: state the caveat and the remedy, pin brazen's silence with a test (upstream bl-f19d) [bl-671d]
- wire the missing OS confinement backend for unattended agents [bl-bca4]
- a workspace whose birth died mid-way can never be prepared again: the name is wedged and the refusal names the wrong reason [bl-c9d2]
- self-provision requests 127.0.0.1:0 so instances never contend, and a wireless window paints its refusal instead of opening inert [bl-dc14]
- the bare yog bl and lernie namespaces stand their process in the world: one set_env at the arm, so no namespace verb reaches ambient substrate [bl-81c9]
- /model accepts claude-code although every yog request carries tools its protocol rejects [bl-3d22]
- the drill-in record picker cannot open stderr.log or driver.log: a long driver error is unreadable in-window [bl-83d6]
- workspace birth IS a barrier: the boundary's intake resolves names over the live enumeration, not the derivation's cached copy [bl-6c9e]

## [0.0.4](https://github.com/mudbungie/yog/compare/v0.0.3...v0.0.4) - 2026-08-15

### Changes

- V3 Adjudicator core: Deliver candidate, the derived acceptance mark, and the fan's candidates on the work-diff; judge/synthesize ride V2 [bl-c2bd]
- CI-only coverage red on main since the lernie 0.0.10 lockfile change: tarpaulin misattributes transcript/spine.rs's multi-line hline call (87-88) as uncovered — bind the stroke, one-line the call [bl-914f]
- consume lernie 0.0.10: the chat-wedge fixes (alternation-wide unpaired decline, crash settlement at the drive boundary) [bl-4c1f]
- REGRESSION of bl-52ed: tool-host is the only top-level command that rejects --help [bl-4667]
- three engine/wire tests bind the fixed loopback port, so the suite cannot pass on a box where yog is running [bl-4c50]
- an unanswered delivered message with no live driver paints nothing: the chat just stops — derive the dead-driver banner, and finally read driver.log [bl-ace6]
- BLOCKED on lernie bl-b98d — composer send-and-interrupt: Ctrl+Enter (and a button) interrupts the agent and triggers on the new message [bl-a33d]
- :remote a_stop_ends_the_wait_on_the_tool flakes under full-suite load: a sleep/poll race, seen three times across three gates in one day [bl-3a88]
- the speculate/CI runner intermittently dies with 'cargo_tarpaulin: Failed to run tests: Attempting to handle tarpaulin being signaled' — a runner-side kill that reads as FAIL and poisons the verdict cache for that tree [bl-673a]
- the Actions token cannot write refs, so release-plz publishes to crates.io and then fails to tag: v0.0.3 shipped untagged (403 'Resource not accessible by integration') [bl-1e88]
- world tools: add the yog shim — a responder (and any world agent) cannot call 'yog gesture' today [bl-8398]
- speculate.yml restates the balls pin and a dead billing premise: derive the version from Cargo.toml, bump the substrate to 0.5.11 [bl-71c4]

## [0.0.3](https://github.com/mudbungie/yog/compare/v0.0.2...v0.0.3) - 2026-08-15

### Changes

- REMOTE read path: the accessory tail, part two — the ball-and-spend family, the fire-time gates, the misc singles, and staleness's clock [bl-b4b5]
- REMOTE read path: the accessory tail — every remaining in-process read is an unspelled Reply (mark seats, flight strip, the ball-and-spend family, fire-time gates, altitude-0 chrome) [bl-296f]
- REMOTE read path: the seat's own Query::Agent — the last in-process read, and the rendering ruling it needs [bl-48ae]
- REMOTE class 2: focus by name, and the raise that focuses a workspace the enumeration has not read [bl-7407]
- REMOTE read path: migrate the whole §11 inspector family onto the wire, and spell config-frozen-at [bl-13f9]
- REMOTE read path: the conversation-list and inspector folds want the answer at a different altitude; the tab bar wants name-keyed focus [bl-44e9]
- REMOTE 9.8 residual: the last four in-process acts — Message, the Prepare/Prompt doors, the 3.6 deletes, the slash line's act arm [bl-1747]
- REMOTE read path: the reads left in process are blocked on a viewport fold, an addressing change, or a synchronous consumer [bl-f297]
- REMOTE 9.7 residual: the window's gestures still dispatch in process — the wire needs an asynchronous receipt [bl-4841]
- the landing repair's idempotence test fails intermittently on macOS: converge's first pass returns NotFound [bl-1ce0]
- REMOTE 9.7 residual: migrate the window's remaining reads onto the wire, surface by surface [bl-adcb]
- macOS main is red in the wire era: ~15 failures in test_support wire cert minting plus two stragglers — macOS-only, unrelated to git identity [bl-8626]
- REMOTE 1.2 vs 4.1 RULED: the local window is a wire client of localhost — implement the read path over loopback mTLS [bl-ae05]
- the runner has no git identity, so multiplex_bl's close leg fails on every CI/speculate build [bl-ff85]
- tool hosts III — the routing leg and the client-side executor: an invocation crosses to a tool host and the capture comes back (REMOTE §5, §9 step 7) [bl-024b]
- tool hosts II — the clients tool, loading, and routing: agents see and drive a tool host's advertised tools (REMOTE §5) [bl-c907]
- tool hosts I — advertisement and presence: a tool host's set lands in its registration, liveness is connection RAM, the workspace surface renders both (REMOTE §5) [bl-4e08]
- REMOTE §9.5 residual — the window becomes a seat: the frame's read path rides the wire [bl-ccf7]
- REMOTE §9.6 — registration and scoping: the per-workspace client registry, reply filtering, auto-registration on create, per-seat ui.json split [bl-8bbc]
- REMOTE §9.5 — the wire: mTLS listener in 'yog serve', client transport in the shell, the window becomes a seat of loopback [bl-b6fa]
- REMOTE §9.4 — the shell paints only boundary payloads: retire the raw GitTree/Agent imports from paint code [bl-1eb0]
- REMOTE §9.3 — name-based addressing: PathBuf leaves the boundary types; the wire spelling is the name, resolved at the dispatch chokepoint [bl-f5f6]
- I3/§5.2 promise a 24 h sweep of `.yog-tmp-<pid>` leftovers; only the stage-dir half was ever written [bl-e47c]
- §12's module map states three rules about itself that nothing holds: line counts drift in 90 of 147 rows, ~75 rows are out of sort order, 24 test modules carry rows [bl-273c]
- drive beat for the unseeded first turn: S0 step 5 has never been driven by any wire beat [bl-9e10]
- one-time sweep: DESIGN sentences that describe a mechanism nobody wrote [bl-9639]
- the shared drive 'gesture' helper has no deadline: a yog that dies mid-run hangs every windowed verb forever instead of failing [bl-5cf7]
- :CONTROLS is a hand-listed scan with ID_IDENTS' failure mode: a new widget constructor is silently unpoliced [bl-8e7a]
- a named sender's deposit still paints the raw id: header_line floors it at ladder rung three and never asks the name fact [bl-b6d0]
- REMOTE §9.2 — Reply gains a strict decode side, with encode-decode round-trip tests over the whole Reply surface [bl-7067]
- consecutive_fires beat is probabilistic: post-fire mint seeds re-roll from real entropy, so the acceptance fixture races lernie's word pool [bl-dd3d]
- branch cleanup runs only where nothing leaks: trap speculate-gate's delete, and widen the prune to every branch with no open PR [bl-1ea9]
- N>1 mutating fan: materialize isolated candidate attempts through balls' attempt capability and bind each variant [bl-8746]
- three S6-T1 beats have not run since 2026-08-11 and their first real-substrate execution is unproven [bl-1061]
- freeze project instructions into agent context with visible provenance [bl-aa8b]
- two docs still send run logs to docs/drive-logs/, a directory bl-244f burned [bl-514e]
- narrow-tall windows are an untested size class: the conversation pane lays rows LEFT of its own edge and clips them there, and §11 rule 1 is asserted on one edge of two [bl-7414]
- one ball body with an absolute home path wedges EVERY bl op in the checkout — the store leak gate scans the whole store, not the file the op touched [bl-1007]
- the §8.3 Login roster is reachable from the empty state, before a turn is spent [bl-3b62]
- boundary-complete the inspector reads: the transcript, steps, files, rail and inbox surfaces have no headless spelling — the chats themselves are unreachable by any face but the window (REMOTE.md §9 step 1) [bl-6233]
- a config-kind dispatch error renders a remedy beside the reason, and the picker names the row that failed [bl-dd7f]
- the naming scan enumerates field names, so the same fact under a new name evades it — this has now recurred verbatim [bl-45c7]
- 'inference — a model call is streaming' paints twice on the one conversation surface: the altitude-1 header and the flight strip [bl-3f70]
- the descent graph needs a seat: where V1.3's two edges are drawn now that the gutter is gone [bl-5cf8]
- an explicit prompt gesture: fire inference from the current state — the lernie nudge; a failed first turn re-dispatches in place [bl-9bef]
- the ten brazen provider rows paint twice in the same frame: once in the Login pane and once in the Config pane's provider table [bl-20cb]
- the macos CI job has never been green: 14 tests, two root causes — de-gated by bl-0158, still unfixed [bl-1015]
- seven shapes of a test that proves nothing: land the mechanical checks, because documenting the shapes has already failed [bl-70b8]
- the acceptance fixture's workspace has no config lineage, so every §9.4 picker beat stops at 'cannot read roles:' [bl-a842]
- BLOCKED on lernie bl-22a5 — the retarget gesture: move a live conversation onto the config the picker just wrote [bl-2d19]
- no branch model is written down, so 'one branch' is a habit two agents happened to keep [bl-7066]
- subtract the retired roster walk: AppModel::roster_step, Pick::Step and attention::step are dead outside their own tests [bl-0bfd]
- coverage debt: S2 and S9-S18 are graduated rungs with in-crate tests and ZERO real-substrate drive beats [bl-bb20]
- activity and inbox rows elide the informative tail of a path or agent id and keep the invariant prefix, so every row scans as the same string [bl-3aa1]
- DESIGN 12's module map is missing 23 source files, and 'minted workspace' survives in src prose that 3.1 forbids [bl-9f72]
- the ops trail is 99% drift noise: every derivation pass has been late since 2026-08-12, one 'yog-drift late' row every 15s [bl-4b28]
- typed target binding at fire: pass the project worktree as lernie's working-directory parameter; retire the goal-prose operative channel [bl-6654]
- the picker writes context_window: 200_000 as a declared default even when the roster it just read carries the provider's real number [bl-848f]
- headless config discovery is incomplete: no lineage-file read or provider-model query [bl-dff8]
- a world search that matches nothing renders nothing at all — the frame is byte-identical to never having searched [bl-648a]
- sweep the drive beats for vacuous assertions: empty-variable and generic-string greps pass in runs where the gesture never happened [bl-f16e]
- consume lernie's mint: delete the local wordlist+draw, draw preview and fire through the crate [bl-cd38]
- the Inbox tab's empty state is a bottom-anchored '(no deposits)' that names no paved path [bl-71fc]
- DESIGN §12's shell-glue row is one ~10,000-character line, so any two agents adding a module conflict unconditionally [bl-0012]
- the empty-world masthead splits across two alignment axes: the wordmark is left-aligned while the tagline and name prediction beneath it are centred [bl-fb1c]
- S4-T4 beat defeats itself under load: until_landed retries a gesture that ADDS agents while its predicate demands an exact count [bl-0e44]
- AGENTS.md rule 6 records a lernie pin two releases stale: '=0.0.3' where Cargo.toml has '=0.0.6' [bl-bb09]
- tool roster vs the prompt cache: advertisement becomes durable, the conversation freezes its roster at the root, absence answers at invocation — REMOTE.md §5 rework [bl-bc7c]
- DECISION + destructive migration: git history, refs, tags, author metadata, and the permanent crates.io 0.0.1 artifact [bl-4f96]
- design investigation: thin-client/backend split — one yog backend on the home server, multiple UI clients sharing chats, each client exposing its machine-local tools [bl-b9a2]

## [0.0.2](https://github.com/mudbungie/yog/compare/v0.0.1...v0.0.2) - 2026-08-13

### Changes

- macos gates every release while failing: split it off the CI workflow and correct the README's claim [bl-0158]
- CI has never been green on GitHub runners: five tests assert on the host, not on yog [bl-e492]
- the public tree, at the 2026-08-13 publication audit
